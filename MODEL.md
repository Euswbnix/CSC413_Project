# 模型决策：手搓 CfC + 稠密 + 等宽 LSTM

> 前置：[REVIEW.md](REVIEW.md)（blocker 清单）、[PROTOCOL.md](PROTOCOL.md)（实验协议）。
> 本文档settle两件事：**哪些自己写、哪些 import**，以及**稠密还是 NCP 稀疏接线**。
>
> `reference/cfc.py` 是已验证的参考实现（56 行有效代码）。
> `reference/oracle_test.py` 是验证脚本，**我在本机 torch 2.8.0 + ncps 1.0.1 上跑过，
> 全部 `0.000e+00`**。

---

## 0. 决定

**边界：凡是内部结构会出现在模型图或参数表里的，自己写；其余全部 import。**

自己写：`CfCCell`（38 行）+ 序列包装（16 行）+ `LeCunTanh`（2 行）+ 接口/工厂 + Δt 与 rollout 管线 + 全部测试。
**合计约 550 行、约 21.5 人时**（其中 8 小时是测试、4 小时是 Δt harness），约 3 人日 / 1.5 对日。
相对 `pip install ncps` 方案的边际成本约 17 小时（照 ncps 写 `cfc_model.py` 本来也要约 3 小时，Δt harness 两种方案都要）。

**接线：稠密，单层，每一臂都是。** NCP 稀疏作为模型砍掉，只留一句实测的 README 句子，
代码里仅保留一个可选的、密度匹配的随机 mask，并写明删除触发条件。

**LSTM 不自己写。** 这是唯一一个其失败模式会制造**确认假设的假阳性**的组件。

---

## 1. 参考实现（已验证）

文件：[reference/cfc.py](reference/cfc.py)。符号遵循 Hasani et al. 2022 Eq.(4)（= arXiv:2106.13898 Eq.(10)），
**不用库的命名，因为库的命名是误导性的：ncps 的 `ff1` 是论文的 h，不是它的 f。**

```
x_t    = concat([z_t, h_{t-1}], dim=-1)              # 顺序是承重的，必须与 ncps 的 cat([input,hx],1) 一致
cand_h = tanh(W_h x_t + b_h)                         # 论文 h，平衡候选        (ncps ff1)
cand_g = tanh(W_g x_t + b_g)                         # 论文 g，快候选          (ncps ff2)
γ_t    = sigmoid((W_f x_t + b_f) ⊙ Δt_t              # 论文 f，液态时间常数    (ncps time_a)
               + (W_o x_t + b_o))                    # 偏置头，Eq.(4) 里没有   (ncps time_b)
h_t    = (1 - γ_t) ⊙ cand_h + γ_t ⊙ cand_g           # 逐单元凸插值
y_t    = W_out h_t + b_out                           # 共享读出，强制
```

`W_* ∈ R^{H×D}`、`b_* ∈ R^{H}`，`D = I + H = 96`（I=32 是 CNN 特征宽，H=64 是隐宽）。
输出与新状态是同一个张量。`h_0 = 0`，无可学习初始状态（两个参考实现都是这样）。

**`γ_t ∈ (0,1)` 所以 `h_t ∈ (-1,1)` 严格成立 —— 这正是读出层不可省的原因（REVIEW B1）。**
实测：对着有 ±100° 尾部的度数标签，tanh 饱和的状态加一个 Linear 读出跑 4,000 步 AdamW
只能到 **87.2°**（RMSE 3.87°），加不加 weight decay 都一样；而在标准化标签上能到 **99.8°**（RMSE 0.35°）。
**有界状态 + 度数标签是一个尺度问题，但它看起来完全像"循环 cell 处理不了急弯" —— 一个凭空捏造的架构发现。**

### Δt 的单位（不是装饰）

`Δt_t = (timestamp_t − timestamp_{t−1}) / median(Δt over train split)`，**中位步长即 Δt = 1.0**。

实测 `‖∂L/∂W_f‖ / ‖∂L/∂W_o‖` 在初始化时：

| Δt 单位 | 梯度比 |
|---|---:|
| **中位数归一化（Δt=1.0）** | **1.0000** |
| 原始秒 @30fps（Δt=0.0333） | **0.0333** |
| 帧索引（Δt=30） | **30.0000** |

这个比值**恒等于 \|Δt\|** —— sigmoid 的导数因子在两个头之间完全抵消。
所以用原始秒会让时间通路相对于一个"不用 Δt 也能干同样事"的偏置头**梯度饿死 30 倍**，
**于是门学会忽略时间，README 的连续时间声称变成假的，而每个测试仍然是绿的。**
用帧索引则相反，门会饱和。

> （这一条我修正了 agent 报告里的一个数：它写帧索引情形是 8.0，实测是 30.0。
> 论证方向没问题，数字要改。）

### 可选 backbone（flag，默认关）

`x_t ← φ(W_b·concat([z_t,h_{t-1}]) + b_b)`，`φ(v) = 1.7159·tanh(0.666·v)`（LeCun 1998 缩放 tanh，无参数），
然后 `D = backbone_units`。**作为 flag 存在**：4 行代码，给 Hyperparameter Tuning 一个真实的开关数据点，
并让 oracle 能覆盖 backbone 路径（已验证 0.0）。

**Headline 运行用 `backbone_layers=0`**：`backbone_units=128` 时 backbone 占 45,440 参数中的 12,416，
且每个时间步多加一整个非线性层 —— **而 LSTM 没有。这正是重写要消除的那个混淆。**

### 初始化（声明的，不是继承的）

每个权重矩阵 `nn.init.xavier_uniform_(W, gain=1.0)`，每个偏置 `nn.init.zeros_`。
gain 1 是论文记录的；零偏置与 ncps 的 Keras 移植一致。

实测后果（I=32, H=64, h=0, Δt=1）：**γ 的 mean 0.501、std 0.190、饱和到 (0.01,0.99) 之外的单元占 0.00%**
—— 门坐在高梯度区。**这三个数必须写进 `tests/test_cfc_anchors.py`，因为 oracle 在结构上对初始化盲**：
一个用 `N(0,8)` 初始化、偏置 −12、起步就 93.6% 门饱和的 cell，能以 0.0 通过**每一条** oracle 断言；
torch 默认的 `kaiming_uniform_(a=√5)` 也能，而它给出的门窄 40%。

---

## 2. 正确性计划 —— 顺序是本文档最重要的一条

**要防的失败很具体：一个细微错误的 cell 会训练、会收敛、会在直行 bin 打败 predict-0、
然后落在 LSTM 后面 5-15%，与一个真正的架构发现无法区分 —— 然后它进了 20 分的 Justification。
没有任何 loss 曲线能检测到它。**

### 为什么 oracle 必须排第二：我亲手复现的反例

**把 lerp 方向倒过来、再把头映射交换一下，可以在 oracle 上拿到精确的 `0.000e+00`。**

```
sigmoid(-u) = 1 - sigmoid(u)
```

所以 `γ·cand_h + (1-γ)·cand_g`（错的方向）配上 `h_head←ff2, g_head←ff1` 的映射，
输出与正确实现**逐位相同**。我在 float64 下实测，4 个固定 Δt、逐样本 Δt、
**以及按名字比对的梯度检查**，全部 `0.000e+00`。

**这是一个团队会自然到达的状态**：把映射字典来回换，直到 pytest 变绿。
而 oracle 不会阻止它 —— **一个错的 cell 加一个补偿性双射，是整个流程的不动点。**
其余测试也都拦不住：零不动点测试两边都给 0（γ=0.5，两个候选都是 `tanh(0)`），
`|h|<1` 成立，`gradcheck` 通过，"Δt 只经 f_head 进入"也通过（f_head 清零后两个候选都与 Δt 无关），
分块等价性和 batch 独立性也通过。

**然后 README 和模型图会描述成代码的反面。**

### Layer 1 — 解析锚点。**与 cell 同一次坐下写完，在 oracle 之前。** 约 110 行，不依赖 ncps。

七条锚点，每条都是 README/模型图所做的一个断言，且**不引用任何库**：

1. **门极限 —— 堵住那个洞的锚点。** `f_head.weight` 清零、`f_head.bias = +10`、`o_head` 清零：
   Δt=50 时 `h → tanh(g_head(x))`。`f_head.bias = −10` 时 `h → tanh(h_head(x))`。
   **实测：正确实现残差 0.00e+00，倒置实现 1.40–1.78。**
   这条**内在地**钉住了"门把状态推向哪个候选"，所以任何命名置换都无法同时满足它和 oracle。
2. **纯偏置闭式。** 全部权重清零、`z=0`、`h=0` ⇒ `h_1 = (1−σ(b_o))·tanh(b_h) + σ(b_o)·tanh(b_g)`，用 numpy 算。把每个偏置钉到正确的项上。
3. **Δt 只经 f_head 进入。** f_head 清零后，输出在 Δt ∈ {0, 0.1, 1, 10} 上逐位相同。模型图面板 2 核心论断的机械证明。
4. **恒定 Δt 的退化性。** 一个 `(f_head.weight·c, f_head.bias·c)` 的 cell 在 Δt=1 下等于原 cell 在 Δt=c 下。
   **这把本项目最有价值的那句诚实话机械化了** —— 在均匀帧率下门就是 `sigmoid(affine(x_t))`，
   CfC 没有任何连续时间内容。**一个既是 README 论断、又是通过的断言的东西。**
5. **Δt 敏感性 + 形状契约。** 跨 batch 变化的逐样本 Δt 必须产生不同的行；
   `assert dt.shape == (B,1)` 必须**拒绝** `(B,)` 和 `(B,T)`。
   这个断言关掉整个 ncps bug 类，**包括 B == H == 16 时那个不报错、错 0.178 的静默错轴广播**
   —— H∈{16,…} 的宽度扫描在 T=16 下会正好撞上它，而 batch 独立性测试抓不到它（单行重跑会以同样方式错广播）。
6. **初始化统计。** 断言 Δt=1、h=0 时 γ 的 mean ∈ [0.45,0.55]、std ∈ [0.15,0.23]、饱和单元数为 **0**。
7. **有界性，两半都要。** 随机输入和 200 步 rollout 上 `|h| < 1` 严格成立（**把 B1 机械化，免得有人把读出层优化掉**），
   配上：在标准化标签上回归器输出可以超过 ±100。外加 float64 下的 `torch.autograd.gradcheck`，
   **`z`、`h` 和 `dt` 都 requires_grad**。

### Layer 2 — oracle。约 95 行。证明"我们的 cell **就是**参考实现"。

`ncps==1.0.1` 放 `requirements-dev.txt`；源码 vendored 进 `tests/reference/`，带 Apache-2.0 头和 commit SHA。
**`tests/test_hygiene.py::test_oracle_is_available` 必须 FAIL 而不是 skip，除非 `ALLOW_NO_ORACLE=1`。**
一个被 skip 的 oracle 与没有 oracle 无法区分，而在 grader 裸跑 `pip install -r requirements.txt` 时，
Code/Documentation 的核心论断会静默地永不执行。

让权重搬运有定义的三件事：

- **构造参考实现时显式传 `backbone_layers=0, backbone_units=0`** —— **不是它的默认值**；
  否则 ncps 会静默建一个 128 单元 LeCun MLP，头的形状就不一样了。
- **拼接顺序钉死**为 ncps 的 `torch.cat([input, hx], 1)`，在我们的 `cat` 旁边写断言注释。
  **不编码进搬运里**，所以搞反了会有 O(1) 的偏离被抓到 —— 这正是测试必须搬真实随机权重、而不是只检查形状的原因。
- **映射写成带注释的 dict**，每次 `copy_` 前两边都断言形状 `(H, D)`，方向是**参考 → 我们**，让缺头或多头暴露出来。

**我在本机实测（torch 2.8.0 / ncps 1.0.1 / float64 / CPU，`reference/oracle_test.py`）：**

| 配置 | max\|diff\| |
|---|---|
| 稠密 forward，Δt ∈ {1.0, 0.37, 3.0, 0.0} | **0.000e+00** |
| 稠密，逐样本 Δt `(B,1)` | **0.000e+00** |
| 全部 8 个命名张量的梯度（非对称 loss） | **0.000e+00** |
| `backbone_units=128, backbone_layers=1` | **0.000e+00** |
| T=9 序列包装，输出**与**返回的 `hx` | **0.000e+00** |
| 分块等价（4+5 携带 hx vs 一次 9） | **0.000e+00** |
| 严格 `\|h\| < 1` | max 0.949972 |
| 四头 mask vs ncps | **O(1)，预期偏离，刻意的，不是 oracle 行** |

float32 下用 `rtol=1e-5, atol=1e-6` 再跑一遍确认生产 dtype：
代数等价的重排（`a(1−g)+gb` vs `a+g(b−a)`）在 float32 下差 1.19e-7、float64 下 1.1e-16，
**所以 float64 那一行才是抓代数错误的那条，float32 抓的是 kernel 差异。**
在 5090 上再跑一次 CUDA float32（`atol=1e-5`），因为 TF32 会破坏逐位一致。

> **"代数相同"(0.0) 与"任何真 bug"(≥0.1) 之间有十一个数量级的余量 —— 把这句写进 README，
> 因为一个需要调的 tolerance 就是一个在藏失败的 tolerance。**

四头 mask 那一行：那是我们真正会训练的配置，而**没有任何参考实现计算它**，
所以它由 Layer 1 的不变量覆盖 —— 被 mask 的条目梯度恰好为零，**在全部四个头上检查**，不是一个头。

### Layer 3 — 语义与结构。约 155 行。

- **分块等价**：T=32 一次调用与两次 16 携带 `hx` 逐位相同（实测 0.0）。
  **这件事授权了 M7/M8 要求的有状态 rollout；没有它整个评测协议只是一个假设。**
- **长度无关**：T=32 的前 8 个输出等于 T=8 的运行（0.0）。把 §2 只是断言的变长claim演示出来。
- **batch 独立**：批量前向的第 i 行等于该行单独跑（9.4e-16）。
- **参数计数**：公式 vs `named_parameters()`，H ∈ {16,32,64,128}；断言 `nn.LSTM − CfC == 4H`；
  断言 `sum(all) == sum(trainable)`；断言 `mask` **按身份**是 buffer
  （`any(p is cell.mask for p in cell.parameters()) is False`）且**在** `state_dict()` 里。
- **编码器形状取自 forward hook** —— **被断言的形状就是模型图面板 1 的标注**，所以图不会与代码漂移。
- **`tests/test_hygiene.py`**：grep `models/` 和 `train.py`，任何 `ncps` 出现即失败，
  **让"仅测试期依赖"成为事实而不是承诺**。
- **`tests/test_overfit_one_batch.py`**（`@pytest.mark.slow`）：一个**含 |angle| > 40° 帧**的 batch，
  300 步，逐帧 MAE < 1°，在标准化标签上。B1 的硬化门提升进测试套件，每次重构后重跑 ——
  **而它是所有设计里唯一一条会在标签尺度欠拟合上失败的断言**（原始度数 87.2° vs 标准化 99.8°）。
- **梯度健康度日志，不是测试**：每 epoch 记 `‖∂L/∂W_f‖/‖∂L/∂W_o‖` 和 `mean|tanh(g)−tanh(h)|`。
  **当两个候选在训练中收敛到一起**（这是降 loss 最便宜的方式，因为任一候选单独都是个堪用的 RNN），
  `∂h/∂W_f` 在任何 Δt 下都消失，**项目要研究的那个机制会在第 3 个 epoch 静默死掉**。
  如果它塌了，那是一个结果，不是一个麻烦。

整套 `make test` → `pytest -q`，几秒；加 `-m slow`。

---

## 3. 接线判决：稠密

### 为什么不走 ncps 的 NCP 稀疏，连消融都不走

`WiredCfCCell` **一次改变四件事**：
它变成一个**三层堆叠 RNN**（每步深度 3，对 LSTM 的 1 —— 我实测 `num_layers = 3`）；
它**重新划分状态**为 [19 inter | 12 command | 1 motor]；
它**移除 backbone**（而 `CfC.__init__` 在 wiring 模式下传 backbone 参数会直接 `raise`，
所以稠密路径和 wired 路径是**结构上不同的模型**）；
它**只 mask `ff1`/`ff2`**，而 `time_a`/`time_b` 保持全稠密。

**卖成一个旋钥，实际是四个。而这个旋钥几乎不转** —— 我实测：

| `sparsity_level` | cell 级密度（全部四个头） |
|---|---:|
| 0.5（默认） | **84.0%** |
| 0.9（最稀疏合法值） | **71.2%** |

因为每层 mask 的 hidden-to-hidden 块在 `wired_cfc_cell.py:38-43` 里是硬编码的 `np.ones((H,H))`。
而 `recurrent_command_synapses`（AutoNCP 四个 fanout 超参之一）**在 CfC 路径上从未被读取**。

> **通过这个库做的"稀疏 vs 稠密"实验，实际是拿一个三层 84% 稠密的网络去比一个单层 100% 稠密的网络，
> 然后把差异报告成一个关于生物连接性的发现。**

### 为什么也不自己写一个 NCP 仿制块 mask

它用一个不可引用的组件替换一个可引用的组件：两句话的描述里已经藏了六个算法决定
（块划分、四类允许连接、fan-in 三元组、抽取规则、种子、孤点修复 pass），
全都必须发表才能达到本课程在别处适用的复现标准，而这笔 README 预算花在了审查排在**第一顺位砍**的消融上。
在 fan-in (6,4,4) 下它让 92.3% 的权重死掉，可能**根本训不起来**，
修起来要一个四次运行的密度扫描 —— 而那本身是对 M5"只锁三个超参"的第四个搜索超参。

### 实际交付什么

cell 里保留 5 行 `mask` 钩子：一个 `register_buffer`（**永不是 `nn.Parameter`**），
默认施加到**全部四个头**，带一个 `mask_gate=False` flag 复现 ncps 的两头约定，好让 mask 代码路径有 oracle 行。
**当且仅当第 4 周有余裕**，跑**一次**消融：种子化的密度匹配随机 mask，`torch.rand(H, I+H) > p`，
**同一个 H、同一个深度 1 —— 同一个 cell，±稀疏性，别的什么都不动**，一句话说完，无需引用。
**那是 `ncps` 在结构上无法表达的单变量消融。** 如果没跑，提交前把 `mask` 参数删掉，README 的论证一寸不动。

### 这对 CfC-vs-LSTM 的混淆做了什么 —— 真正的回报

**之前**（ncps + AutoNCP vs `nn.LSTM`）有**七件事**不同：更新方程、状态维度、连接稀疏性、
每步深度（3 vs 1）、backbone 有无、参数量、读出结构。审查判定 headline 比较**不可解**。

**之后**（手写稠密 CfC H=64 vs `nn.LSTM(32,64)`）：
**同样的输入宽 32、同样的状态宽 64、同样的稠密连接、同样的深度 1、同样的共享 CNN 编码器、
同样的共享 `Linear(64,1)` 读出、同样的优化器、同样的裁剪、同样的数据管线，24,832 vs 25,088 参数。**

七个差异塌缩成 **1 个有意的差异 + 2 个具名子机制 + 1 个量化的伪影**：

1. **有意** —— 更新方程：两个 tanh 候选的门控凸组合 vs LSTM 的加性 cell state。
2. **具名** —— **CfC 没有独立的 `c_t`，因此没有加性恒等通路**。
   这是一个**先验的、有原则的理由去预期 LSTM 不落下风**，
   **它把"LSTM 赢了"从一个借口变成一个解释。**
3. **具名** —— CfC 的门是 Δt 的显式仿射函数，LSTM 的不是。
4. **伪影** —— 4H = 256 个参数，就是 cuDNN 强加给 `nn.LSTM` 的冗余第二偏置向量，
   **而且是偏向基线的：我们没有把对手缩小。**

> 这也比 CfC 论文自己的协议更好 —— 他们拿 33,089 循环参数的 LSTM 比 4,184 的 CfC。

### NCP 图和 Lechner 框架怎么办

`wiring.draw_graph()` 砍掉，M9 的面板 2 是**被替换、不是丢失**。四个面板变成：

1. CNN 编码器，每条箭头的张量形状**取自活的 forward hook**，并在 `tests/test_encoder_shapes.py` 里断言
   —— 所以图不可能与代码漂移。我实测的形状链：
   `66×200×3 → (24,31,98) → (36,14,47) → (48,5,22) → (64,3,20) → (64,1,18)`，**flatten = 1152**。
2. **一个 CfC 步** —— 96 维拼接、四个各 6,208 参数的标注方框、两个 tanh、`×Δt`、`+`、sigmoid、
   以及 lerp 节点，每条箭头标形状、每个框标参数量。
3. 三步展开带，状态箭头，共享的 `Linear(64,1)` 画一次并虚线连到每一步。
4. 一个窗口的预测 vs 真值轨迹。

> **面板 2 是决定 Model Figure(4) 的那一个，而它恰恰是代码还在调 `WiredCfCCell` 时无法诚实画出的那一个 ——
> 一张渲染出来的 NCP 图是一张「接线」的图，不是一张「前向传播」的图，而这一项是按前向传播评分的。**

Lechner 框架以唯一站得住的形式存活：**NCP 论文主要把稀疏接线呈现为一种可解释性/可审计性的提供物
（它的标题就是 "enabling auditable autonomy"），而 CfC 作者把这个家族的鲁棒性归因于 cell 语义。**
这不需要任何依赖。引用先例，让这次砍读起来像一次测量而不是一次撤退：
**作者们自己的 PyTorch 参考实现（`raminmh/CfC`，论文 Code Availability 里点名的那个）完全没有 wiring 支持**，
而稠密 CfC 变体（约 4,184 循环参数）就是他们在 1 km 公共道路上开真 Lexus 的那个配置。
另外 REVIEW F10 已经移除了那个 claim：`AutoNCP(19,1)` 给 206 个突触而论文是 253，
**所以"我们复现了他们的电路"从来就不可得。**

---

## 4. 参数量

**公式**（无 backbone，`D = I + H`）：

```
CfC cell        = 4 · [H·(I+H) + H]  =  4·H·(I + H + 1)
带 1 层 backbone = D_b·(I+H) + D_b + 4·(H·D_b + H)
读出             = H + 1
```

**在 headline 配置 I=32, H=64（D=96）下，已对 `named_parameters()` 断言：**

| 模块 | 算术 | 参数 |
|---|---|---:|
| `h_head` `Linear(96→64)` | 64·96 + 64 | 6,208 |
| `g_head` `Linear(96→64)` | 64·96 + 64 | 6,208 |
| `f_head` `Linear(96→64)` | 64·96 + 64 | 6,208 |
| `o_head` `Linear(96→64)` | 64·96 + 64 | 6,208 |
| **CfC cell** | `4·64·(32+64+1) = 4·64·97` | **24,832** |
| 读出 `Linear(64→1)` | 64 + 1 | 65 |
| CNN 编码器（每一臂共享） | 1,824 + 21,636 + 43,248 + 27,712 + 36,928 + 36,896 | **168,244** |
| **CfC 臂合计** | | **193,141** |

**循环层占模型的 12.9%，共享感知编码器占 87%。**（H=32 时只占 4.7% —— 这是 H=64 当 headline 的部分原因：
**比较需要那个「不同的部分」在模型里占一个非平凡的份额。**）

**匹配对照（I=32，实测）：**

| 臂 | 循环公式 | H | 循环参数 | vs CfC |
|---|---|---:|---:|---:|
| **CfC（我们的）** | `4H(I+H+1)` | 64 | **24,832** | — |
| **`nn.LSTM`（基线，原样导入）** | `4H(I+H) + 8H` | 64 | **25,088** | **+256 = 恰好 4H，1.03%** |
| `nn.GRU` 若保留 | `3H(I+H) + 6H` | 76 | 25,080 | +1.00% |
| 纯 CNN 对照 | `Linear(32→1)` | — | 33 | — |

**4H 恒等式在每个宽度上精确成立，我实测过：**
H=16 → 3,136 vs 3,200；H=32 → 8,320 vs 8,448；H=64 → 24,832 vs 25,088；H=128 → 82,432 vs 82,944。
`tests/test_param_counts.py` 在四个宽度上断言 `count(nn.LSTM) − count(CfC) == 4H`。
**H 由规则固定在 64 并写明，不扫** —— M5 的三个搜索超参仍然恰好是 lr、T、k；
宽度扫描活在公式测试里，**不花任何训练运行**。

**每一臂的冻结参数都是零**：`sum(p.numel() for p in m.parameters())` 等于可训练计数，断言之。
免费且值一句话的对照脚注：**参考库对一个可训练数为 5,544 的 CfC 报告 6,898 —— 高估 24.4%** ——
因为它把稀疏 mask 注册成 `nn.Parameter(requires_grad=False)`；我们用 `register_buffer`。
**PROJECT.md §3 的那行代码会把错数字直接打印进 README。**

`scripts/param_table.py` 从测试所断言的同一批公式函数生成上面每一张表，
**所以 README 的表是生成的输出而不是抄的**，而 `make test && python scripts/param_table.py` 就是
"我怎么验证这个？"的答案。

---

## 5. 什么仍然 import，以及具体为什么

**1. `nn.LSTM` —— 基线。** 唯一一个失败模式会制造**确认假设的假阳性**的组件，
也是权重搬运 oracle 在结构上盲的那一个：一个 `forget_bias = 1.0` 的手写 LSTM 在补偿搬运下
与 `nn.LSTM` 吻合到 8.3e-17，任何门顺序约定或不同权重尺度也一样被搬运吸收掉。
**"CfC 输了"是一个团队会去审查的错误否定；"CfC 赢了"来自一个被悄悄削弱的基线，没有人会质疑。**
手写能买到的 1.03% 精确性不值这个代价，而且残差本来就偏向基线。
显式传 `batch_first=True`（ncps 默认 True，torch 默认 False）。
**这一次运行同时充当 M9 的两个角色**：参数匹配基线 **和** 反稻草人的不受约束基线 —— 因为同 H 下它就匹配到 1.03%。

**2. `nn.GRU`（若该臂复活）。** 单偏置的手写 GRU **不是** `nn.GRUCell` 的重参数化 ——
`b_hn` 进在 `r ⊙ (W_hn h + b_hn)` 里面，所以偏置折不掉；而且不像 CfC 和 LSTM，**它没有 swap-in 退路**。

**3. LTC。** 九个参数组、手调初始化范围、需要 wiring 对象、每时间步物化六次 `(B,U,U)` 张量
并为 BPTT 全部保留（约 CfC 每步成本的 28 倍）。
更糟的是一个陷阱：ncps 的 `_init_ranges` 是为 Identity 路径写的，
所以硬编码 Softplus —— 那是**正确**的稳定性决定，因为 `LTCCell` 默认 `implicit_param_constraints=False`
且库里从来没有地方调 `apply_weight_constraints()` —— 会**静默地把每个正量翻倍**
（`softplus(0.5) = 0.974` 对名义中点 0.5），**而 oracle 看不见，因为搬运复制的是 softplus 之前的原值。**
砍掉该臂（REVIEW §5 第 3 条）。用 `no_gate`（`h = tanh(h_head) + γ⊙tanh(g_head)`）替代 ——
一行，论文自己命名的变体，**它消融掉的是造成量程问题的那个凸组合约束**，而不是"我们用没用 solver"。

**4. `AutoNCP` 和 NCP 接线生成器。** 约 75 行里约 6 行是建模（fanout 算术）、
约 35 行是没有概念内容也没有 rubric 价值的修复 pass 记账。

**5. 卷积、矩阵乘、autograd、Adam、cosine schedule、梯度裁剪、DataLoader。**
按构造在每一臂之间逐字节保持一致，**所以写它们只能引入一个会被误归因给架构的 bug** ——
一个偏置修正写错的手写 Adam 会让某一臂收敛变差然后被归咎于 cell，**这是可得的最糟的静默失败**。
而 rubric 要求你**解释**前向传播，不是实现 GEMM。

**6. `pure` / Cf-S 模式。** 参考实现里在初始化时就是坏的
（`init_weights` 用 xavier 覆写了 `A` 和 `w_tau`，把状态钉在 `A` 周围一个无法零中心化的带里），
**所以没有正确的 oracle 可对。**

---

## 6. Day-1 门与逃生舱

### Day-1 门（第三方可检查，在任何训练运行之前）

第 1 天结束时，按此顺序：
1. `models/interface.py` 存在，含 `SteeringRegressor(encoder, rnn, readout)` 和一个可互换的 `rnn` 模块
   —— **在 cell 之前写**，因为它是让逃生舱只需一行的东西；
2. `models/cfc.py` 写完；
3. `tests/test_cfc_anchors.py` 绿 —— 具体是门极限那一对、纯偏置闭式、Δt 形状契约；
4. `tests/test_cfc_oracle.py` 在稠密配置、float64、`atol=0` 下绿。

**GO**：第 1 天结束时四项全绿。
**NO-GO → 立刻走逃生舱**：若 (3) 不绿，或团队无法承诺把 (3) 与 (2) 在同一次坐下写完。

> **一个没有锚点的手写 cell，严格劣于一个导入的 cell**：它承担了全部静默失败风险而没有任何缓解，
> 而失败只在 grader 把代码和图放在一起读的时候才浮现。
> 锚点是整个预算里最便宜的一项（2.5 小时），也是唯一能在 remap 置换攻击下存活的东西，
> **所以它们也永不在砍单上。**

### 第 1 周门（按真正重要的外部可观测量）

cell 轨道完全可以对着随机张量测试、不需要任何数据，**所以它必须由与数据轨道不同的人负责，
而且资源争用时数据轨道优先**。

若第 1 周结束时数据管线还没有产出经验证的帧-标签对齐 memmap（66×240）——
包括 REVIEW B5 的"`data.txt` 第 i 行就是 memmap 第 i 行"规则、那个约 161 个文件/行不匹配的断言、
以及那个**唯一能抓出整序列差一格的、叠了转角的 10 秒 GIF** —— 那么
**把 cell 工作冻结在当前绿的状态，两个人都上数据。**
**一个错位的管线让项目里每个数字作废；一个错的 cell 只让一条臂作废。**

还有一件三份设计都没放进时间线的事：**有更早截止日的 14 分 Proposal（REVIEW B6）**，
包括那个至今不存在于任何地方的分工段落。**在第 2 周之前给它排期。**

### 逃生舱 —— 已证明可逆，不是希望可逆

**oracle 测试同时就是退路证书。** 因为我们的 `CfCCell` 和
`ncps.torch.CfCCell(I, H, mode="default", backbone_units=0, backbone_layers=0)`
是**逐位相同的函数、可训练参数量也相同（24,832 = 24,832）**，
`models/cfc.py` 的 cell 可以在项目任何时点被库的那个替换掉，`SteeringRegressor` 不变，
**不需要重跑任何一次实验。**

**关键：只换 *cell*，保留我们那 16 行的包装。**
`timespans` 的 bug 活在 ncps 的**包装**里（`timespans[:, t].squeeze()`），不在它的 cell 里
—— 已验证其 cell 处理 `(B,1)` 的 Δt 是 max|diff| = 0.0 —— **所以保留我们的包装连不规则 Δt 那一臂都保住了。**
唯一失去的是四头 mask 消融，而那本来就排在第一顺位砍。

**把这句话逐字放进 oracle 测试的 docstring，并把退路构造参数也记在那里**，因为
`ncps.torch.CfC(32, 64)` 用默认值是 **45,440** 参数，比预期的 24,832 **大 83%**。

---

## 7. README 写法的改变

**Model 段围绕方程写，不围绕库写。** 三句现在写得出来了（因为代码存在）：

> *我们的循环层是一个手写的闭式连续时间（CfC）cell，遵循 Hasani et al., Nature Machine Intelligence
> 4, 992–1003 (2022) Eq.(4)（= arXiv:2106.13898 的 Eq.(10)）。每一步我们把 32 维 CNN 特征 `z_t`
> 与前一个 64 维状态 `h_{t-1}` 拼接，施加四个独立的 `Linear(96→64)` 映射，产生两个候选状态
> （各经 tanh 压缩）以及一个时间门的斜率与偏置；新状态是门加权的凸组合
> `h_t = (1−γ_t)⊙tanh(W_h x_t + b_h) + γ_t⊙tanh(W_g x_t + b_g)`，其中
> `γ_t = sigmoid(f(x_t)⊙Δt_t + o(x_t))`，`Δt_t` 是帧间实测间隔，单位为训练集中位间隔。
> 该门是论文对液态时间常数 ODE 闭式解中指数衰减 `exp(−[w_τ + f]t)` 的刻意平滑替代，
> 所以帧间间隔越大，状态被推得越靠近快候选。*

**然后是诚实段落，它自己独立成块，而且值真分：**

> *我们实现的是参考形式而非 Eq.(4) 的字面形式，有四点差异。
> 我们的门带一个论文方程里没有的、与时间无关的学习偏置 `o(x_t)` ——
> 每个参考实现都加了它，因为 `σ(−f·t) = ½` 在 `t = 0` 处成立，
> 而论文自己声称的"在 t=0 附近约为 1"这个性质否则无法实现。
> 我们用无 backbone 变体，所以 f、g、h 是 `[z_t ; h_{t-1}]` 的三个独立线性映射而非共享 backbone 上的三个头；
> 参考库自己在 wired 模式下也省掉 backbone。
> 论文称为 f 的那个头在代码里是无约束的，所以学到的逆时间常数可以为负，
> 论文的 Theorem 1 覆盖不了训练后的模型。
> 而因为我们的帧接近均匀采样，Δt 近乎恒定、门退化为 `sigmoid(affine(x_t))` ——
> **在规则采样的视频上，CfC 相对 LSTM 因此不是"连续时间 vs 离散时间"，
> 而是在同等参数预算下的一套不同的门控代数。**
> §Results 报告一个 Δt 真正变化的丢帧变体，那才是 CfC 被设计来应对的体制。*

**Code 段**：一个短小节，按**锚点、然后 oracle**的顺序命名，附实测数字；
一句话披露 `ncps` 是 vendored read-only 在 `tests/reference/` 下、带 Apache-2.0 头和 commit SHA、
且模型从不 import 它。然后单独一段：

> *这证明我们的 cell **就是**参考实现。它**不**证明参考实现就是论文的 Eq.(4) —— 它不是；
> 差异见 §Model，最大的一处是时间门里一个论文方程并不包含的第二个学习头。*

**§10 反模式清单新增**：❌ 画一个写着 CfC 的方框。
