# PROJECT.md 评审报告

> 针对《连续时间循环模型的端到端转角预测》计划，逐条对照 CSC413 proposal(14 分) +
> final project(100 分) 评分标准的审查结果。
>
> 标注 **[已亲自验证]** 的条目，我从 ncps 1.0.1 wheel 源码 / PyPI API / GitHub HTTP 状态码
> 直接核对过，不是二手转述。其余条目标注了来源。

---

## 0. 总结论

**方向是对的，而且选得比大多数本科项目好。** 计划里 §5.1(零基线)、§5.2(时序泄漏)、
§5.3(参数量表述)、§10(反模式清单) 四处是真正的加分项——尤其 §5.2，这个数据集
99% 的公开复现都栽在打乱后切分上，把这件事讲清楚本身就比任何精度数字更值
Justification 的分。所以问题不是"选题有毛病"。

**最大的风险是：计划的核心论断"CfC 在扰动下退化更平缓"，按目前设计根本测不出来。**
单 seed、亮度增强同时出现在训练和 OOD 测试两侧、退化曲线的纵轴至今没定义、急弯 bin
可能只包含个位数的独立转弯事件——这四件事叠加，最终那张图无法区分"架构差异"和
"初始化噪声"。

**第二风险：§6.2 写的代码跑不起来；就算跑起来，CfC 的输出被 tanh 限制在 (-1,1)，
而标签跨到 ±100°**——§9 第 5 步的单 batch 过拟合会在第一天就失败，而且失败现象是
"模型学不动"，没人会想到是输出量程问题。

**第三：占 10 分的 Advanced Concept 押在一个这个数据集支撑不了的"明确物理对应关系"上，
而且 §6.1 把图像存成 66×200，横向平移时一个真实像素都没得移。**

**第四：20 分的 Justification 被 §8.2 一行字指派给了全项目最贵、与 rubric 最不对齐的
实验；而同样 20 分的 Code/Documentation 在整份计划里一个字都没提。**

四件事都能在这周内解决，其中三件只需要改文档、不需要跑任何东西。

---

## 1. 开工前必须解决的六件事

### B1. CfC 的输出被限制在 (-1,1)，表达不了 ±100° 的标签 **[已亲自验证]**

`ncps/torch/cfc_cell.py` 默认 mode 下：

```
ff1 = self.tanh(ff1); ff2 = self.tanh(ff2)
t_interp = self.sigmoid(t_a * ts + t_b)
new_hidden = ff1 * (1.0 - t_interp) + t_interp * ff2      # 两个 tanh 的凸组合 → 严格 ∈ (-1,1)
```

`wired_cfc_cell.py` 的 `forward` 返回最后一层的 `h`(即 motor neurons)，
而 `cfc.py` 在 `proj_size is None` 时 `self.fc = nn.Identity()`。
**所以 §6.2 的读出结构在物理上不可能输出 (-1,1) 之外的值。**
同样的坑也砸在对照组上——`nn.LSTM` / `nn.GRU` 的输出同样是 tanh 有界的。

修法：
- `AutoNCP(units, output_size=8)` + 自己接 `nn.Linear(8, 1)`(9 个参数)。
- **不要用 `proj_size=1`**：那等于在一个饱和的标量上再叠 Linear(1,1)，要打到大角度
  就得让 motor neuron 常驻 tanh 饱和区，梯度会死。
- 注意加宽 motor 层会改变 AutoNCP 的层划分(`command = max(int(0.4*(units-output_size)),1)`)，
  循环层参数量要重新数。
- 标签按 train split 的 mean/std 标准化，报告时逆变换回度数。这一步是为了 loss 尺度
  (MSE 下一个 100° 帧的贡献是 1° 帧的 10,000 倍)，不是为了量程。
- LSTM/GRU/LTC 套完全相同的 Linear 头，并在 README 里说明。
- 把 §9 第 5 步的门槛加硬：过拟合用的那个 batch **必须含一个 |angle| > 40° 的帧**，
  通过标准是该帧的逐帧误差 < 1°。

### B2. §3/§6.2 里有四条库的事实是错的 **[已亲自验证]**

| 计划里的写法 | 实际 | 后果 |
|---|---|---|
| `CfC(input_size=32, wiring=AutoNCP(...))` | 第二个参数名叫 `units`，类型 `Union[int, Wiring]`，**没有 `wiring` 关键字** | §3 的片段是位置传参所以能过，但 §6.2 是 `models/cfc_model.py` 的规格书，bug 会传进真代码 |
| 退路仓库 `mlech26l/CfC` | **HTTP 404**。真实地址是 `raminmh/CfC` | 单文件性质成立，但不是 drop-in：`return_sequences=False` 是默认值，forward 返回裸 tensor(于是 `m(x)[0]` 会静默返回第一个样本而不是序列)，需要一个 6 键 hparams dict，**且完全没有 wiring 支持**——退回去等于放弃整个稀疏接线的故事 |
| `pip install ncps` 装好即可 | PyPI 元数据 `requires_dist = ['future', 'packaging']`，**不含 numpy**，而 ncps 在模块加载时就 import numpy | 干净 venv 里 `import ncps` 直接 ModuleNotFoundError |
| `sum(p.numel() for p in m.parameters())` 是参数量 | `sparsity_mask` 被注册成 `nn.Parameter(..., requires_grad=False)`，**会被计入** | AutoNCP(32,1) 下该表达式给 6,898，可训练参数只有 5,544，**高估 24%**。§5.3 的诚实报告、§6.3 的 LSTM 尺寸匹配、§12 的分层表格全部按错数字校准 |

另外 **`ncps` 最后更新不是 2023-09，是 2024-08-14 发布的 1.0.1**，且在 torch 2.8 上
可正常运行。README 的措辞要从"ncps 可能与新版 torch 不兼容"(一个 grader 能当场证伪的
断言)改成"这个依赖已停止维护，所以我们锁定 ncps==1.0.1 并自行验证了前向传播"。

还有一条静默陷阱：`mixed_memory=True` 会在 CfC 前面挂一个完整的 LSTMCell 且不报错——
一旦误开，headline 的 CfC-vs-LSTM 对比就变成了 LSTM+CfC vs LSTM。

修法：把 §3 的一行式换成 `scripts/smoke_test.py`，要做到——
打印 torch/ncps/numpy 版本(README 复现章节要用)；**断言**输出 shape == (2,16,1) 而不是 print；
跑 `.backward()` 并断言每个 `requires_grad` 参数都有有限且非零的梯度；在 CUDA 上重跑一遍；
断言 `AutoNCP(3,1)` 抛 ValueError(硬约束是 `output_size < units-2`，**已验证**)；
断言模型工厂从不设 `mixed_memory=True`；同时打印 total 和 trainable 两个参数量并断言二者不等。
`requirements.txt` 显式钉死 torch / numpy / `ncps==1.0.1`。
每个模型构造全新的 AutoNCP 实例(wiring 首次 build 时会绑定输入宽度，复用会报错)。
`nn.LSTM`/`nn.GRU` **显式传 `batch_first=True`**——ncps 默认 True，torch 默认 False，
静默的 batch/time 转置会直接废掉 §6.3 的"严格控制变量"。

### B3. 增强是按帧写的，但样本是 16 帧的窗口——默认实现会静默毁掉所有循环模型

四个视角独立收敛到这一条，没有一个反驳者能撼动它。

§7 把平移、翻转、亮度纯粹描述成图像操作，完全没提时间轴；而 §6.1 要求
`(B,16,3,66,200)` 并强调"循环层必须真正在时间维度上维持状态"。
如果按 torchvision 的常规写法把 transform 放进 `Dataset.__getitem__`，
**每一帧都会独立抽一次平移量、翻转决策和亮度**——于是一个窗口内摄像头每 33 ms
横向瞬移一次，还会偶尔照镜子。翻转只对部分帧的标签取负，产生物理上不可能的序列。

这个失败是**静默的**(不报错，loss 照样下降)、**不对称的**(污染 CfC/LSTM/GRU，
对纯 CNN 基线毫发无伤)，产出的正是"时序信息没有帮助"这个假结论——然后它会被
写进 20 分的 Justification 里当作架构发现。

修法：在 §7 加 **[决策]**、在 §10 加反模式——**所有增强参数每个窗口只抽一次，
对全部 T 帧施加完全相同的变换**：同一个平移量、同一个翻转决定、同一个亮度；
翻转时对整条标签序列取负，平移时给每一帧标签加同一个偏移量。
在 `data/dataset.py` 里断言：喂一个恒定图像序列进增强器，断言输出的 T 帧完全相同、
T 个标签修正量完全相同；另外断言翻转窗口的标签序列 == 原窗口标签序列的逐元素取负。
放在 §9 第 4 步、过拟合门槛之前。README 里提一句这个单元测试——
Code/Documentation(20 分) 考的就是这类证据。

### B4. Advanced Concept 押的"物理对应关系"这个数据集给不了，而且 §6.1 没留出可平移的像素

两个独立问题落在同一个 10 分项上。

**(a)** §7 声称平移量与标签修正量之间有"明确物理对应关系"并称其为"拿分的核心"。
这个对应关系在这里推不出来：作者自述摄像头是"随手用胶带粘在挡风玻璃上"的网络摄像头，
没有公开内参外参；**没有速度通道**(仓库 issue #3，作者确认可以采但没采)；
方向盘转角到路径曲率的传动比未公开。NVIDIA 能推是因为他们有标定过的三摄像头
装置加速度信号。**"度/像素"这个常数在这里是一个伪装成物理的超参数**——
一个了解这个数据集的 grader 可以当场证伪。
(Udacity 血统里广为流传的 0.004/px 是另一个摄像头的归一化转向单位，不能迁移到度。)

**(b)** §6.1 把存储帧固定在 66×200、§5.5 又把预处理定为一次性 resize 存 memmap，
于是横向平移**一个真实像素的余量都没有**。任何实现都只能边缘复制或补零，
而补边带的宽度是标签修正量的完美线性函数——模型可以直接从边界伪影回归标签
而不是看路，增强消融就此失去意义。这个问题在第二周才咬人，届时要重解 63,000 张 JPEG；
今天改一行就行。

修法：
- §5.5 / §9 第 3 步改成**存 66×240**：从 455×256 原图裁下方 150 行
  (与参考 loader 的 `[-150:]` 裁切一致，在 Data Transformation 里注明出处)，
  resize 到 66×240，训练时随机取 200 宽的窗口并配上对应的标签修正，验证/测试取中心裁切。
  代价 2.99 GB vs 2.49 GB。标 **[决策]** 并把理由写在旁边，免得后面有人"顺手优化掉"那 0.5 GB。
- §7 的论证重写：**明确写出度/像素这个常数及其单位**，明确写出裁切行数，
  明确写出它依赖平地 / 固定前视距离 / 匀速三条假设，并明确写出这个数据集
  既无相机标定也无速度通道；然后**把 k 当成三个被搜索的超参数之一**，
  在 {0, 0.05, 0.1, 0.2, 0.4} deg/px 上扫，画验证集弯道 bin MAE 对 k 的曲线。
  **一条有极小值、从数据里经验地识别出该常数的曲线，比一个站不住的"推导"是强得多的
  10 分故事**，而且它把消融从两行表格升级成了一个结果。
- 顺便加一句：水平翻转产生的是一个左侧通行的世界，而部署域里不存在——
  一句免费的话，直观展示你想过这件事。

### B5. 按时间切分可能把高速路放进 train、山路放进 test，而计划里没有任何东西能测出这一点

地形沿时间轴**不平稳**。一篇用了前 40,000 帧的论文把它切成四个连续四分之一，
标注为 Highway&City / Highway&City / Hill / Hill&City——**录制从高速城区开始、
后段进山**；而 §4 自己说 ±100° 出现在山路段。
严格的 70/15/15 时间切分因此有两种失败：(a) 训练集里几乎没有 |angle| ≥ 40°，
headline 的急弯 bin 变成纯外推；(b) 测试集的急弯 bin 近乎为空，主指标无定义。
而 §4 的 EDA 全是全局统计，**没有任何一处把 bin 和 split 交叉**，所以两种情况都发现不了。

叠加问题：63,000 帧 @ 假定 30 fps ≈ 35 分钟，跑"6 km"路线 → 平均约 10 km/h。
这意味着要么有大量静止路段(无速度通道，测不出来)，要么**路线被重复走过**——
而一旦重复traversal，**300 帧缓冲带就完全不起作用了**：同一个弯的第一圈和第二圈
会分别落进 train 和 test，成为近乎相同的场景。

再叠加：约 161 个图像文件在 data.txt 里没有对应行(仓库 issue #2，作者确认)，
所以**任何按位置索引做图像-标签关联的管线都会静默错位**，产生的乱序时间轴会
制造一个假的"循环层没用"，而且这个错误能毫发无伤地通过 §9 第 5 步的过拟合门槛。

修法(**写 preprocess.py 之前**，在 §4 的 EDA 里加四件事并按结果分支)：

1. 一张 **(split × bin)** 表：帧数、百分比、**独立转弯事件数**(该 bin 内的极大连续段，
   间隔 < 15 帧的合并)；并把 70/15/15 的边界画在 §4 本来就要画的转角时序图上。
   门槛：每个 bin 至少 300 个测试帧，最好有 ≥10 个独立事件。
2. 门槛不过时**不要**改成块状轮转切分——在一条 6 km 单路线上，那会把 train 和 test
   的块放在相隔几分钟的同一段路上，是比 §5.2 要修的那个泄漏更严重的违规。
   正确做法是**仍然按时间切、但重新选边界**(例如 60/20/20 并挪动切点)，
   让每个 split 都含一部分山路段，并在 README 里说明。
3. **重访审计**：在唯一那次解码 pass 里给每帧算一个 32×32 灰度描述子，
   把两两自相似矩阵存成 PNG(对角线外的亮带 = 重访地理)；
   再把每个测试帧在该空间里对全部训练帧做最近邻，报告最小距离的直方图。
   若在远离切分边界处出现近重复，说明路线折返——headline 表格报两遍，
   全测试集 + 去泄漏子集。
4. **规则：data.txt 的第 i 行就是 memmap 的第 i 行。** preprocess.py 按序解析 data.txt，
   永不 glob。断言解压出的 .jpg 数与 data.txt 行数并打印差值(预期约 161)。
   随机重解码 20 行确认与对应行文件名匹配。
   **把某个 10 秒片段渲成 GIF 并叠上真实转角，确认方向盘转向与画面弯道一致**——
   这是全项目唯一能抓出"帧-标签整体错位一格"的检查，而所有 EDA 统计量都会放它过去。

### B6. Proposal 是一个独立计分的 14 分交付物，有更早的截止日，却不在 §9 的任何一步里

Rubric 给 proposal 独立的 2+2+4+2+2+2 = 14 分和独立的时间线。§9 自称是"不要跳步"的
唯一执行计划，而 proposal 不在其中。大部分内容已经存在(§4 供数据事实、§6 供模型、
§12 供伦理要点)，但**占 14 分中 2 分的分工段落在整份文档里根本不存在**；
而"说服 grader 你探索过数据(像 A1 Q1 那样)"的 2 分，取决于 proposal 是否写自真实的
EDA 数字，而不是 §4 里那五个"约"。§11 也漏了 handout 指名的最终提交物 `github.txt`。

修法：`proposal.md` 加进 §11，在 `stats.txt` 存在之后插入 §9 第 2.5 步。
**重写，不要粘贴或翻译 PROJECT.md**——1-2 页，五个散文式小标题按 rubric 的顺序、
用 rubric 自己的名词，因为格式那 2 分明确写着"paragraphs and topic sentences"，
而 PROJECT.md 是 bullet 体。`github.txt` 也加进 §11。
另外，**从要提交进 repo 的 PROJECT.md 副本里删掉 §2 的评分权重表和 §13 的四个提问**：
§11 会 commit PROJECT.md，也就把它放进了 20 分 Code/Documentation 的评审面——
把评分标准背诵给 grader 看、再书面询问"负结果还照常给分吗"，不是你想让 TA 读到的东西。
本地保留完整版，仓库里放精简版。

---

## 2. 事实核查清单

按"改不改会不会丢分"排序。§4 的每一个"约"都要换成你自己 EDA 量出来的数。

**F1 — 授权(§4/§12)。计划写反了一半，而改正后反而更有利。**
仓库带正式 `LICENSE`：**MIT，Copyright (c) 2018 Sully Chen**，宽松许可，
**没有"仅供研究"的限制**。那条禁令不是许可条款，它在 README 里一个标题就叫
`# IMPORTANT` 的段落下，原话大意是绝不应当用基于这些数据训练的计算机视觉软件去驾驶汽车、
这些数据集纯粹用于研究与统计。没有 data license、没有 DOI、没有规范引用格式
(有人问怎么引用，作者答"按你出版方的指引")。
→ 正确写法：**数据是 MIT 许可、法律上允许本用途，而作者另外在 README 里请求它
永不被用于驾驶汽车——本项目尊重这个请求。**
"指出许可允许的事与作者请求的事之间的落差"是比"断言一条许可并不包含的禁令"
强得多的伦理答案。在 proposal 的数据段和 README 的 Data Source 里各引一句原文并注明出处。

**F2 — §5.4 关于 LTC 的理由是反的。**
LTC 论文说它的 ODE **本身就是 stiff 的，而这正是它采用融合半隐式求解器的原因**
("This choice of discretization method results in achieving stability")；
定理 2 证明隐状态有界。ncps 的 `LTCCell` 用固定 6 步半隐式 Euler(`ode_unfolds=6`)，
**没有自适应步长控制器，所以 solver 发散根本不是一种可能的失败模式**。
论文自陈的弱点恰好相反："LTCs express the vanishing gradient phenomenon."
→ CfC 优先的**决定**保留，**理由**必须换掉，否则这段错误推理会落在 20 分的
Justification 里被懂这篇论文的人抓住。正确措辞：LTC 每步墙钟约 2.4×
(实测 T=16 下 fwd+bwd：CfC 4.6 ms vs LTC 11.4 ms)，且把 BPTT 图深乘以 6
(16×6 = 96 次串行 cell 求值)，更难调条件。§5.4 的"图很深"本来就是对的，只有"发散"要改。

**F3 — LTC 和 CfC 不是等参数量。** 同一 wiring 下实测(input_size=32, AutoNCP(32,1))：
CfC 6,898，LTC 10,402(约 1.51×)。LTC 每个突触分配 w/sigma/mu/erev、每个神经元分配
cm/gleak/vleak，外加输入输出仿射映射。
→ 要么给 LTC 更少的 units 来匹配，要么在 README 里直说"LTC 按 wiring/units 匹配而非
按参数量匹配"并同时报出两个数字。**静默称之为等参数量，正是那种会丢 Justification 分的事。**

**F4 — "约 30 fps" 没有任何一手来源。** 网上唯一依据是第三方 README 从假定的
25 分钟时长反推出 2017 版的 30 fps。→ **从 2018 版的时间戳里量出来。**
计划里每一个"秒"都静默依赖它：300 帧 ≈ 10 秒的缓冲带、0.53 秒的窗口、ACF lag 的解释。

**F5 — "约 6 km 路线"不是数据集作者说的**，唯一来源是一篇只用了前 40,000 帧的
二手论文。→ 归因给那篇论文、标为第三方估计，或者干脆删掉。
§12 的伦理段落把"单一 6 km 路线"当事实来用，要么弱化措辞，要么换成从时间戳量出的时长。

**F6 — "山路段转角可达 ±100°，其余多在 ±50° 内"是分布主体范围，不是极值**，
且统计自前 40,000 帧。真实极值未公开。→ 改述为"分布主体"，并且**在从 EDA 拿到真实
min/max/分位数之前，不要把急弯 bin 的边界钉死在 40°**——那个 bin 的人口完全取决于
这条未记录的尾巴。

**F7 — 图像与 data.txt 不是一一对应。** issue #2：约 161 张图无标签行。
作者确认角度本身不受影响("同步逻辑不受丢帧影响")，所以标签可信——
**但关联必须按文件名，绝不能按位置索引**。作者自己的 `run_dataset.py` 用
`str(i) + '.jpg'` 循环，就是索引式的，因此是错位的。
另外丢帧意味着 data.txt 的相邻两行在时间上不一定相邻，T=16 的窗口可能跨过一个 gap
而 Δt 非均匀 → 按时间戳缺口把索引切成 segment，禁止窗口跨 segment。

**F8 — §5.1 的"零基线"叙述里，近零那堆质量有一部分是停着的车。**
没有速度/油门/刹车通道(issue #3，作者确认)，所以你用不了这个文献里的标准过滤
(例如 FM-Net 剔除低于 15 m/s 的帧以"丢弃错误的转向读数")。
红灯和 stop sign 处的静止段全部留在数据里、全部落进直行 bin、抬高近零质量和
帧间自相关，**让 predict-0 人为地更强**。
另外，度数刻度本身是作者手工拟合的线性标定，他自称"rough approximation"
(把方向盘转到两个位置、对 CAN 总线字节拟合一条直线)，所以绝对增益和零点带有
未量化的误差；而且**这是方向盘转角、不是前轮转角**(约 15:1 传动比下，
方向盘 ±100° ≈ 前轮 ±6.7°)。
→ 两个便宜的动作：(1) 在唯一那次预处理 pass 里用相邻降采样帧的平均绝对像素差
检测疑似静止段，**分 split 报告其占比和段长分布**，叠在转角时序图上，
并在 README 里说明这些帧是丢弃、保留还是单列。"你最好的那个 bin 里有多少是一辆停着的车？"
正是 20 分那一项奖励的、扎根于 data summary 的问题，而计划目前答不上来。
(2) 在绝对度数 bin 之外并列报告**分位数 bin**，使任何结论都不依赖一个目测标定的刻度；
并明确写"steering-wheel degrees"。

**F9 — Chahine et al. 2023 的 OOD 范式和计划想做的不是一回事。**
扫描式设计确有先例(他们 Fig. 2 覆盖高斯噪声、亮度、对比度、饱和度，每个模型一条趋势线，
结构上就是同一张图)。但两点不同：
(a) **指标**——他们不对 ground truth 算误差，而是把原始序列和扰动序列分别过网络、
把输出指令积分成两条轨迹、记录终点距离，即**相对模型自身无扰动输出的自洽性**。
他们的结论"CfC 偏离标称轨迹最少"讲的是**输出稳定性**，不是精度。
(b) 他们的遮挡测试是**物理的**(真实目标旋转后用黑幕遮住一半，闭环飞行)，不是数字色块。
另外值得知道：他们的训练增强扰动了亮度/对比度/饱和度**并且**加了 SD 0.05 的高斯噪声——
和 §7 vs §8.2 是同一种纠缠。还有：**他们的 CfC 优势主要出现在分布外**
(四个环境 85/95/90/67.5% vs LSTM 82.5/92.5/62.5/27.5%)，
**也就是说干净测试集上 CfC 与 LSTM 打平是正常结果，不是失败。**
→ 引用他们的扫描设计，把数字遮挡记为你自己的扩展，并**并列报告自洽性指标**——
几乎免费，是与你所引论文的对等比较，且能把"更准"和"更稳"分开，
在预期叙事没兑现时是保险。据此设定 §8.2 的预期，让"干净数据打平"读起来是预料之中而非失败。

**F10 — 不要给 Lechner 论文的 CNN 骨干安一个参数量数字。**
Nature MI 正文付费墙，`convolution_head.py` 只存在于一个公开 git 树里没有的
87 MB/1.5 GB 复现归档中。间接证据互相矛盾：二手来源引"比确立 SOTA 的卷积网络小 63 倍"，
PilotNet 约 250k 参数(推出总数约 4k)，而 CfC 论文另外描述过一个"仅约 4k 可训练参数"的
像素到转向任务。这和"几十万参数的头"对不上。
→ §5.3 的告诫**靠已核实的措辞就站得住**：Lechner 报告的是"19 个控制**神经元**，
把 32 个封装输入特征经 253 个突触连到输出"，19 是神经元不是参数，前面另有卷积头。
**报你自己测出来的编码器参数量**。另外 `AutoNCP(19,1).build(32)` 给出的是
[11 inter, 7 command, 1 motor] 共 206 个突触，不是论文的 253——
所以要写"一个 AutoNCP 生成的 NCP 接线，N 个单元"并报你自己的划分，而不是暗示复现了他们的电路。
最后：**SullyChen / Udacity / comma.ai 上都没有已发表的 CfC/LTC/NCP 转向结果**
(已检索；已发表的 liquid network 驾驶工作全部用作者自采数据)。
把这句写出来——这是诚实的新颖性声明，它让 predict-0 和等参数 LSTM 成为正确的标尺，
并且预先挡掉与已发表 SullyChen 数字的比较(那些数字大多来自打乱切分、且以弧度报告，
所以会显得比你好得多，而原因恰恰就是 §5.2 存在的理由)。

---

## 3. 十项重要修正

**M1 — 用 uint8 memmap + stride 4 把 seed 协议"买"下来。这是唯一一个腾出预算而不是消耗预算的改动。**
四个视角收敛在 seed 缺口上：§6/§8/§9/§12 全篇没有 seed、没有重复、没有误差棒，
而 §6.3 的标题写着"严格控制变量"、§8.2 的交付物是逐模型退化曲线——**曲线之间的间距就是结果**。
n=1 时既支撑不了正面结论，**也支撑不了它自己的退路**("若无差异就照实报告"在 n=1 时同样无据)。
急弯 bin 的测试支撑最少，而整个 OOD 叙事就住在那里，run-to-run 波动很可能与架构效应同量级。
另外 **`AutoNCP` 的 wiring seed 默认 22222，取自它自己的 RandomState，与 `torch.manual_seed` 完全无关**
(**已亲自验证**)——所以就算认真扫 seed，拓扑也会被静默地固定住。
→ memmap 存 **uint8**(2.49 GB vs float32 的 9.98 GB，能进 page cache，主机到显存传输减半)，
在训练循环里到 GPU 上再 /255。**训练窗口 stride 设 4**，并用 §4 本来就要算的 lag-1 自相关来论证：
stride 1 是 44,100 个窗口 = 每 epoch 705,600 次 CNN 前向，stride 4 是 176,400 次。
省下的算力买 **CfC 和匹配 LSTM 各 3 个 seed**(多 4 次训练)，
GRU/LTC/纯 CNN 各 1 个并标注"单次运行，仅供参考"。
`torch.manual_seed` 和 `AutoNCP(..., seed=)` 一起变，并在 README 里说明接线拓扑是一个独立的方差来源。

**M2 — 重构 Justification，把 §8.2 从"20 分的核心"降级。**
§8.2 的标题字面写着"OOD 扰动实验(Justification 20 分的核心)"，
但 rubric 那一项点名的锚点只有两个：**"refer to data summary and hyperparameter choices"**，
整个 70 分方案里对鲁棒性和分布偏移只字未提。一行字就把最大的评分项指派给了最贵、最不对齐的实验。
而**没有任何地方确立"多少 MAE 才算合理"**——那才是真正的问法。
与此同时 §4 已经在算相邻帧转角差的中位数和 lag-1/5/15/30/90/300 自相关，却没说要拿来干什么——
那正是零算力成本的**不可约误差**论证的原料。
→ 标题改成"OOD 扰动实验(Justification 的加分项，非主干)"，新增 §8.4 作为主干，五个小节、OOD 放最后：
(i) **在这里什么算合理**——从相邻帧差分布和自相关推一个噪声地板；
点名 **persistence 基线**(ŷ_t = y_{t-1})——grader 真正会想到的那个朴素解——把它报进表里，
然后用一段话处理掉：模型只收到图像、不收到过去的真值角度，所以 persistence 不是本任务的解，
且在标签缺席时(部署时永远缺席)不可用。**主动报出那个打败你的基线、并精确说明它为什么不算数，
恰恰是这份 rubric 奖励的东西。** 在给出你的数字之前先说出目标区间。
(ii) 我们的数字合理吗——对照 predict-0、persistence 和噪声地板，分 bin，**点名引用**那张分 split 分 bin 的表。
(iii) 哪里失败、为什么——分 bin、分具名失败模式，每一条挂到一个 data summary 事实上。
(iv) 超参数买到了什么——点名引用调参 CSV。
(v) OOD 作为附加分析。
再加一条分流规则写死："若进度落后，OOD 整体砍掉，§8.4 不受影响。"
另外把 §4 的阈值列表从 {1,2,5,10,25} 改成 **{1,2,5,15,40,100}**，让它吐出项目真正使用的 bin 边界；
并且**从直方图自身的结构 + 测试支撑要求推导** bin 边界，而不是断言 5/15/40——
那一段同时满足 Quantitative Measures(2 分) 和 Data Summary(4 分)。

**M3 — 修好 OOD 实验，让它能产出证据。**
三个缺陷：(a) §7 训练亮度/阴影不变性、§8.2 测试亮度偏移——**同一个变换出现在两侧**，
而 §6.3 保证五个模型收到完全相同的增强，所以这个轴**根本无法区分架构**。
(b) §8.2 精确规定了图的两个轴("横轴是扰动强度，每条线是一个模型")却**从没说纵轴是什么量**，
而下一行就写了预期叙事——**未定义的纵轴紧挨着已写下的预期结果，是事后选择制造结论的教科书条件**。
(c) 四个轴全是光度/空间的，共用同一个 CNN 编码器，测的是感知而不是时序通路——而模型的差异在时序通路。
→ (1) 在 Data Transformation 里把每种扰动划进 **SEEN-IN-TRAINING vs UNSEEN** 两栏。
亮度和阴影坚决进 SEEN；任何裁切类/边缘遮挡类也部分进 SEEN(因为 §7 做了横向平移)。
把亮度 OOD 的强度推到训练抖动范围的 **2 倍以上**，并且**同时画增强训练版和无增强训练版两条曲线**
(无增强那条本来就要为 §7 的消融训练，所以免费)，图注里说明低强度段量的是学到的不变性、
高强度段量的是外推。
(2) **提前用文字定义主面板**：加性退化 MAE(σ) − MAE(0)，第二面板给含 σ=0 的绝对分 bin MAE，
让读者看见起点差距。**不要用比值面板**——它系统性地美化起点更差的那个模型。
加一张**击穿点表**：每个模型的弯道 bin MAE 首次越过该 split predict-0 基线时的扰动强度。
每模型每种扰动一个数，与"绝对还是相对"的选择无关。
(3) 加一个**时序通路扰动：随机丢帧 {0, 10%, 25%, 50%}**——和 Δt 那块是同一个 eval 循环、
和纯 CNN 对比想要的是同一种记忆压力条件，三件事塌缩成一个实现。
(4) 说明每种扰动是逐帧 i.i.d. 还是每窗口冻结，**并且两种都报**——两者之差直接测量时序滤波，
成本只有推理。
(5) 所有强度档在**验证集**上选定并冻结，然后测试集只跑一次，并在 README 里写明测试集被评估了几次。
(6) 加上 Chahine 自己的指标(见 F9)。
(7) 雨/雾用 albumentations 的 RandomRain/RandomFog，别手写；时间紧就整轴砍掉。

**M4 — 去解析时间戳。它对正确性、对 EDA、对"为什么用 CfC"的唯一诚实答案都是承重的。**
§4 把毫秒时间戳的格式连字段分隔符都记下来了，然后**再没有任何一节读过它**。
后果：(a) §5.2 的 300 帧缓冲只守 split 边界，一个跨越多小时录制中断或丢帧缺口的 16 帧窗口
是合法训练样本、标签不连续——而已知约 161 帧缺失，所以缺口确实存在；
(b) "约 30 fps"没有一手来源，而计划里每个"秒"都静默依赖它；
(c) **`timespans=None` 时 ncps 代入 ts=1.0**，于是 CfC 就是一个稀疏接线的门控 RNN 在训练，
而 README 的连续时间叙述描述的是代码从未执行过的机制——**这正是"黑箱 vs 真懂"那条轴要扣的分**。
→ 在 `eda_sullychen.py` 里：用 `%Y-%m-%d %H:%M:%S:%f` 解析
(**注意毫秒前的分隔符是冒号不是句点**，用 `.%f` 会抛异常)，断言单调性，
打印 Δt 的中位数/p1/p99 和有效 fps = 1/中位数，画 log-y 的 Δt 直方图，
列出所有 Δt > 0.5 s 的行号。按这些缺口把帧索引切成连续 segment，
**让 dataset.py 的窗口采样器拒绝跨 segment**(不只是拒绝跨 split)——
这是正确性工作，无论直方图长什么样都要做。
把所有"约 30 fps"换成实测值并据此重推缓冲宽度；
**把 §4 的 ACF lag 列表延长到 900 和 1800**，让去相关曲线真正到达地板——
缓冲带取 |r| < 0.1 的最小 lag、下限 300，README 里一句话把缓冲宽度绑到实测去相关 lag 上，
就把一个整数变成了一个被论证过的选择。
然后按测量结果分支：若 p99/p50 的 Δt 只差百分之几，**不要**跑均匀 vs 真实 Δt 的消融——那按构造就是零。
改写一句诚实的 Introduction："采样在 N fps 下实质均匀，因此 CfC 的连续时间形式在这里
是作为一个闭式门控 cell 被使用，而非作为不规则 Δt 积分。"
只有 Δt 真的不规则才传 timespans——**若要传，注意 ncps 1.0.1 的 `timespans` 在 batch size > 1 时是坏的**
(open issues #81/#82/#83，无人回应)：`ts = timespans[:, t].squeeze()` 会剥掉单例维度导致 B>1 广播错误，
而当 B 恰好等于隐层宽度时**不报错、静默给出错误结果**。修法是一行(reshape 成 [B,1] 而不是 squeeze)；
Δt 要以标称帧间隔为单位归一化(中位数处 Δt=1.0)，绝不用秒，否则会把学到的时间常数整体缩放。
无论消融怎么定，**丢帧评估都要跑**——它只要推理，且是唯一一个 CfC 的时间输入能做到而 LSTM
结构上做不到的轴。给 LSTM/GRU 同样把 Δt 作为输入特征拼进去(诚实的 GRU-D 式基线)，
**同时**报告不带 Δt 的 LSTM，把"有没有这个信息"和"能不能用这个信息"分开。

**M5 — 现在就锁定恰好三个被搜索的超参数，其余全部按写明的规则固定，并记录每一次运行。**
Hyperparameter Tuning 是 4 分，而 rubric 在 20 分的 Justification 里**第二次**点名超参数选择。
§9 第 8 步把整个优化设置压缩成"超参搜索"四个字，而三个承重量散在三节里没定：
§6.2 把 AutoNCP 的单元数写成字面占位符 `units`、§6.1 把 T=16 定死却没给理由、§7 从没给平移系数。
在一份把其他每个决定都标成 **[决策]** 的文档里，这个遗漏是刺眼而非正常的。
还有一个真陷阱：**扫 `units` 会改变循环层参数量，从而破坏 §6.3 的匹配控制**，
除非 LSTM/GRU 的隐层尺寸随着获胜值重新推导——**不要在同一个实验里既调容量又比容量**。
→ 只搜三个，只在 CfC 上搜，串行，减少 epoch 数：
learning rate ∈ {3e-4, 1e-3, 3e-3}；
**T ∈ {8, 16, 32, 64}**(同时充当变长证据，也直接测出时序上下文值多少钱)；
**平移系数 k ∈ {0, 0.05, 0.1, 0.2, 0.4} deg/px**(同时充当 Advanced Concept 的经验标定)。
`units` 固定在 32，把容量列为未测试的 future work。
其余按规则固定并**明说是固定的**而不是暗示扫过：AdamW、weight_decay 1e-4、
`clip_grad_norm_` 1.0、batch 32-64 条序列、cosine decay、30 epoch、patience 10 早停。
把获胜的 lr 转给所有基线，并在 README 里说明你**刻意**选了共享 lr 而非逐模型调参——
"choices that make sense"考的就是一个被声明的权衡。
把 §9 重排成"第 7 步搜参、第 8 步基线"，让对称成为阻力最小的路径。
**目前 §9 和 §12 都没写、但至关重要的一条**：早停和 checkpoint 选择要用
**验证集分 bin MAE**(或下面的宏 skill score)，**绝不能用全局验证 MSE**——
全局 MSE 会为每一次运行选出最收缩的那个 checkpoint，静默地把 §5.1 存在的意义全部抵消掉。
每次运行写一行 CSV(config hash、超参、seed、git commit、验证全局 MAE、验证分 bin MAE)，
把表格贴进 README——**这张表本身就是交付物**。写明选择只用了验证集、测试集只碰过一次。

**M6 — README 从第 2 天起并行推进；把 Code/Documentation(20 分) 真正考的基础设施搭起来。**
(a) §9 把"README 撰写"列为第 10 步、在 OOD 之后——但 §12 检查表的十项**全都是 README 产物**，
且**没有一项出现在 §9 的排序里**，所以 §9 里根本没有生产其中九项的槽位。
(b) **Code/Documentation 是 20 分**——和整个 Advanced Concept + Model Figure + Model Parameters 加起来一样重——
而没有任何一节、任何一条检查项碰过它。§11 的树里没有 runs/、没有 scripts/、没有 tests/、
没有 .gitignore，requirements.txt 未钉版本，没有入口点，没有复现步骤；
figures/ 存在但计划里没有任何东西往里写；§10 禁止提交数据，§11 却不提供任何防止机制。
→ 把 README 从第 10 位删掉，改成**从第 2 步开始的常驻轨道**：EDA 完成当天就 commit README.md，
按 rubric 每个计分项开一个空标题，输入一就绪就填——
Data Source/Summary/Transformation/Split 从 stats.txt 立刻可写；
Ethics 从 §12 已写好的要点改成散文，不依赖任何实验；
**Model Figure 和 Model Parameters 在 CfC 前向跑通当天就写**，两者都不依赖结果；
Training Curve 随第一次运行。
§9 再加两条规则：一个**冻结日期**("交付前一周之后不再新增架构、数据集或指标")，
以及一条 **§12 优先规则**——交付前一周任何未打勾的 §12 项，一律靠**砍实验**来完成，绝不靠砍写作。
然后按价值顺序搭：
(1) `scripts/make_tables.py` glob `runs/*/final_metrics.json` 生成 README 的 markdown 表，
`scripts/make_figures.py` 写进 figures/，**一条命令重生成 README 全部证据**——
这是对"我怎么复现表 3"的最佳答案；
(2) 每次运行写 `runs/<name>/config.json`(超参、seed、git commit、torch 和 ncps 版本) 和 metrics.csv；
(3) 钉死版本的 requirements.txt + 覆盖 data/、*.npy、*.lmdb、runs/*/checkpoints/ 的 .gitignore；
(4) `data/download.sh` 用 **gdown**(对大文件直接 wget Google Drive 会静默存下病毒扫描的 HTML 页面)、
校验 SHA-256、不匹配就拒绝继续、解压、断言 .jpg 数 == data.txt 行数；
(5) 每 epoch checkpoint + `--resume`；
(6) README 的"Reproducing our results"给出字面命令序列和大致墙钟时间。
把 §12 重写成**每个 README 计分项一行**、按 rubric 顺序、带分值、带 owner、带 due date——
目前有六项**没有任何检查行**，其中包括 20 分的 Justification。
给 §9 的十步加 owner 列，分成四条可并行的轨道：数据 / 模型 / 评测框架 / 写作
(**评测框架可以在任何真实模型存在之前，用随机张量搭好并单元测试**)，第 5 步是全员到场的联合门。
若真的退回第三方源码，放进 `third_party/`，保留原始 license header、源仓库和 commit hash，并加 README 说明。

**M7 — 写下三句评测协议，做出那张分 split 分 bin 的表——后面每一个解释都依赖它们。**
三个决定在 §5.1/§6.1/§8.1 中都没写，每个只值一行，每个都会静默改变 headline 数字：
(1) bin 按**预测值**还是**真值**划分——按预测划分是退化的且美化模型(predict-0 模型的急弯 bin 会是空的)；
(2) **评测 stride**——stride 1 时每个测试帧被计入约 16 次且误差不独立，
这会人为地收窄恰恰是比较所依赖的那条 seed 带；
(3) 指标是**逐帧**还是**逐窗口**——不同人实现不一致会让增强消融和 OOD 网格不可比。
这里还藏着一个免费结果：many-to-many 下每个帧在 16 个不同位置被预测过，
位置 1 是刚清零的状态、位置 16 有完整上下文，**平均会把冷启动预测混进分数里，
系统性地低估整个项目要测量的时序上下文优势**。
而 §4 的 EDA 完全没有分 split 统计，所以计划里没有任何东西能说出急弯测试格里有几个独立转弯事件。
→ 三句话写进 §5.1：bin 按**真值**划分；指标**逐帧**、跨所有时间步计算；
评测从**对每个测试 segment 的连续有状态 rollout** 得到(见 M8)，而非从 strided 窗口——
这样每个测试帧恰好被覆盖一次，不重复计数也不丢帧。较小的 stride 只用于训练，理由取自 lag 自相关。
然后**为每个模型报告逐帧 MAE 随窗口内位置(t=1..T)的曲线**——
一条从位置 1 降到 16 的曲线是循环状态在起作用的直接证据，是可得的最干净的 CfC-vs-LSTM-vs-CNN 判别器，
让 warm-up 可见，而且成本只是在已有预测上多做一次聚合。
把 CNN-vs-循环的差值**报两遍**：全部 16 步、以及只用第 4-15 步，并说明差异。
另外，在 stats.txt 和 README 里放一张 **(split × bin)** 表，列：n_frames、墙钟分钟数、
n_independent_turn_events、平均 |angle|、**逐 split 计算的分 bin predict-0 MAE**——
这就是"summary statistics to help interpret your results"的字面意思，
而且把 n_frames 和 n_events 作为列带进每张结果表，能阻止任何读者把 200 个相关帧误当成 200 个样本。
**若最高 bin 的独立测试事件少于约 10 个，就把 [15,40) 和 [40,∞) 合并**并在 Quantitative Measures
里论证这次合并——这是一个应当从数据出发、而不是从 §5.1 里那几个整数出发的决定。
最后，定义**一个** headline 标量：**宏 skill score = 1 − 四个 bin 上 (MAE_model / MAE_predict0) 的均值**。
predict-0 按构造恰好得 0，且它无法靠收缩取胜，这给了你一个可辩护的单一数字，
同时保留分 bin MAE 作为分析视角。报全局 **MAE**(不只是 RMSE——§8.1 目前列 RMSE，
而 §4 和 §5.1 里所有基线比较都用 MAE，三节的单位对不上)，
再加一个虚警率 P(|ŷ|>5 | |y|<5)，并直说 predict-0 在这一项上按构造满分。

**M8 — 把变长"演示"出来，而不是"声称"，顺便白拿一个记忆跨度结果。**
§2 断言变长约束已满足("many-to-many 序列回归 + CfC 循环层，满足")，
但 §6.1 毫不含糊：(B,16,...) 进、(B,16,1) 出，T 是字面常量，没有任何 padding。
循环层按构造是长度无关的，所以这是**演示缺口**而非合规危机
(而且这条约束是你自己的转述、不是 handout 原文，**不要为它重构整个项目**)。
但它蕴含的实验是免费的、零重训、且回答一个决定整个项目有没有在测量东西的问题：
**如果分 bin MAE 对 T 是平的，循环层就没有东西可以积分**，
§6.3 的所有架构比较都塌缩成逐帧回归器——你需要在第 2 周知道这件事，不是第 4 周。
→ 训练时按 batch 采样 T(batch 内同构，T ~ Uniform{8,16,32,64})，让训练输入真正变长；
评测时对连续测试 segment 做有状态 rollout，T ∈ {8,16,32,64,128,256,full}。
**不要**把一个 9,450 帧的 segment 一次前向——仅输入张量就约 1.5 GB 激活；
切成约 256 帧的块并在块间传递 hx(`CfC.forward` 返回 (output, hx) 且接受 hx)，两者严格等价。
注意返回的 hx 是**全部 wiring.units 个神经元**的状态、不只是 motor 输出，
所以要写 `y, _ = model(x)`，且**不要在模型图里把 hx 标成输出**。
纯 CNN 按构造长度无关，给出一条平的参考线。
把 §2 改写成指向这张表，并在 README 的 Introduction 里写明输入输出是变长的。
**把纯 CNN 那一臂从 §9 第 7 步挪到第 6 步**、紧跟第一条 CfC 训练曲线之后：
如果纯 CNN 落在 CfC 的噪声范围内，那么任何架构都没有时序余量可挖——
这正是计划目前缺少的那个 go/no-go。

**M9 — 让模型图、参数表和定性样例变得具体而非通用。**
三个 README 项共 12 分，§9 没有任何一步生产它们，于是会默认落到通用版本。
(a) Model Figure(4) 考的就是黑箱 vs 真懂；§6.2 的抽象层级("隐状态服从由神经网络参数化的 ODE")
正是真去画的时候会塌成一个圆角矩形的那种。
(b) Model Parameters(4) 要的是"数参数**并说明它们从哪来**"；§5.3 的 CNN/循环两行拆分说明不了任何来源，
而 §6.3 的整个比较建立在"隐层调小到与 NCP 参数量相当"之上，**却没有任何地方发表这个匹配**，
于是 headline 结果的承重假设无法被验证。
(c) Model Examples(4) 对 many-to-many 模型是未定义的——阻力最小的路径会产出一个
"成功样例 = 一条直路上模型预测约 0"，除了 predict-0 基线之外什么也没展示。
→ **图，四个面板**，在第 4-7 步之间单独占 §9 一步(它只依赖规格):
(1) CNN 编码器，每条箭头上标张量形状，**从真实 forward hook 打印出来**，通道数钉死后
(24/36/48/64/64，FC 1152→32)；
(2) **你实例化的那张 AutoNCP 图**，用 `wiring.draw_graph()`，标注 inter/command/motor 数量和突触数——
AutoNCP 的接线是生成出来的，所以渲染**你自己那张**就是通用图和具体图的区别，
也是唯一能把它和一堆 LSTM 框图区分开的东西；
(3) 一个 CfC 步，把闭式更新写成带标注的公式、每一项命名，**对照 Hasani et al. 2022 核对**而不是凭记忆写，
并把 elapsed-time 输入标成实测 Δt——**诚实地画出那个门，会让"区分性机制在固定采样率下几乎没被激活"
这件事变得可见**，从而把图变成 Justification 那句诚实告诫的直接支撑；
(4) 一条三步展开的带状图，画出状态箭头和 many-to-many 结构。
§10 加一条 ❌ 画一个写着 CfC 的方框。
**参数表**：一张推导表，每个模块一行，**把算术写出来**("5×5×3×24 + 24 = 1,824")，
手推列和 torch 列的总和必须一致，并给**三列参数量：可训练 / 冻结的稀疏 mask / 总计**——
mask 数本身就是"NCP 稀疏性是什么"的一个漂亮说明。
再来一张小表给对照组：每个变体的循环层参数和总参数并排，加一句把 §6.3 的控制明说成
"循环层参数匹配在 ±10% 以内，因为 CNN 编码器在所有变体间完全相同"，
再加一句诚实的话：**参数量匹配是必要而非充分的**——匹配之后，状态维度(32 稀疏 vs 约 24 稠密)、
连接稀疏性、时间常数参数化仍然混淆着，所以任何差距只能归因于"这个族在这个规模下"，
而不是连续时间本身。
数字已经替你算好：**LSTM H=24 给 5,568，GRU H=29 给 5,481，对上 CfC 的 5,544 可训练参数。**
在匹配版旁边**再加一个不受约束的 LSTM(H=64)**，这样比较就不会被读成砍小的稻草人。
**样例**：把单位定义为一个 16 帧窗口，每个样例两个面板——4 帧采样图叠上预测/真值角度，
外加一张预测 vs 真值随全部 T 步的折线，标注该窗口的 MAE 和同一窗口的 predict-0 MAE。
**成功样例从中弯或急弯里挑**，这样它展示的是 predict-0 做不到的事；
失败样例从一个具名的可诊断模式里挑，并标注预测在**何时**开始发散——
早期发散(感知失败) vs 晚期发散(状态/记忆失败)，这是关于循环层有没有贡献的直接 Justification 证据。
再加一张 300-600 帧的有状态 rollout 图，把两个窗口标在上面；rollout 一旦存在它几乎免费，
而且是 Results 段可得的最强图片。

**M10 — 补上伦理段的三个具体缺口，并让结果表可读。**
Ethics 4 分，考的是"不同群体是否被不同地影响"——§12 已经覆盖了五六个不同群体，
所以这一节不弱，它有三个具体的洞。
(a) 六条全是危害；rubric 问的是"**help or hurt**"，一面倒的清单最容易被读成缺乏反思。
(b) rubric 点名三样东西——**一种引发伦理问题的用途、模型的局限、数据的局限**——
而 §12 没有对应这三者的结构，且缺了好几条模型层面的局限。
(c) 这一节没有约束仓库里任何可见的东西；约束确实存在，但归档在 §2、§4、§5.1、§8.2、§10 里，
那不是伦理评分者会去读的地方。
→ **用字面对应 rubric 名词的三个小标题**，把现有条目归档进去
(第 2 条的标签监督括注和第 3 条的模仿学习本来就是模型局限，挪过去即可)。
补上真正缺失的三条模型事实：行为克隆从不观察自己犯错后的恢复，所以开环测试 MAE
对闭环行为说明有限；它是点回归、**没有不确定性估计、无法发出"我不知道"的信号**；
T 帧 @ N fps 不到一秒的上下文，**意图和路权无法被表示**。
加两三句带张力的好处、而不是吹捧：一个小到能枚举其状态变量的循环控制器，
在事故后比一个上亿参数的骨干更可审计——**而同一套可审计性论证也常被用来营销并不安全的系统，
所以小模型不等于安全模型**；按 §5.3 诚实地把范围限定在循环控制器上，CNN 前端仍然是不透明的。
**把旁观者作为一个独立群体加进来**(两三句：公共道路影像里的人从未同意、也无法退出；
带时间戳的固定短路线暴露了一个可识别个体的日常规律；我们不再分发任何帧)，
并且**只发布必需的那两张样例帧、按模型输入分辨率发布、模糊任何可读的人脸和车牌**——
不需要工具，不需要 §11 条目。§10 的"不提交数据"扩展成"包括 figures/ 里的帧"。
再加一个"**我们为此做了什么**"清单，把已在别处做出的决定收拢过来：仅离线预测；不提交数据；
每张表都列 predict-0；负结果照实报告、曲线不修饰——再加两条新的、免费的：
**不发布训练权重**(handout 明确允许)并说明理由；
**把作者"不要用于真车"的请求放在 README 顶部**，让这条限制被向下游传递，
而不是被你自己的合规视为已履行。
有一句话值得**同时**放进 Ethics 和 Justification：
"§8.2 的扰动是光度的而非语义的——它们测量的是传感器退化，不是路上有谁发生了变化，我们不主张后者。"
**表格**：一张 headline 表——行 = predict-0、persistence，然后是模型，用一条分割线隔开基线；
列 = 四个 bin 加总体 Pearson r，**每个列头带上该 bin 的测试帧数**。
每个格子给出度数 MAE **加上同 bin 相对 predict-0 的比值**("3.9° (0.62×)")，
这样读者不用自己做除法就能看到 rubric 那条"不得输给朴素基线"的答案。每列加粗最优。
细表里加**分 bin 的 p95 绝对误差**(MAE 是对称的，区分不出一个 bin 的最坏帧和中位帧)。
把 LTC、GRU、RMSE、seed 方差、增强消融降级到附录或折叠块。
退化曲线排成 2×2 面板网格，共享 y 轴，一个共享图例，predict-0 画成虚线参考线。

---

## 4. 按分值排的失分点

| 分值 | 项目 | 目前的缺口 |
|---:|---|---|
| **20** | Code/Documentation | 整份计划一个字没提。无 runs/、scripts/、tests/、.gitignore，requirements 未钉版本，无入口点，无复现序列 → M6 |
| **20** | Justification of Results | 被 §8.2 指派给一个 rubric 从未要求的鲁棒性实验；**没有任何地方确立"多少 MAE 算合理"**；§12 检查表里没有这一项的行 → M2 |
| **8** | Quantitative & Qualitative Results | 在第一个数字出现之前，呈现容量就已超载(5 变体 × 3 基线 × 4 bin × 3 指标 × 消融 × 4 扰动族 × 多档强度)，而"clear presentation"是明写的评分点；且没有定义聚合标量 → M7、M9 |
| **4+4+4** | Model Figure / Parameters / Examples | §9 没有任何一步生产它们；将要打印的参数量高估 24%；"一个样例"对 many-to-many 未定义 → M9 |
| **4** | Data Summary | EDA 全局且全在标签侧。无分 split 统计，无图像侧 EDA(§6.1 断言"裁掉上部天空"却没有任何关于地平线和引擎盖位置的证据)，63k 张 JPEG 一次性解码却无完整性 pass；阈值列表也从不吐出 >40° 的占比 → B5、M2 |
| **4** | Hyperparameter Tuning | 整个优化设置就是"超参搜索"四个字，三个承重量(units / T / k)是字面的洞 → M5 |
| **4** | Training Curve | 从没被审视过。纵轴指标未指定，而最自然的选择(MSE 训练损失)和结果表里的任何东西都不连通 → 画验证分 bin MAE，且**画的量必须就是早停所选的量**；预先用一句话说明启用 §7 增强后训练损失会长期高于验证损失，否则在 grader 眼里像坏了；五个模型画在共享坐标轴上 |
| **2** | Quantitative Measures | bin 边界 5/15/40 是断言的，没有推导；没有 MAE vs RMSE 的论证；三个协议选择(按真值还是预测分 bin、逐帧还是逐窗口、什么 stride)全都没写 → M7；**并且在表格之前、而不是等 TA 问了之后，主动说明 predict-0 在直行 bin 按构造不可战胜，这是预期而非缺陷** |
| **2** | Data Split | 300 帧缓冲被定成 [决策]**在**本该决定它的测量**之前**；ACF 的 lag 列表也从没到达去相关地板；且 §5.2 把 shuffle bug 引到了错误的仓库(应为 `SullyChen/Autopilot-TensorFlow`，给出文件路径、行号和 commit SHA，并引用那三行) |
| **2 (+2)** | Authors | 全文没有任何名字，§9 十步无 owner，proposal 计分的分工段落根本不存在 → 四条工作流(数据/模型/评测框架/写作)各带一名 owner 和一名 backup，写一次、在 proposal 和 README 的 Authors 里复用；**Justification(20) 需要一个具名 owner，没有人可以是"那个写字的"** |

---

## 5. 砍掉什么(按此顺序)

如果把所有建议全部采纳，计划将隐含：5 模型 × 3 seed + 一个单独的系数扫描 + 3 项超参搜索
+ 125 格 OOD 网格 + 不规则 Δt 评测 + 长度泛化表 + 逐位置图。远超一个月。
以下每一条都不让你丢任何 rubric 分。

1. **GRU。** §6.3 的控制是循环层参数匹配，匹配的 LSTM 已经是门控基线，GRU 只是在同一根轴上
   多加一个点。整个砍掉，并在 README 里说明你选择了在**一个**匹配比较上做深度分析，
   而不是在门控变体之间铺广度。**光这一项就资助了四次额外 seed 运行中的两次。**
2. **手写的雨/雾 OOD 轴。** 全计划里每 rubric 分实现成本最高、没有机制故事，
   且最可能变成一个多日的图像处理兔子洞。若要保留任何天气轴，用 albumentations 并引用它，别自己写。
   砍掉后 OOD 网格从 4 个族降到 2 个(噪声 + 遮挡)加上新的丢帧轴——而**丢帧才是真正能区分架构的那个**。
3. **LTC。** §5.4 已经把它限定为一次运行，但事实核查显示它的理由是反的、
   而且它和 CfC 不是参数匹配的(10,402 vs 6,898)，所以照现在写会产出一段错误的 README 和一次不公平的比较。
   **只在第 4-8 步提前完成时保留**；保留就明说是按 wiring 而非按参数匹配并报出两个数字。进度一落后，第一个丢它。
4. **2017 版数据集，除了当 smoke-test fixture 以外的任何用途。** **绝不要并进训练**——
   如果 2017 和 2018 覆盖同一区域，合并就重新引入了 §5.2 要防的近重复泄漏，
   而且是**跨数据集版本**的、切分逻辑看不见的(切分只在一个文件列表内工作)。
   它没有时间戳，支撑不了任何 Δt 工作，还要单独的解析器。
   如果最后有余裕，对它做一次**只推理**的 pass 是可得的最强非合成泛化数字——
   但要在跑完描述子相似度检查之后、标注为"跨 session、同区域"，写明假定的固定采样率，
   并同时报告 2018 测试集在假定均匀 Δt 下的数字，让读者能把 session 偏移和 Δt 失配分开。
   §10 加一行：绝不合并 2017 和 2018。
5. **从提交的 repo 里：§2 的评分权重表和 §13 的四个提问。**(见 B6)
   PROJECT.md 本身保留——一份清晰的工程文档对 Code/Documentation 那一项是加分的。
6. **从 proposal 里，砍掉一切不计分的东西**：§3 的环境和 bash 块、§5.4 的求解器推理
   (选择保留为一个从句)、§5.5 整节、§10、§11 的目录树(它属于 README)、§13，
   尤其是 §2 的评分表。§6.3 的五行表压成一句话，§8.2 的扰动族压成一句话，§14 压成两个行内引用。
   §5.3 砍掉 Lechner 框架的讨论，但**把分层报告的要求带走**——
   proposal 的 Model 段和 README 的 4 分 Model Parameters 项都需要它。
   只保留并扩写 §4 的真实数据事实、零基线和时序泄漏两项发现各一个从句
   (**这两个从句就是那 2 分探索分**)、架构、伦理、分工。

**尽管有评审建议，也不要加**：Transformer 变体；单独的三值增强系数消融
(把系数折进超参搜索即可)；多损失 × 多架构的权衡前沿；把 L1 或 Huber 当作损失替换
(**L1 瞄准条件中位数，在零峰分布上会收缩得更狠**——它会加深你正试图防止的那个弯道 bin 失败)；
把块状轮转或地理留出当作默认切分；一份 PREREGISTRATION.md；一个 blur_figure.py 工具；
一次干净机器下载彩排；n=3 时的任何 t 检验或 p 值。

---

## 6. Proposal 的段落骨架

1-2 页，12pt，**五个散文式小标题，用 rubric 自己的名词、按 rubric 的顺序**，
写成带主题句的段落而非 bullet(格式那 2 分明确写着"paragraphs and topic sentences")。
最多一张表，无代码块，无超过四项的 bullet 列表。**在 stats.txt 存在之后再写**；
下面每一个数字都必须来自你自己的 EDA，不能来自 §4 的"约"。

**第 1 段 — Task(2 分)。** 开头第一个从句就回答这一项：
"We train a many-to-many sequence regressor that maps a variable-length sequence of
forward-facing RGB frames to a per-frame steering-wheel angle in degrees."
然后字面给出输入输出形状 (B,T,3,66,200) → (B,T,1)，说明 T 在训练和测试时都变化，
说明预测是开环离线的(不做闭环控制)，并指出目标是**与该帧同一时刻**的角度，
即估计驾驶员此刻在做什么，而不是接下来该做什么。全程标准术语；
2/2 的门槛是"grader 能准确想象这个模型会被怎么用"。

**第 2 段 — Model(2 分)。**
"一个跨时间步共享的 PilotNet 式 CNN 编码器为每帧产生 32 维特征；
一个带稀疏 NCP 接线的闭式连续时间循环层(CfC)跨序列维持状态，
经一个线性头每步输出一个标量。"
三个真正需要解释的术语各用标准词汇解释一次，然后再也不用品牌名：
NCP 接线是循环单元之上一个固定的稀疏连接掩码，组织成 interneuron / command / motor 三层，
因此大部分循环权重在结构上为零；motor neurons 是输出单元；
CfC 使用一个由神经网络参数化的 ODE 的闭式解，从而避免每步做数值 ODE 求解。
**删掉"Liquid Neural Network"**——那是品牌，买不到任何东西。
用一个从句点出对照组(一个循环层等参数的 LSTM 和一个无循环的单帧 CNN)，
并给出你将分开报告的两个参数数字：编码器约 168k，循环层约 5.5k 可训练。
写"recurrent-layer parameters matched"，不写"等参数量"。

**第 3 段 — Dataset(4 分，最长的一段**，因为它那 2 分探索子项是唯一 grader 无法从数据集页面自己核实的部分**)。**
第 1 句，获取(1 分)：来源 URL、commit SHA、访问日期、压缩包 SHA-256、
存在 MIT LICENSE、一句作者所述限制的简短原文引用并注明出处，以及那个诚实的框架——
**许可允许本用途，而作者另外请求它永不驾驶汽车，我们通过只做离线预测、不提交任何数据来尊重它**。
第 2 句，充分性(1 分)：从 data.txt 行数量出的帧数、压缩包大小、
**从时间戳量出的时长和有效 fps**、抽样验证过的图像尺寸一致性。
然后是探索数字(2 分)，也就是只有真跑过 EDA 的人才说得出的那些：
|angle| < 1/2/5/15/40° 各自的占比；|angle| 的 p1/p50/p99 和**真实**最小最大值；
全局 predict-0 和 persistence 的 MAE；分 bin 的 predict-0 MAE；
lag-1 和 lag-300 的转角自相关；相邻帧转角差的中位数；
以及——**挣到那分的那一句**——你所选 70/15/15 时间切分下**分 split 分 bin 的帧数和独立转弯事件数**，
边界画在转角时序图上。
结尾用两个从句各说一个驱动了设计的发现：绝大多数帧接近零所以恒零是强基线(故用分 bin MAE)；
广为流传的 loader 先打乱再切分所以相邻近重复帧同时落进 train 和 val
(故用带实测去相关缓冲的时间切分)。**这两个从句就是那 2 分探索分。**

**第 4 段 — Ethical implications(2 分；门槛是"many groups ... impacted differently"，
所以群体的广度胜过对任一群体的深度)。**
用同样多的句子点名至少四个受影响不同的群体：车内驾驶员，拥有接管权；
行人和骑行者，承担风险、**不出现在任何训练信号中**(唯一的监督是一个标量转角)、且无法选择退出；
公共道路上被拍摄而未同意的旁观者，其带时间戳的固定路线暴露了一个可识别个体的日常规律；
以及那位唯一的示范者，其习惯(包括不安全的那些)被克隆。
加一句带张力的好处：一个小到能枚举其状态变量的循环控制器在事故后比一个极大的骨干更可审计，
而同一套论证也常被用来营销并不安全的系统。
加一句预先堵住对你自己结果的误读：受控合成扰动下的鲁棒性不是安全部署的证据，
而且我们的扰动是光度的而非语义的——它们建模的是传感器退化，不是路上有谁变了。

**第 5 段 — Division of work(2 分)。**
一张四行表，每行一条工作流，各带一名具名 owner 和一名具名 backup：
**数据**(下载、EDA、预处理、memmap、切分审计、README 的 Data 各节)；
**模型**(编码器、CfC、基线、运行追踪、README 的 Model 各节)；
**评测框架**(增强模块及其单元测试、evaluate.py、ood.py、make_tables.py、make_figures.py)；
**写作**(README 骨架、Ethics、Introduction、Justification、代码文档、干净克隆的复现检查)。
写明实现顺序的第 4 步和第 5 步是结对编程、由谁和谁；
写明评测框架轨道在任何真实模型存在之前就用随机张量搭好并单元测试，因此不被模型轨道阻塞。
2/2 的门槛是"如果换个人接手，他知道自己的职责究竟是什么"——
所以**每一行都必须给出一个交付物和一个完成判据，而不是一个话题**。

---

## 7. 三件所有视角都漏了的事

**A1 — §9 第 5 步对序列模型来说不是一个门。加上时间打乱对照。**
在单个 batch 上过拟合只证明优化器能工作，**它完全不能证明循环层被用到了**——
模型完全可以靠记住逐帧外观、同时让隐状态彻底闲置来把 loss 压到零。
加一个只要五行、在第 5 步就跑的对照：拿一个训练好(或过拟合好)的模型，
**在每个评测窗口内打乱帧的顺序**，重新测量。
如果 loss 没有明显变差，循环状态就是装饰品，CfC-vs-LSTM 的比较什么也没在测量——
而你在第 2 周就知道了，不是第 4 周。
增强接上之后再跑一次——它也是 B3 那个逐帧增强 bug 的最便宜的探测器，
因为逐帧抖动会摧毁时序连贯性，届时打乱测试同样将不再显示退化。
在 README 里报告每个模型的"打乱 vs 有序"差值；
它是对"时序信息在这里重要吗？"的一个直接的、单数字的回答，
应该和噪声地板一起放在 Justification 的开篇小节。

**A2 — 验证集和测试集各自是一段约 5 分钟的连续录像，这可能让早停几乎失去意义。**
所有人都指出了时间切分可能把不同地形放进不同 split。**没有人推出它对模型选择的后果**：
如果 val 是 70-85% 那一段、test 是 85-100% 那一段，而地形沿录像变化，
**那么你是在一种地形上选 checkpoint 和超参、在另一种地形上报告结果**。
4 分的 Hyperparameter 那一项里每一句"我们在验证集上调参"，就都建立在一个可能不迁移的选择信号上。
两个便宜的回应：(a) 攒够 10-15 次运行后，把验证分 bin MAE 对测试分 bin MAE 做散点并报告相关性——
相关性弱就说明选择是噪声，而**展示你检查过**是几乎没有队伍会有的免费 Justification 内容；
(b) 在切分表旁边报告每个 split 的地形构成(用疑似静止掩码和转角分布作代理)，
让读者能看出 val 和 test 是不是可比的总体。如果不是，就直说，
并把 val 选出的超参当作一条写明的局限，而不是假装选择是干净的。

**A3 — 做图之前先给图做预算。**
采纳上述建议会产生**十五张以上**候选图(四面板模型图、5 条训练曲线、分 bin 表、
逐位置 MAE、2×2 退化网格、2 个定性样例、增强消融、长度泛化、自相似矩阵、
最近邻直方图、Δt 直方图、转角直方图、带切分边界的转角时序、带裁切框的均值图像、
高 |angle| 帧拼图、打乱对照柱状图)。
**一份有十五张图的 README 会被略读而不是被读**，而 Quantitative & Qualitative Results
明写着按呈现清晰度评分。现在就决定**哪七张**承担论证，其余放进 figures/ 各用一行引用。
建议排序：(i) 四面板模型图；(ii) 带 ratio-to-predict-0 的 headline 分 bin 表；
(iii) 共享坐标轴的训练曲线；(iv) **跨模型的逐帧 MAE vs 窗口内位置**——
全项目信息量最大、也最便宜的一张图；(v) 两个定性样例合成一张轨迹图；
(vi) 2×2 退化网格；(vii) 带切分边界和分 split bin 计数的转角时序图，一张顶三张。
把图的生产分配给写作轨道，并在 §9 里给它单独一步——**最后一周实际上就是花在图上的**。

---

## 8. 保护清单：不要在任何一次"简化"里丢掉这四样

1. **§5.2 的时间切分 + 缓冲带。** 这是计划最强的资产，也是这个数据集大多数复现搞错的那一件事——
   作者自己的 loader 先打乱再切分，无 seed，且根本没有测试集。
   **不要因为"大家都这么写"而改回随机切分**，也不要为了方便改成块状轮转：
   在一条短路线上那会把 train 和 test 的块放在相隔几分钟的同一段路上，比你修掉的那个泄漏更糟。
2. **§5.1 的分 bin 指标 + 每张表都列 predict-0。** 这是对零主导目标的正确回应，而且是独立想到的。
   保留它，并保留 §10 里那条禁止无基线列表格的反模式。
3. **§5.3 拒绝写"我们的模型只有 19 个参数"。** 论文自己的措辞——19 个控制**神经元**、
   32 个封装输入特征、253 个突触、另有一个卷积头——精确地印证了这条谨慎。
   逐字保留这一条，也保留 §10 里的对应版本。
4. **§10 本身，以及 §8.2 预先承诺照实报告负结果。** 两者都是这份 rubric 奖励的姿态。
   Chahine 的数字让这一点变得具体：**他们的 CfC 优势主要出现在分布外，在分布内很小甚至没有**——
   所以干净数据上 CfC 与 LSTM 打平是**预期结果**，不是失败。围绕这个来规划写作，而不是围绕那个期望中的差距。

---

## 附：方法说明

本报告由 15 个 agent 产生：6 个独立审计视角(rubric 逐条对齐 / 工程可行性 / 数据与 EDA /
评测方法论 / 排期与风险 / 伦理与写作)，每个视角配一个对抗性反驳 agent(职责是**推翻**该视角的发现，
默认怀疑，只有推不倒的才标记为 confirmed)，外加 2 个联网事实核查 agent，最后一个汇总 agent
合并去重、丢弃被推翻的、降级被夸大的。

B1、B2 和 §2 中所有关于 ncps 的条目，我事后从 ncps 1.0.1 wheel 的源码、PyPI 元数据
和 GitHub HTTP 状态码亲自复核过。F1、F4-F10 来自联网核查的一手来源，
其中标为"unverifiable"的(F10 关于论文骨干参数量)已明确说明不要引用数字。
