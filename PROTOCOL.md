# 实验协议（算力充裕版）

> 前置：[REVIEW.md](REVIEW.md)。本文档跑在那些 blocker 修复**之上**，不是替代。
> 本文档假设 RTX 5090（32 GB, Blackwell sm_120），可租用云 GPU。
>
> **合计：294 次训练，约 25 GPU-小时（含 30% 余量约 32），6 路并发下约 7-8 小时墙钟
> —— 一个无人值守的夜晚。**

---

## 0. 算力改变了什么、没改变什么

**没改变的：交付物。** 294 次运行落进 `runs/`，README 里出现的是 **8 张图 + 3 张表**，
一条命令全部重生成。rubric 对广度的回报有上限，而 Quantitative & Qualitative Results(8 分)
明写按呈现清晰度评分 —— **50 次运行没有故事，分数低于 12 次运行有故事**。

**改变的：证据的性质。** 免费 GPU 买到的最有价值的东西不是新的对照臂，而是
**CfC-vs-LSTM 的比较从"断言"变成"可核查"**：10 个 seed、实测噪声地板、两个方差来源、
120 配置的选择有效性散点、四条互不替代的时序论证路径。

**结论：算力问题从来不是这个项目的瓶颈，25 GPU-小时一晚上跑完，所以不要把它当算力问题讨论。**
瓶颈是人力小时和 README 的呈现容量，二者都没变。

**为 null 结果做规划。** Chahine et al. 自己的分布内结果就是平手，所以
**CfC 和 LSTM 的误差带重叠是预期结果，不是失败**。主图（Figure 1）是特意挑的：
即使两条线重合，它仍然携带"有循环 vs 无循环"的分离、predict-0 地板和冷启动披露。
而 2×2 的 OOD 网格全部内容**就是**那条间距，一旦 null 就整张图作废 —— 这就是它排在第 3 位而不是第 1 位的原因。

---

## 1. 管线设计

### 1.1 Day-0 硬件门（在下载 3.1 GB 之前）

先 `nvidia-smi` 读驱动版本再分支：
- **驱动 ≥ 580.65.06** → `pip install "torch>=2.8"`，PyPI 默认 Linux wheel 自 2.8.0 起已带 sm_120 kernel，
  网上广为流传的 `--index-url .../cu128` 是过时建议。
- **驱动 570.x–579.x** → 升级驱动，或钉 `torch==2.10.0 --index-url https://download.pytorch.org/whl/cu128`。

**危险点：驱动过旧时 torch 装得上、`torch.cuda.is_available()` 返回 True、显卡名字也报得对，
然后每一个 kernel 都失败。** 所以 `scripts/smoke_test.py` 必须 **assert 而不是 print**：

- `"sm_120" in torch.cuda.get_arch_list()`
- 一次**真实的 on-device** `conv2d` + `.backward()`，断言每个可训练参数的梯度有限且非零
  ——**这是唯一能抓出"检测到但不可用的 GPU"的检查**
- 输出 shape == `(2,16,1)`；`AutoNCP(3,1)` 抛 ValueError；模型工厂从不设 `mixed_memory=True`
- wiring 参数是 `Wiring` 实例而**永不是裸 int**（裸 int 会静默挂上 128 单元的 backbone MLP，参数量整个变掉）
- `total != trainable`（差值就是冻结的稀疏 mask）
- 版本一律用 `importlib.metadata.version("ncps")`，**绝不用 `ncps.__version__`**
  —— 1.0.1 的 wheel 里 `ncps/__init__.py:21` 写的是 `"0.0.2"`，用它会让 294 个 `config.json`
  全都引用一个 PyPI 上不存在的版本。

`requirements.txt` 显式钉 `torch`、`numpy`、`ncps==1.0.1`（ncps 不声明依赖却在模块加载时 import numpy）。

### 1.2 存储 66×240，模型输入 66×200，用断言强制

裁原图 455×256 的下方 150 行（与参考 loader 的 `[-150:]` 一致，在 Data Transformation 里注明出处），
resize 到 66×240，存 uint8 memmap（2.99 GB）。训练随机取 200 宽 + 对应标签修正，val/test 取中心裁切。

**在模型 `forward` 里放 `assert W == 200`**：宽度 240 时 flatten 会从 1,152 变成 1,472，
编码器参数从 168,244 悄悄涨到 178,484，而**参数表的手推列与 torch 列相符是证明这张表是真的唯一依据**。

### 1.3 数据不常驻显存（尽管有 32 GB）

2.79 GiB 的 uint8 数据集能塞下九份，但常驻仍然是错的选择：

| 方案 | 每进程 VRAM | 可并发 |
|---|---:|---:|
| 常驻显存 | ~5.15 GiB | 5 |
| 从 pinned host RAM 流式 | ~2.37 GiB | **12** |

流式每步搬 48.7 MB，25 步/秒约 1.2 GB/s，不到 PCIe 5.0 实用带宽的 5%。
**这个 workload 是 dispatch-bound 而不是 FLOP-bound**（wired CfC 每训练步发出约 4,185 次 ATen dispatch，
与 batch 无关；而算术量是它 6,337 倍的编码器只发约 60 次），
所以**并发是唯一真实的提速杠杆**，常驻会为了优化一个从来不是瓶颈的传输而牺牲一半以上吞吐。

顺带修正 README 里 uint8 的理由：**省下的从来是每 epoch 的 JPEG 解码（CPU 成本），不是显存** —— float32 也塞得下。

### 1.4 不用 DataLoader，不用 worker

一步 = 在 pinned uint8 张量上 `gather` → `.to(device, non_blocking=True)` → `.float().div_(255)`
→ GPU 上批量增强（约 50 行）。省掉 294 次短运行各自的 worker 启动只是附带好处，
**决定性理由是正确性**：

> 增强参数以 **`(B,1,1,1,1)`** 的形状对 **`(B,T,C,H,W)`** 的 batch 采样，
> 于是 REVIEW B3 要求的"每窗口一次变换、T 帧一致"**成为广播的默认行为**，
> 而那个有 bug 的逐帧版本反倒需要额外费力才写得出来。

**标成 [决策] 并引用 B3，不要标成性能优化。**
`tests/test_augment.py`：推一个恒定图像序列进去，断言 T 个输出和 T 个标签修正量全部相同；
断言翻转窗口的标签序列 == 原窗口的逐元素取负。

### 1.5 窗口采样：均匀随机起点，不是固定 stride-4 网格

拒绝任何跨越 split 边界、缓冲带、或时间戳缺口 segment 边界的窗口。
每 epoch 窗口数**钉死在 11,025**（即 stride-4 网格给出的数量，理由取自实测 lag-1 自相关），
所以优化步预算不变：172 步/epoch、30 epoch 共 5,160 步。

**固定 stride 会让帧 *i* 只能出现在由 *i* mod 4 决定的那几个窗口内位置上，
从而污染逐位置 MAE 曲线 —— 而那是本项目的主图。** 这是罕见的、免费的、保护自己最好那张图的修复。

### 1.6 batch 固定 64，并且禁止当提速杠杆

显存能吃下 B·T ≈ 22,000 帧（训练集 44,100 帧的一半塞进一个 batch），所以 batch 根本不是考量。
**我明确推翻"为了利用率把 batch 提到 128-256"的建议**：循环部分每步成本与 batch 无关，
所以更大的 batch 在墙钟上几乎免费 —— 但它**改变优化步数**，让在 64 上跑的 lr 网格作废，
并给全部 294 次比较注入一个混淆。可比性胜过 4× 墙钟。

README 措辞：*"batch size 是按优化质量而非硬件利用率选的；我们刻意没有把可用显存花在它上面，
因为优化步数是本报告中每一个比较的混淆变量。"*

### 1.7 精度：纯 fp32，AMP 关，TF32 显式关掉

设 `torch.backends.cudnn.conv.fp32_precision = "ieee"`（等价于 `allow_tf32 = False`）。
**PyTorch 在 Blackwell 上的默认是不对称的** —— cuDNN 卷积开 TF32、cuBLAS matmul 不开 —— 
于是 PilotNet 那一摞会跑在 10 位尾数上，而 CfC 的 linear 跑真 fp32，
**一个不对称的精度差异正好坐在 headline 比较的中间**。关掉它每步多几毫秒，
换来"我们验证了这个差距不是精度伪影"这句话 —— 大多数队伍写不出来，因为他们不知道自己的 fp32 卷积从来不是 fp32。

**不用 AMP**：`tanh` 和 `sigmoid` 不在任何 autocast 列表里，所以整个循环隐状态会在低精度里
跨全部 T 步传递，而在一个 dispatch-bound 的步上换来约等于零的提速。

### 1.8 确定性开启，代价实测后写进 README

`torch.manual_seed` + `random.seed` + `np.random.seed`，`torch.use_deterministic_algorithms(True)`，
`cudnn.benchmark=False`，脚本里设 `CUBLAS_WORKSPACE_CONFIG=:16:8`（`nn.LSTM`/`nn.GRU` 仍带
cuDNN-RNN 的确定性要求，而它们是对照臂），
以及 **`AutoNCP(..., seed=)` 显式传入并随 run seed 变化** —— 它从自己的
`np.random.RandomState(22222)` 取数，与 `torch.manual_seed` 完全无关，
否则 10 个"seed"下拓扑是静默固定的。

开关各计时 50 步一次，把百分比写进 README；预期是个位数，因为确定性只碰卷积反向，
而 dispatch-bound 的循环那一半本来就是确定的。

### 1.9 两条评测路径，刻意不同

- **每 epoch 验证**：批量 T=16 窗口（590 个窗口一个 batch，约 38 ms），用于早停和训练曲线。
- **有状态连续 rollout**（约 3.7 秒纯串行 dispatch，约等于三分之一个训练 epoch）：
  只用于最终测试数字、OOD 网格、长度扫描。每 epoch 都用 rollout 会白烧三分之一训练时间。

**在训练曲线下写一句，否则它看起来像 bug**：逐 epoch 的窗口式验证 MAE 会略**差于**最终 rollout MAE，
因为它包含冷启动帧。

早停和 checkpoint 选择用**验证分 bin MAE**（宏 skill score），**绝不用全局 MSE**
（后者在每次运行里都选出最收缩的那个 checkpoint）。两者每 epoch 都记，选择反事实就是免费的。

### 1.10 OOD 网格当 batch 轴，不是 for 循环

rollout 的 dispatch 成本（约 9,450 个串行时间步 × 49 次 launch ≈ 3.7 秒）是每次 rollout 的、与 batch 无关，
所以逐格循环要约 134 分钟，而把 64 个网格格塞进 batch 维只要约 3.2 分钟。约 40 行，约 40×。
**重点不是省两小时，而是这张 2×2 图会被反复迭代 —— 这是少见的买人力时间的优化。**

### 1.11 吞吐与两个内存陷阱

用约 20 行的 `xargs -P 6` 队列在单卡上跑 6 路并发；限制来自 CPU 核（每个 job 一个饱和的 dispatch 线程），
不是 GPU（平均利用率 10-20%）。有效吞吐约 4.3×。

- **重访自相似矩阵必须先降采样到 ≤6,300 帧**（每 10 帧取一）再物化 —— 63,000 帧下是 15.9 GB，
  且超出 PIL 图像炸弹保护 22 倍，**那个验证整个切分的审计会在第 2 周直接死掉**。
- 测试集对训练集的最近邻搜索按 query 块分块，只保留 running minimum。

### 1.12 CPU 退路（写进计划）

因为是 dispatch-bound，CPU 惩罚反而只有约 4-8×（16 核约 33 分钟/run，全项目约 60-120 CPU-小时），
所以第一周 CUDA 不顺时触发的是"退回 CPU 继续推进"，而不是三天的 wheel 考古。

---

## 2. 十六阶段协议

GPU 成本列合计约 25 小时。「切」列给出落后时的砍单顺序。

### 必守核心（永不砍）

| # | 阶段 | 内容 | GPU | rubric |
|---|---|---|---:|---|
| **P0** | Day-0 硬件与正确性门 | 见 §1.1。外加：单 batch 过拟合（该 batch **必须含一个 \|angle\|>40° 的帧**，通过标准是该帧误差 <1°）；增强时序一致性单元测试；翻转标签取负测试；**一段 10 秒片段的帧/标签 GIF 对齐检查**；按序解析 data.txt 后随机抽 20 行重解码比对（永不 glob） | 0.4 h | Code/Doc(20) |
| **P1** | EDA 与数据审计（一次解码 pass） | 时间戳 `'%Y-%m-%d %H:%M:%S:%f'`（毫秒前是冒号）、断言单调、实测 fps、log-y Δt 直方图、列出所有 Δt>0.5s 并据此切 segment；\|angle\| 阈值 {1,2,5,15,40,100}；真实 min/max/p1/p50/p99；ACF 到 lag 1800，缓冲 = max(首个 \|r\|<0.1 的 lag, 300)；**(split×bin) 表**：n_frames / 墙钟分钟 / 独立转弯事件数 / 平均\|angle\| / **逐 split 的分 bin predict-0 与 persistence MAE**；静止段掩码；6,300 帧子采样的重访审计 + 分块最近邻直方图 | 0.1 h | **12 分**（Data 各项 + Quant Measures） |
| **P2** | 指标与判据冻结，**commit 进 git** | stats.txt 出来当天，把这些写进 README 的 Quantitative Measures 并提交：bin 边界（从直方图结构 + 测试支撑要求推导）、headline 标量（宏 skill score = 1 − 各 bin 上 MAE_model/MAE_predict0 的均值，predict-0 恰好为 0 且无法靠收缩取胜）、headline 帧子集、逐帧指标、**bin 按真值划分**、最终数字来自有状态 rollout、以及判据：**"只有当 seed 的 min-max 带不相交时才把效应报告为真实"**。README 引用该 commit SHA | 0 | Quant Measures(2) + Justification(20) |
| **P3** | **模型图（4 面板）+ 参数推导表，第 3 天写完**（不依赖任何结果） | 面板 1：编码器每条箭头标张量形状，今天就能硬编码 —— 66×200×3 → 31×98×24 → 14×47×36 → 5×22×48 → 3×20×64 → 1×18×64，flatten **1152**，FC 1152→32，编码器 168,244 可训练。**附一句：Bojarski et al. 对这一层写的是 1164，与他们自己的架构图（64×1×18=1152）矛盾；我们全程报自己实测的形状。** 面板 2：`wiring.draw_graph()` 画**我们自己那张** AutoNCP。面板 3：一个 CfC 步的闭式方程，**对照 Hasani et al. 2022 核对**，时间门老实画出来。面板 4：三步展开带，显示状态箭头和 many-to-many 结构。参数表每模块一行、算术写出来（`5×5×3×24+24=1,824`）、手推列与 torch 列总和相符、**三列：可训练 / 冻结 mask / 总计** | 0 | **Model Figure(4) + Parameters(4)** |
| **P4** | **GO/NO-GO 时序余量门**（在任何调参之前） | 10 runs：CfC ×5 seed，CNN + MLP(32→128→1) 无循环头 ×5 seed（参数与 CfC 循环块匹配到 3 个以内）。lr=1e-3 暂定，T=16，增强关，30 epoch。外加零成本地把 10 个 checkpoint 用**窗口内帧序打乱**重评一次 | 1.0 h | Justification(20) |
| **P7** | **Headline 对照臂与包夹控制**（冻结配置下） | 45 runs，30 epoch。CfC 10 runs = **5 wiring-seed × 2 init-seed 因子设计**；匹配 LSTM 10 init seed；GRU 5；**不受约束的 LSTM（约 5.75× 容量）5**；CNN+MLP 头（容量匹配）5；CNN+Linear 头（33 参数）5；**双帧 CNN**（t−1 与 t 堆成 6 通道，无循环，只有 conv1 变）5 | 4.0 h | Quant Results(8) + Params(4) + Curve(4) + Justification(20) |
| **P8** | 增强消融 + **把逐帧 bug 当测量仪器跑** | 消融 {无 / 仅翻转+亮度(k=0) / k\* 全量} × {CfC, LSTM} × 5 seed（减去 P7 已有的 10）→ 20 新；CNN+MLP × {无} × 5。**bug 对照：增强参数按 `(B,T,1,1,1)` 而不是 `(B,1,1,1,1)` 采样 —— 一个字符的改动 —— CfC×3 + CNN×3 = 6 runs**。增强关的 checkpoint 免费兼任亮度 OOD 面板的第二条曲线 | 3.0 h | **Advanced Concept(10)** + Justification(20) |
| **P9** | **时序论断路线 1：训练时破坏帧序** | 10 runs = CfC×5 + LSTM×5，训练窗口的 16 帧随机置换（每窗口每 epoch 换一个新置换），标签同样置换。**不需要纯 CNN 臂**：它按构造不变，而那个不变性就是一致性检查 | 1.0 h | Justification(20) |
| **P13** | **纯推理的再聚合 —— 全协议里每小时得分最高的一项** | 0 训练。在 294 次运行已产出的预测上做 10 项分析：**(1) 逐帧 MAE vs 窗口内位置 t=1..16【主图】**；(2) 每步前把隐状态清零、权重不变 —— 状态链路的精确切断；(3) 评测时帧序打乱；(4) 长度泛化 T∈{8,...,256,full}，rollout 按 256 帧分块并在块间传 hx；(5) 预测的一阶差分布与 lag-1 自相关 vs 真值；(6) **Chahine 式自洽性**（扰动 vs 无扰动输出的偏离，不用真值）；(7) 分 bin p95 绝对误差 + 虚警率 P(\|ŷ\|>5 given \|y\|<5)；(8) 宏 skill score；(9) **120 个网格配置的验证-测试散点**（只在配置冻结提交之后算）；(10) checkpoint 选择反事实（全局 MSE vs 分 bin MAE，从已写的逐 epoch 日志重算） | 0.5 h | Justification(20) + Quant Results(8) + Intro(4) |

> **P13 必须在第 1 周用随机张量搭好**，在任何真实模型存在之前 —— 这就是它不在第 3 周关键路径上的原因。

### 可缩减 / 可砍

| # | 阶段 | 内容 | GPU | 砍序 |
|---|---|---|---:|---|
| **P5** | **完整笛卡尔超参网格**（仅 CfC，15 epoch） | 120 runs = lr{3e-4,1e-3,3e-3} × T{8,16,32,64} × 平移系数 k{0,0.05,0.1,0.2,0.4} × 2 seed。选择用验证分 bin MAE。固定并**声明为固定**：AdamW / wd 1e-4 / clip 1.0 / batch 64 / cosine / 30 epoch / patience 10。报三条一维边缘切片，120 行 CSV 留在 `runs/` | 7.6 h | 减到 1 seed（60 runs）。**绝不退回坐标下降** —— 完整网格是人力更省的选项（嵌套循环 vs 带决策点的分阶段协议），而且它把选择有效性散点从 n=15 的姿态变成 n=120 的结果 |
| **P6** | **逐架构学习率诊断** | 36 runs = 4 臂 × lr{3e-4,1e-3,3e-3} × 3 seed，15 epoch。**交付物是两个数字的对比，不是新操作点**：共享 lr 下的 CfC−LSTM 差距，vs 各臂用自己最优 lr 时的差距。**所有被报告的轴仍用共享 lr** | 2.0 h | 减到 1 seed（12 runs）。**不要整个砍** —— 一个人力小时，堵住 CfC-vs-LSTM headline 最明显的攻击 |
| **P10** | 容量阶梯，**每一级都重新匹配**，附录 | 12 新 runs = units{16,64} × {CfC, LSTM（H 按该级实测 CfC 计数重新推导）} × 3 seed；units=32 从 P7 免费得来。横轴用 log2 的**实测**参数量，任何残余失配显示为可见的水平偏移，而不是一个"相等"的声称 | 1.3 h | **第 3 顺位砍**。附录 |
| **P10b** | 优化步预算检查：**用测量回答 stride 问题** | 6 runs = 4× 步预算（44,100 随机起点窗口/epoch，约 690 步/epoch）× {CfC, LSTM} × 3 seed | 2.3 h | 第 4 顺位。**随机起点采样器本身不可砍** |
| **P11** | 切分敏感性：**第二个方差来源** | 9 runs = 一个替代时间边界 × 3 臂 × 3 seed。替代边界从 (split×bin) 事件计数表里选，使每个 split 都含一部分山路段；**仍严格按时间切**，用实测缓冲，永不块状轮转、永不地理留出 | 0.85 h | **第 1 顺位砍** |
| **P12** | LTC 作为机制与成本臂，附录，**用测量修好 F2 的理由** | 3 runs × 3 seed，同一 wiring。外加两项零训练测量：`TorchDispatchMode` 数每训练步的 dispatch 数；50 步计时对比。**两个参数量都发表**，精度列标注为 wiring-matched 而**非** capacity-matched | 0.45 h | 第 2 顺位砍（3 次训练）。**dispatch 计数与计时不可砍** —— 它们不要训练，而 F2 要求这个理由无论如何都得修；没有测量，修法只是换个说法 |
| **P14** | OOD 退化网格，**支撑章节**，网格当 batch 轴 | 0 训练。6 臂 ×(10 或 5) seed × 4 族 × 5 档 × 2 模式（每窗口冻结 vs 逐帧 i.i.d.），约 64 格塞进一次分块 rollout 的 batch 维（约 3 分钟）。**四个族，只有这四个**：高斯像素噪声(UNSEEN)、亮度推到训练抖动范围 2× 以外(SEEN，同时画增强训练与不增强两条曲线)、**丢帧{0,10,25,50%}（唯一触达时序通路而非共享编码器的族）**、随机遮挡面积占比。所有档位在验证集选定并冻结，测试集只跑一次。主面板是加性退化 MAE(s)−MAE(0)，**在画任何曲线之前先用文字定义**；第二面板给含 s=0 的绝对分 bin MAE；**没有比值面板**。外加击穿点表 | 0.3 h | 第 4 顺位 —— 减到两个族（高斯噪声 + 丢帧），保住 SEEN/UNSEEN 对比和唯一有区分力的那个族 |

### 两个门

- **G1（P1 之后）**：若最高 bin 的独立测试事件数 < 约 10 → 合并 [15,40) 与 [40,∞)，
  headline 帧子集移到 |y| ≥ 5。若某个 split 几乎没有山路段 → **手工重切时间边界**
  （例如 60/20/20 并挪动切点），**永不块状轮转、永不地理留出**。
- **G2（P4 之后）**：若无循环臂落在 CfC 的 5-seed 范围内 **且** 评测打乱对 CfC 毫无代价
  → **没有时序余量**。把 README 主线转向"有无时序"这个问题 + 数据质量故事
  （泄漏修复、重访审计、predict-0 基线里那些静止帧的质量），立刻砍掉 P10 / P10b / P12，并诚实报告这次转向。
  **在第 2 周而不是第 4 周知道这件事，就是把 P4 放在网格之前的全部目的。**

---

## 3. 恢复 / 仍然砍掉 / 不要花在什么上

### 恢复（原审查纯因算力而砍的）

- **Seed —— 完全恢复。** headline 两臂各 10，其余对照臂各 5，合计 4.0 GPU-小时。
  **免费升级**：CfC 的 10 次是 **5 wiring-seed × 2 init-seed 因子设计**，
  把 NCP 拓扑抽签点名为一个稠密基线在结构上没有的方差来源。
- **GRU —— 恢复，5 seed，表里一行、附录，永不进主图的线。**
  原来的砍有两半理由：8 分呈现项上的广度 vs 深度（真实）、以及资助四次额外 seed（作废）。
  呈现那一半用一条**事先注册的合并规则**买下来：若 LSTM 与 GRU 的带在每个 bin 都重叠，
  就折叠成一行"门控家族"，由 LSTM 在所有图里代表该家族。
- **LTC —— 恢复，3 seed，仅附录，重构为机制与成本臂。** 原砍的两个理由是一个反了的论证(F2)
  和一个不公平的参数匹配(F3) —— **都不是算力问题**，而且都可修。
- **完整笛卡尔网格 —— 恢复到 120 runs。** 两个理由只有一个是算力：
  笛卡尔网格**人力更省**，而且它把 REVIEW A2 的选择有效性散点变成真结果。
- **AutoNCP units 固定 —— 部分恢复为 3 级匹配阶梯（附录）。** 混淆论证本身是对的，
  算力不改变它 —— 但花三个人力小时在每一级重新推导 LSTM 隐层就能移除它。
  32 仍是**事先声明的** headline，阶梯只用来说明比较对匹配不敏感。
- **训练 stride 4 —— 恢复为一次测量，而不是改成 stride 1。**
  天真地"恢复 stride 1"是陷阱：stride 决定的是优化步预算，那会让每个比较的训练量静默翻四倍。
- **逐架构学习率 —— 恢复为 36 run 的诊断**，但被报告的运行仍用共享 lr。

### Instructor 裁定（2026-09-10，当面）

- **CfC 作为 RNN 组件可以** → PROJECT.md §13 第 1 问关闭。不是需要审批的 neural-ODE 类方法。
- **固定 16 帧窗口训练 + 评测时 stateful rollout 满足变长序列要求** → §13 的变长顾虑关闭。
  注意这**确认了**现有设计（训练定长、评测演示长度泛化），M8 的"演示而非断言"不变。
- **领域适配增强能否计入 Advanced Concept：不确定** → 那 10 分不能单押。见下方 Transformer 条目。
  行动：用"改标签 + 系数从数据标定"的具体说法重问一次，并索取书面答复。
- §13 第 4 问（能否用第三方实现）已因手搓 CfC 自然作废。

### 仍然砍掉（尽管算力免费）

- **手写雨/雾** —— 理由已经**转移**了：`albumentations` 让它变成一小时，所以实现成本的理由确实弱了。
  但它输在 **README 版面**：2×2 网格四个面板全部有主，其中三个已经是探测共享 CNN 编码器的光度族。
  第四个光度族只加行不加机制，而且雨条纹在构图上与亮度/阴影增强重叠，**弄浑那张图里最干净的
  SEEN/UNSEEN 故事**。**这是最可能被"现在只要一小时了"这种理由错误翻案的一条。**
- **合并 2017 数据集 —— 仍然砍，连"只推理跑一遍"也砍。** 后者需要第二次下载、解析器、
  预处理路径和 memmap，一个没有时间戳的假定 fps，以及把 F7 的按文件名关联再做一遍 —— 
  号称 4 小时，实际 8-12 —— 换一句不在中心论断反对清单上的话。
- **Transformer —— 角色改变，不再砍（2026-09-10 更新）。** 原理由是：带学习位置嵌入的因果注意力
  做不了 M7 要求的、其余六臂共享的有状态连续 rollout，会逼出第二套评测协议。
  **那个理由只在它充当「比较臂」时成立。** 两件事改变了计算：
  (a) instructor 对"领域适配增强算不算 Advanced Concept"答复**不确定**，所以那 10 分不能只押一条路
  （rubric 是 "at least one of"，两条并存不扣分）；
  (b) 更重要的是它补上了项目缺失的第三个包夹 —— CfC/LSTM 把历史**压缩**进状态，
  CNN/双帧 CNN **没有**历史，而 Transformer **完整访问全部 T 帧且不压缩**。
  它给出的是"一个 T 帧窗口里究竟有多少时序信息可用"的上界，与循环状态能否抓到它无关：
  **Transformer 也与 CNN 打平 → 强证据是这段视频没有时序信号，而不是我们的循环层不行；
  Transformer 明显赢过两个循环臂 → 瓶颈在状态压缩而非信息量。**
  这把它从 scope creep 变成缺失的对照。
  **约束：它只进窗口路径的表**（`window_batches`，逐位置 MAE 主图本来就跑在上面），
  rollout 路径仍然只有那六臂，共享评测路径的保证照旧成立 —— 只是把范围在 README 里明说。
  约 25 行，参数量配到 24,832，5 seed，约 0.6 GPU-小时。**砍序：第 5 顺位**（在 P11 之前）。

- **不规则 Δt 的训练消融 —— 仍然砍。** 若实测 p99/p50 只差百分之几，它按构造就是零；
  而且要打补丁修 ncps 1.0.1 的 `timespans`（batch>1 时坏，且当 B 等于隐层宽度时**静默**失败）。
  免费替代品：那句诚实的 Introduction、面板 3 里老实画出的时间门、以及丢帧 OOD 族。
- **训练时按 batch 重采样 T（M8 的训练侧改动）—— 砍，但保留它的交付物。**
  在比较中间注入混淆。变长改为**在评测时演示**：一套权重，
  在 T∈{8,16,32,64,128,256,full} 上做有状态 rollout。
- **被报告运行里的逐架构学习率 —— 仍然砍**（只恢复诊断）。
- **超过 10 个 seed —— 仍然砍。** 过了约 10 带就不再可读，逼出小提琴图，损失 8 分的呈现项。
- **任何 n 下的 p 值与 t 检验 —— 仍然砍。** 各次运行只差 seed，不是一个检验所描述的总体的抽样；
  **错的是可交换性假设，不是算术。**
- **`torch.compile` / CUDA graphs / AMP / 更大 batch 当提速杠杆 / 租 GPU —— 全部砍，
  并在 README 里点名并说明理由。** compile 能赢的唯一地方是折叠循环 dispatch 循环 —— 
  风险最高的路径，已记录的失败模式是**静默的数值错误**，正好落在本项目要测的那个差距里，
  而且网格用到的每个 T 都要重新编译。租用四重失败，决定性的是
  **第二个 `runs/` 目录会破坏 `scripts/make_tables.py` 依赖的单一事实来源（20 分）**。
- **其余仍砍**：块状轮转 / 地理留出切分；L1 或 Huber 换损失（瞄准条件中位数，在零峰分布上收缩更狠）；
  `PREREGISTRATION.md` 文件（用 commit 图）；干净机器下载彩排；
  **任何架构间的墙钟/每小时 epoch 数对比表**（`nn.LSTM` 拿到一个融合 cuDNN kernel 跑完整个序列，
  而 ncps 的 CfC 是发约 4,185 次 dispatch 的 Python 循环 —— 这种表会断言一件假事，一句话打发掉）；
  把 units 扫描当调参呈现；超过 8 张主图。

### 不要把算力花在

**更多架构。** 最强的诱惑和最差的交易。第五第六个循环家族花 0.5-1 GPU-小时，
换来每张图上永远多一条线，而它**不提高存在真效应的概率 —— 只提高伪效应的概率**，
而那恰恰是 20 分项惩罚的东西。
**准入规则：每一臂都必须由一句"它许可了哪个删除、或关闭了哪个具名反对"来辩护。**
现有六臂各自有这样一句（H=64 臂退掉"你把基线缩小了"；两个包夹头把状态容量与头容量分开；
双帧 CNN 提供机制；GRU 把门控家族折叠成一行）。**没有这样一句话的臂不准进。**

**预训练骨干（ResNet/ViT）。** §10 仍然正确，算力不触及那个推理。

**更多 seed。** 10 是呈现天花板，不是预算天花板。

**更多 OOD 族或更细的扰动档。** 整个网格最奢侈也就约等于一次训练 —— **这正是它危险的原因：
它是把 README 变差的最便宜的方式。**

**kernel 级性能工作。** TF32 应该**关掉**，每步多花几毫秒买那句
"我们验证了这个差距不是精度伪影"。

**租 GPU。** workload 是 dispatch-bound，租来的 FLOPs 正是已经闲置的那种资源；
全项目约 25-32 GPU-小时；每台租来的机器都在消耗那个**真正稀缺**的资源（人力小时）。
拒绝它，并在 README 里用一行加上实测每次运行墙钟说明，让 TA 不用同款卡也知道该期待什么。

**更长训练。** 30 epoch、patience 10、5,160 优化步已经很宽裕。结果不在 epoch 数里。

**为了更好看的数字重跑。** P2 的冻结 commit 的存在就是为了让这件事不可用。

**不会出现在 8 张图或 3 张表里的任何运行。** 这是总规则，上面各条都是它的实例。

---

## 4. 图表清单（8 图 3 表）

### Figure 1【主图】逐帧 MAE vs 窗口内位置

单面板约 6×4 英寸。X：整数位置 t=1..16，线性，每格标注，标签"position within 16-frame evaluation window"。
Y：逐帧 MAE（方向盘度），线性，从 0 起，在 P2 冻结的帧子集上（**|y_true| ≥ 5°**，
即弯道 bin 的并集，**特意这样选，使这张图在 40° 边界的支撑审计失败时仍然成立**）。

七条系列，每条是 seed 上的中位数 + 同色 min-max 带 + 每次运行画成点：
CfC(n=10, 实线, 饱和)、匹配 LSTM(n=10, 虚线)、GRU(n=5, 细线, 合并规则触发则省略)、
不受约束 LSTM(n=5, 点线)、CNN+MLP 头(n=5, 灰实线, 按构造是平的)、
CNN+Linear 头(n=5, 浅灰虚线, 平)、双帧 CNN(n=5, 灰点划线, 平)。

**外加两条 CfC 与 LSTM 同色的细点线**：同一批 checkpoint 在帧序打乱下重评的结果，
应当又平又高 —— **实线与其孪生线之间的垂直间距，就是打乱对照被画成了一条曲线**。

两条横跨面板的水平参考线：同一子集上的 predict-0 MAE（行内标数值）；
以及 persistence，标注"uses the previous true angle — unavailable at deployment"。
t=1..3 覆盖一条浅灰竖带，标"cold start"。
右上角标注：CfC 与 LSTM 各自的 Δ = MAE(t=1) − MAE(t=16)，度数与带宽并排印出，
**让读者不用量像素就能把效应和噪声作比较**。

图注写明：逐帧指标、bin 按真值、stride-1 窗口且每个窗口起点重置状态
（所以每帧在全部 16 个位置上都被评过，位置比较是**帧内**的）、每条线的 n、
带是 min-max 不是置信区间、正文其他测试数字来自有状态 rollout。

> **三条平的非循环线也是这张图的自检：如果它们不平，位置统计就是坏的。**

### Figure 2 效应阶梯：什么在移动急弯误差，对照 seed 噪声

两个水平面板共享约 14 个行标签。
面板 A（70% 宽）：x = 同一弯道帧子集上相对参考配置的 ΔMAE（度），正 = 更差；
**背后一条灰竖带覆盖参考配置的 seed min-max**，更浅的一条覆盖 P11 的切分带（若更宽），
在 1× 与 3× 带宽处画细线。面板 B（30%，同样的行，无标签）：Δ 宏 skill score。

行按 |Δ| 降序：无循环(Linear 头) / 无循环(MLP 头) / 训练时帧序打乱 / 逐帧(有 bug)增强 /
无增强 / 评测时状态清零 / 评测时帧序打乱 / T=8 / 双帧 CNN / T=64 / 4× 优化预算 /
→LSTM / →GRU / →不受约束 LSTM / 替代切分边界。

标记**形状**编码干预类型（圆 = 重训，三角 = 同权重不同评测 —— 于是读者能看出
评测时打乱**低估**了训练时打乱）；**颜色**编码家族（时序结构 / 增强 / 架构替换 / 协议），四项图例。

> **它是一个列表，所以它靠删行来降级** —— 只剩 10 行时仍是同一张图讲同一个论证，
> 这就是它能安全地待在第 2 位、却太脆弱不能放第 1 位的原因。

### Table 1 headline 分 bin 结果

行：predict-0、persistence，一条分割线，然后是模型。
列：四个（合并后三个）bin + 总体 Pearson r，**每个列头带该 bin 的测试帧数与独立转弯事件数**。
每格：度数 MAE + 同 bin 相对 predict-0 的比值 —— `3.9° (0.62×)`。每列加粗最优。脚注给 seed n 与带宽。

### Figure 3 OOD 退化，2×2 共享 y 网格

高斯噪声(UNSEEN) / 亮度推到训练抖动 2× 以外(SEEN，画增强训练与关增强两条) / 丢帧 / 遮挡。
Y 是加性退化，**在曲线存在之前先用文字定义**；第二小面板给含 s=0 的绝对分 bin MAE。
**无比值面板。** 一个共享图例，predict-0 画虚线。配击穿点表。

### 其余四张主图

四面板模型图、带切分边界与分 split bin 计数的转角时序图、
共享坐标轴的训练曲线（画**验证分 bin MAE**，即早停所选的同一个量）、
两个定性样例合成的一张有状态 rollout 图。

### 附录面板 skill vs 实测循环参数量

x = 循环块可训练参数，log2，**画在实测值上**，所以参数匹配是一个可读的坐标，
任何失配显示为可见的水平偏移，而不是一个"相等"的声称。它的职责是一句话：**比较对匹配不敏感。**

---

## 5. Seed 与方差协议

**分配。** CfC 操作点：10 = **5 wiring-seed × 2 init-seed 因子设计**。匹配 LSTM：10 init seed。
GRU / 不受约束 LSTM / CNN+MLP 头 / CNN+Linear 头 / 双帧 CNN：各 5。
训练时打乱臂：各 5。增强消融各格：5；故意 bug 对照：3。
容量级 / 切分偏移 / 4× 预算 / LTC / 逐臂 lr 诊断：各 3。网格格：2。**任何地方都不超过 10。**

**报告什么。** seed 上的中位数、**min-max 带**、以及每次运行画成一个点让读者看见 n。
用词是 **"min-max over n seeds"**，**绝不写 "error bar"，绝不写置信区间**。
带宽在每一个与之比较的效应旁边数值印出。

**报告两个方差来源，不是一个**：**seed 带**和 P11 的**切分带**；
README 说明哪个更大，并把每个效应对着 `max(seed 带, 切分带)` 引用。
**这是几乎没有本科报告有的东西，也是让一个 null 成为"测量"而不是"没使劲"的原因。**

**判据，在第一次比较运行之前提交。** 写进 P2 的冻结 commit：
*一个效应只有在 min-max 带不相交时才报告为真实*。小于一个带宽的效应写成
"not detectable at n=10"并给出带宽。**提前十二天声明阈值、在 README 里引用 commit SHA，
才是真正防止阈值挑选的做法** —— 而 commit 图是合规形式，因为 REVIEW 禁止 `PREREGISTRATION.md` 文件。

**如何在不搞 p 值戏法的前提下做出可辩护的论断 —— 三种描述，零推断：**

1. **配对胜出计数。** 操作点上报告 10×10 = 100 次配对 seed 比较中偏向 CfC 的次数。
   明说这是对所跑的 100 次比较的完整描述；**点名它对应的 Mann-Whitney 统计量，
   并用一个从句说明我们为什么不把它转成 p 值** —— 各次运行只差 seed，
   是从我们自己的训练流程里抽样、而不是从一个数据集总体里抽样，检验所假设的可交换性才是错的那一环。
   **点名它然后拒绝它，比默默享受它的修辞好处更诚实。**
2. **跨冗余轴的符号不变性。** 协议提供了同一比较的四次独立复现：
   三个容量级(P10)、两个切分偏移(P11)、增强开/关一对(P8)、共享 lr / 各自 lr 一对(P6)。
   若**符号**在所有这些和所有 seed 上一致，就报告为一致并给出计数 ——
   "CfC−LSTM 差值的符号在 4/4 次复现和 100/100 次配对比较中相同"。
   **一个跨冗余轴不变的符号，是比单一操作点上的显著性更强的证据，而且它是关于我们跑过的运行的事实，不是一个概率。**
3. **一切以带为单位。** Figure 2 把全部十四项干预都用参考带的倍数计量，在 1× 与 3× 处画线。
   **那个换算 —— 从"我们的数字是 X"到"这个干预移动它九个带、那个 0.4 个带" ——
   就是把 294 次运行变成一把尺子而不是一张表的东西。**

**两句必须出现的诚实条款。**
其一：**CfC 携带一个稠密 LSTM/GRU 在结构上没有的方差来源（生成的 NCP 拓扑）**；
我们没有调它，报告的是 10 次运行的合并范围而不是最好的那个拓扑。
其二：**参数匹配是必要而非充分的** —— 匹配之后，状态维度、连接稀疏性、时间常数参数化仍然混淆着，
所以任何差距只能归因于"这个家族在这个规模下"，而不是连续时间本身。

---

## 6. 人力再分配与周计划

GPU 一晚上跑完；3 个人一个月约 180 可用人时，而基线管线（下载、预处理、EDA、切分审计、
dataset、编码器、模型工厂、训练循环、评测、README）合理估计吃掉 100-120。
上面所有加装的边际成本约 **22 小时**（P0 断言 +4、指标冻结 1、逐臂 lr 1、bug 对照（一个字符）含在 P8 的 2 里、
训练时打乱 3、容量阶梯 3、切分偏移 3、LTC 机制 1.5、双帧 CNN 2、状态切断 0.5、随机窗口起点 1）
—— **刻意压得这么低，因为每一条要么是 seed 循环、要么是 config flag、要么是嵌套循环、
要么是一个字符的改动、要么是对已有预测的再聚合。除了帧置换和随机起点采样器，没有一条是新的训练期代码路径。**

省下来的时间按此顺序去五个地方：

1. **GPU 碰不到的那 20 分，排在第 1 周。** 四面板模型图和参数推导表在**第 3 天**写完。
   Ethics 第 1 周写成散文，按 rubric 名词分三个小标题，加入旁观者群体、两三条带张力的好处、
   以及"我们为此做了什么"清单。Data 各节在 `stats.txt` 出来当天可写。
   Authors 写一次（四条工作流，各带 owner 与 backup），proposal 复用。
2. **评测框架，第 1 周用随机张量搭好**，在任何真实模型存在之前。这是那个 14 小时的项，
   它解锁 P13 和 P14，也是那些"每小时得分最高"的纯推理分析不在第 3 周关键路径上的原因。
3. **20 分的 Code/Documentation 基础设施，约 13 小时，当作承重件而不是可选项。**
   `runs/<name>/{config.json, metrics.csv, final_metrics.json}`（含 config hash、git SHA、
   `importlib.metadata` 版本）；`scripts/make_tables.py`；`scripts/make_figures.py`；
   一个所有图共用的 seed 带绘图 helper；钉版本的 `requirements.txt`；
   覆盖 `data/`、`*.npy`、`runs/*/checkpoints/` 和 `figures/` 里的帧的 `.gitignore`；
   用 gdown 加 SHA-256 校验的 `data/download.sh`；逐 epoch checkpoint 与 `--resume`；
   "Reproducing our results"里的字面命令序列。
   **硬规则：如果第 2 周结束时 `make_figures.py` 还不存在，停止启动新实验直到它存在。**
   **在最后一周手工拼装 294 个 run 目录，正是一个有好运行的项目交出烂 README 的方式。**
4. **第 3 周末冻结，第 4 周只写作。** 冻结后不新增架构、数据集、指标或图。
   **Justification(20) 有一个具名 owner 和整整三天** —— 没有人是"那个写字的"。
   §12 优先规则成立：最后一周任何未打勾的交付物，靠砍实验完成，绝不靠砍写作。
5. **第 1 周的书面事项。** 给 instructor 发邮件拿到书面确认：领域适配增强算 Advanced Concept
   （§13 第 2 问）—— 因为唯一便宜的退路（单层因果注意力）会破坏共享 rollout 协议，
   在第 3 周才发现代价很大。用 `stats.txt` 而不是 PROJECT.md 写 proposal。
   从 repo 版 PROJECT.md 里删掉 §2 的评分表和 §13 的四问。

**周计划**：
- **第 1 周** — 门、EDA、指标冻结、模型图、评测框架、基础设施、proposal、Ethics 散文
- **第 2 周** — go/no-go 门、网格跑一夜、超参冻结进带时间戳的 commit、逐臂 lr 诊断
- **第 3 周** — seed 运行与消融跑一夜（总共一夜）、全部纯推理分析、8 张图全部脚本生成
- **第 4 周** — 只写作，外加由**没写那份文档的成员**通读一遍复现命令序列

---

## 7. 风险

**弯道 bin 可能太薄，而在查清之前什么都不能定。** 若 (split×bin) 审计返回个位数独立转弯事件，
seed 带会宽到吞掉架构行，Figure 2 退化成一条灰带里插十四个点。缓解已接进 G1，
**而这就是 Figure 1 从一开始就定义在 |y|≥5 子集上、而不是定义在 40° bin 上的原因**。
这是真正的 go/no-go，而且它在 EDA 之前无法察觉 —— 所以 EDA 是 P1 不是第 2 周。

**最可能的结果是 null，而 null 比 gap 更难呈现。** 四重缓解（按强度降序）：
Figure 1 特意选成即使两线重合仍携带三件事；Figure 2 让 null 以"带阈值和实测噪声地板的测量"的形式可读；
双帧 CNN 为 null 提供**机制**（约 4.4k 的循环层无论哪个家族都能表示一阶差分，
而在 30 fps 单路线录像上，一阶差分之外可能本就没多少东西）；README 引用 Chahine 的数字预先把平手框定为预期。
**未缓解的部分**：如果 CfC 在这份数据上真有分布外优势，本协议比一个投入严重偏移与长时程发散的设计
更不容易发现它。**这是用"正面结果的机会"换"可辩护结果的确定性"的一次刻意交易，应当明说而不是藏起来。**

**完全没有时序余量（G2）。** 应对已预先决定，见 §2。

**sm_120 / 驱动。** 570.x-579.x 的机器能装上 torch 然后每个 kernel 都失败，
而且这是一个**不同于** "no kernel image" 的错误，两种情况下 `torch.cuda.is_available()` 都返回 True。
缓解见 §1.1。**已记录的退路**：CPU 只有约 4-8× 惩罚，所以糟糕的 CUDA 一周触发"退回去继续跑"。

**provenance 崩塌。** 294 个 run 目录是真实负债。由 §6 的第 2 周硬规则和"单一 `runs/`"决定缓解。
**这是最容易被低估的风险，因为它在最后一周之前没有症状。**

**人力超支。** 145-160 人时对约 180 很紧。由明确砍序缓解，
并有一条常备提示：最可能超时的四项是 GPU 侧批量增强器、batch 轴 OOD 网格、
分块有状态 rollout、切分边界参数化 —— 每一项都是"顺利一天，不顺三天"。

**bug 对照段落是对一个没人量过的效应量的下注。** 若逐帧增强只让 CfC 损失 0.2° 而不是 2°，
这个设计里最强的一段就蒸发了，三次运行和一个小节标题换来一个耸肩。
赔率看起来不错（四个独立审查视角收敛到 B3），但它是一次下注 ——
退路是**无论测出什么，那个单元测试仍然挣到它的 Code/Documentation 那句话**。

**一个自找的协议让步，披露而非隐藏。** 选择有效性散点会为 120 个非最终网格配置计算测试集指标。
没有任何东西是在它上面选的，而且它只在配置冻结进带时间戳的 commit 之后才运行，
但 **README 必须明说测试集被评估了两次** —— 一次产出所有被报告的数字，一次为那个审计 ——
而不是声称只碰过一次。严格的读者有权反对；它买到的那一段（几乎没有本科队伍会检查自己的模型选择是否有效）
值这个让步，**但只有在让步被说出来的前提下**。

---

## 附：与手搓 CfC 方案的合并结果（已 settle）

见 [MODEL.md](MODEL.md)。模型基础已从「导入 ncps 的 wired CfC」改为
**手写稠密 CfC，H=64，对 `nn.LSTM(32,64)`**。本协议的结构全部不变，
但**对照臂的尺寸全部重算，而且变简单了**：

| 臂 | 旧（ncps wired `AutoNCP(32,8)`） | **新（手写稠密 H=64）** |
|---|---|---|
| CfC 循环块 | 约 4,365 可训练（+1,057 冻结 mask） | **24,832，零冻结** |
| 匹配 LSTM | H=20，4,341 | **H=64，25,088（差 +256 = 恰好 4H，1.03%）** |
| 「不受约束的 LSTM」反稻草人臂 | H=64，25,109，需单独一臂 | **不需要了** —— 同 H 下它本身就匹配到 1.03%，**一次运行兼任两个角色** |
| GRU | H=25 | 砍掉（REVIEW §5 第 1 条） |
| LTC | 同 wiring | 砍掉，换成 `no_gate` 一行消融 |
| 每步循环深度 | CfC 3 层 vs LSTM 1 层（混淆） | **两边都是 1** |
| 循环层占模型份额 | 约 2.5% | **12.9%**（193,141 总参数） |

**P7 因此从 45 runs 降到约 30 runs**，而且去掉的那一臂正是为了回应"你把基线缩小了"而存在的 ——
那个反对现在在结构上不成立。**P10 容量阶梯同样简化**：
不再需要在每一级重新推导 LSTM 隐层，因为 `4H` 恒等式在每个宽度上自动成立
（H∈{16,32,64,128} 实测：3,136/3,200、8,320/8,448、24,832/25,088、82,432/82,944）。

**P3 的模型图面板 2 换掉**：`wiring.draw_graph()` 删除，改成一个 CfC 步的标注方程图。
理由见 MODEL.md §3 —— **一张渲染的 NCP 接线图是「接线」的图，不是「前向传播」的图，
而 Model Figure 这一项是按前向传播评分的。**

**P0 的门新增四条**（来自 MODEL.md §6）：`models/interface.py` 先于 cell 存在；
`tests/test_cfc_anchors.py` 的门极限对、纯偏置闭式、Δt 形状契约全绿；
oracle 在 float64 `atol=0` 下绿；以及 `tests/test_hygiene.py` grep `models/` 无 `ncps`。
**锚点永不在砍单上。**
