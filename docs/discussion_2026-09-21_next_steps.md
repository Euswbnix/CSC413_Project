# 待讨论：HDD 到手之后的下一步

日期：2026-09-21。

**状态**：第一节的 1–5 项待团队讨论决定；第二节是只能由特定的人去办的事；第三节可以马上开始；第四节是已确定的事实和约定，不需要讨论；第五节以后再定；第六节汇总风险与不同意见，尚未采纳。讨论结果写进决定记录后，本文件只作存档。

## 背景

- **研究问题**：在驾驶视频的转向角预测中，LTC 与 CfC 相比于获得同样时间信息的 LSTM，是否在观测变稀疏时具有更好的精度与计算成本取舍？
  见 [`decision_2026-09-21_keep_driving.md`](decision_2026-09-21_keep_driving.md)。截止日期是 2026-12-08。
- **数据**：Honda HDD 已获批并下载，137 个会话，105 小时视频。第一周检查见 [`hdd_week1_checks_2026-09-21.md`](hdd_week1_checks_2026-09-21.md)。
  调研里"10/5 前拿不到 HDD 就改用 nuScenes"的节点已经不需要了。
- **关卡**（详见决定记录"顺序与关卡"一节）：
  - D1 数据：多个独立行程、时间戳、同步转角，能按行程或道路划分；
  - D2 有效基线：冻结特征加简单预测头，在验证集上稳定赢过常数，输出确实随真实转角变化；
  - D3 丢帧有效：历史信息对时序基线有帮助，删帧会让它可测量地变差；
  - D4 比较：预注册后正式比较时序模型。

**D1 的现状：**

| 要求 | 状态 |
|---|---|
| 多个独立行程 | 已确认：137 个会话 |
| 逐帧时间戳 | 已确认：每帧有接收端（ROS）时间戳，另有一列相机内部时钟 |
| 画面与转角同步 | **已确认（2026-09-22，修正版）**。画面运动与转向角的互相关：128 个会话、1816 个窗口，逐会话时间差中位数 0.000 秒（窗口数 ≥ 10 的 77 个会话 p10 −0.084、p90 +0.067，分辨率一帧 33 ms）。CAN 横摆角速度另有约 0.1 秒延迟，不能用来对时。13 个时钟漂移会话在 ±15 秒搜索下时间差仍是 0.000 秒，斜率 0.137（接收端积压应接近 1），所以 2.2 小时不用排除。9 个会话没有可用窗口，属于未核对 |
| 能按行程或道路划分 | 两种都可行，选哪种是本次要讨论的第一件事 |

**文中用到的术语**

| 术语 | 含义 |
|---|---|
| macro MAE | 按真实 \|转角\| 分成 < 5°、5–15°、≥ 15° 三档，各档分别求 MAE 再取平均，防止直行帧主导（`metrics.py` 的 `macro_mae`） |
| CCC | Lin 一致性相关系数，1 为完全一致，常数输出恰为 0（`metrics.py` 的 `ccc`）。此前 SullyChen 上 CfC、LSTM 各 35 个 seed 的平均值只有 0.055 和 0.020 |
| 验证集最优常数 | 在验证集标签上求出的、使 macro MAE 最小的常数（`metrics.py` 的 `best_constant`）。它看过验证集答案，是偏严格的对手 |
| LSTM+Δt | 把相邻帧之间的真实时间间隔 Δt 作为额外输入的 LSTM，让它和 CfC、LTC 获得同样的时间信息 |
| macro skill（相对常数 c） | 1 − 模型的 macro MAE ÷ 常数 c 的 macro MAE，常数本身恰为 0（`metrics.py` 的 `macro_skill_vs_constant`）。δ = 0.05 即 macro MAE 比常数低 5% |
| Kish 有效数 | 各单位数据量不均时，折算成"等量单位"的个数：(Σn)² / Σn²。10 个单位里只要有 1 个特别大，有效数就远小于 10 |
| Advanced Concept | 课程评分里单独的 10 分 |

## 一、需要团队决定的事

### 1. 主问题：新的驾驶，还是新的地理区域

> **已定（2026-09-22）**：采用 **A** 作为主划分，保留完整的测试会话；测试集中约 10% 离训练或验证轨迹不到 50 m 的部分**保留在主结果里**，另按 50 m、200 m 分组补充报告，不再为追求零重合去切碎会话。**B 保留为扩展，暂不推进。**
> 同时确定三点：① "新会话"不等于新日期、新天气，措辞要收窄；② 验证集比测试集更靠近训练路线（23% 对 10%），D2 通过只说明验证基线有效，不保证测试表现；③ 49 个测试会话不是 49 个独立样本，统计以路线簇为单位；同步核对的排除规则执行后，测试集为 21 个会话、17 个路线簇（见第 3 条的验收规则）。

两种留出方式检验的是不同的泛化能力：

| 划分方式 | 检验的泛化能力 | 不能保证的 |
|---|---|---|
| 按完整会话留出 | 一次新的驾驶（新的日期、天气、交通） | 道路可能是训练中见过的 |
| 按地理区域留出 | 训练中没出现过的路段 | 同一次驾驶的其他路段可能在训练集里 |

道路重合并不会让会话划分失效，地理划分也不保证会话独立。**本项目主要研究稀疏观测下各模型的差异，建议以"按完整会话留出"为主。** 如果团队认为"没见过的道路"本身就是主问题，也可以以地理划分为主，但结论要相应限定。

三种候选方案。数字都按 1 Hz 的 GPS 点计算，只计同时满足以下条件的点：车速 > 3 m/s（由 GPS 推算）；0.5 秒内有视频帧；转角标签不跨 CAN 断档；会话 GPS 可用（128 个）。

| | 随机按会话 | **A. 按路线成组留出整段会话** | B. 2 km 地理区块 |
|---|---|---|---|
| 做法 | 会话随机分给 train/val/test | 只用 GPS 做确定性的贪心搜索：先找一组彼此常同路、与其余会话少同路的会话作为测试组，再从剩下的会话里用同样方法找验证组 | 区块随机分给 train/val/test（63/12/25%） |
| 测试集占比 | 26% | 25%（49 个会话） | 24%（20 次抽取中最少 19%），扣除缓冲和跨界窗口之后 |
| 测试点离 train/val 轨迹不到 50 m | 64%（20 次抽取中最少 55%） | 10% | 0%（缓冲已删掉） |
| 同一次驾驶是否同时出现在 train 和 test | 不会 | 不会 | 会：92 个会话两边都有 |
| 测试单位与有效数 | 会话 | 会话，Kish 约 20 | 区块，约 93 个，大小很不均匀，Kish 约 17。区块之间并不独立，这只是上限，不能与会话的 Kish 直接比较 |
| 回答的问题 | 新驾驶，但多数是熟路 | 新驾驶，而且多数是新路；但同一天的其他会话可能在训练集里，所以**不是**新日期、新天气 | 新路段，但不是新驾驶 |

如果 A 也按 B 的规则删掉缓冲区、丢掉跨界窗口：train 60%，val 12%，test 22%；44 个测试会话有剩余数据，Kish 约 18。两者留下的数据量相近。

**建议：选 A 作为主划分。**
- 它同时满足"新驾驶"和"多数是新路"两个条件，推断单位仍是行程，决定记录不用改；
- 测试会话的 Kish 有效数约 20，但它只校正会话大小不均，不反映路线层面的相依——这些会话正是按"彼此同路"选出来的。统计单位见下面"统计方法"一节：主分析按路线簇。区块的 Kish（2 km 约 17，1 km 约 40）同样只校正大小不均，不能与会话的 Kish 直接比较；
- 测试集里剩下 10% 离 train 或 val 轨迹不到 50 m 的帧，建议**保留在主结果里**，同时把测试集按"熟路 / 新路"分开报告，作为地理泛化的补充；
- 如果时间允许，再用 B 做一次"未见路段"的扩展实验。

**A 的代价：**
- 测试组是搜出来的，不是随机抽取的。搜索只用 GPS，不看转角，也不看模型输出，所以不会泄露答案。但结论只适用于这 49 个会话走的、被搜索挑出来的少重合路线，不能推广为任意没见过的路线；bootstrap 给出的不确定性也只以这组路线为条件。B 的区块是随机分配的，没有这个问题，这是 B 的优点。
- 测试组的构成与全体略有不同：≥ 15° 帧占 15.9%，高速（> 25 m/s）占 11.5%；128 个 GPS 可用会话合计分别是 16.9% 和 13.2%（137 个会话合计的 ≥ 15° 占比是 16.6%）。
- 20 天里有 13 天，训练集和测试集都有当天的会话，所以日期、天气只是部分独立。
- **测试组不是 49 个独立样本**：按"任一方向重合超过一半"聚类，49 个会话构成 26 个路线簇，有效簇数 9.5；执行同步核对的排除规则之后是 **21 个会话、17 个簇、有效簇数 8.3**（验证组 9 个会话、8 个簇、5.1）。统计要以路线簇为聚类单位。
- 验证组较弱：它是选完测试组后剩下的，只有 15 个会话、7 天，Kish 约 10。50 m 内与其他划分重合 23%，200 m 内 32%（测试组分别是 10%、17%）。D2 和编码器选择都用它，验证集上的结果可能比测试集乐观。

**不管选哪种，都要事先写定的规则：**
- **窗口与隐藏状态**：每个样本的历史窗口里，所有帧都必须与目标帧属于同一划分，而且都不在删掉的区域内，否则整个样本丢弃（不是从帧流里删帧）。逐帧推进、传递隐藏状态的评测，在会话开头、长断档之后和划分边界处都要重置状态。A 方案里会话整段属于一个划分，这一条自动满足；B 方案要逐帧检查。
- **弯道检查**：划分之前写定标准，例如每个划分中 ≥ 15° 帧的占比不低于全体的某个比例。不满足时怎么办也事先写定（A 换下一个起点，B 换下一个种子），并记录换了几次。检查只用标签、车速和位置。注意 A 方案的分组和构成已经算过（见上），标准是在看到这一结果之后定的，预注册里要如实写明。现在就把 A 的全部参数固定下来：50 m 半径、25 m 栅格、目标 25% / 12%、先 test 后 val，以及划分文件的 SHA-256。选组规则只用一个标准：把所有起点得到的不同分组按栅格重合从低到高排列，第一个就是主划分（测试组有 68 个不同候选，第一个是 0.114，其后是 0.121）；不满足弯道检查就取下一个。
- **缓冲的敏感性**：B 方案主结果用 50 m 缓冲，同时报告 200 m；A 方案的"熟路 / 新路"也按 50 m 和 200 m 各报告一次。
- **GPS 不可用的 9 个会话（9.5 小时，占 9%）**：建议主实验不用。其中 5 个没有 GPS，4 个 GPS 明显有误。不知道它们走的是哪些路，放进训练集就可能与测试集同路。
- **帧率混合的会话**（201703081749，37 分钟）：约 3/4 的时间是 15 Hz，其余是 30 Hz。建议主实验不用。
- **划分结果存哪里**：划分文件由 GPS 推出，都只存在服务器上仅持证人可读的目录里。公开仓库只提交生成脚本、参数、随机种子、划分文件的 SHA-256，以及各划分的汇总（会话数、小时数），用来证明划分在训练前已经固定。"会话 → 划分"清单同样由轨迹推出（在 A 方案里它就是划分文件本身），按本项目规定也不进公开仓库；以后如果要公开，先取得 HRI 书面同意。
- **数字的口径**：上表是按 1 Hz GPS 点估算的。固定划分时，要在逐帧时间戳上按最终规则重新计算保留比例。

**统计方法（写进预注册，这里只列候选）：**
- **主统计量**：整个测试集上两个模型的 macro MAE 之差，按帧加权。
- **敏感性**：按会话平均的版本。两者代表的东西不同：按帧加权时，长会话的权重大。
- **分解**：把主统计量按测试会话分解，每个会话的贡献是 Σ w_i (|e₁,i| − |e₂,i|)，e₁、e₂ 是两个被比较模型在第 i 帧的误差。w_i 用整个测试集的 macro 权重（`metrics.macro_weights`），各会话贡献之和恰好等于总差值。
- **不确定性**：主分析用 **seed × 路线簇**的配对重采样，同一簇内的会话一起抽取；按会话、按日期聚类作为敏感性分析。
- **聚类单位**：主分析按**路线簇**聚类，报告里写出簇数（排除后的测试集 17 个簇、有效数 8.3）。成簇规则：**任一方向**重合超过一半就并成一簇，即 A 有超过一半的点落在 B 的 50 m 内、或 B 有超过一半的点落在 A 的 50 m 内，不要求两个方向都满足。另做按会话、按日期聚类的敏感性分析。**如果按会话聚类和按路线簇聚类的结论不同，报告结果对推断方式敏感，不能只采用更显著的那个。**
- **最少帧数**：每个会话最少要有多少帧、帧太少的会话怎么处理，事先写定。

### 2. 训练和评测是否只用行驶中的帧

- 第一周检查的转角分布只统计了车速 > 3 m/s 的帧，占全部帧的 60%。
- 停车时方向盘可以打在任何位置，从一张前视画面几乎看不出来。把这些帧放进来，会改变各档转角的构成、常数基线和 CCC，从而影响 D2 能不能通过。
- **建议：只把车速 > 3 m/s 的帧作为预测目标，训练损失和评测都只算这些帧，在 D2 之前定下来。** 上面的划分数字已经按这个口径计算。
- 历史窗口里的帧不管车速都保留，Δt 用真实值。不能从帧流里删掉停车帧，否则每次停车都会在历史里留下一个人为的时间跳跃，和要研究的删帧条件混在一起。

### 3. 顺序与视觉编码器

**建议的顺序：**
1. **同步核对**：
   - Alex 目视抽查；
   - **已完成**（`hdd/sync_check.py --signal steer --max-lag-s 5`，2026-09-22）：画面与**转向角**对齐，134 个有可用窗口的会话时间差中位数 0.000 秒；横摆角速度那一路比画面晚约 0.1 秒，不能用来对时。搜索范围 ±2 秒与 ±5 秒结论一致，默认已改为 ±5 秒。
   - 第一版估计量是错的（先累积再互相关，归一化也用错），会把任何偏移都算成 0，已修正。
   - 现在的校准：真实窗口注入已知偏移，斜率 0.999；平滑合成信号注入 100/300/500/1000 ms，估计 100/300/499/999 ms；单元测试守住这条性质。
   - **验收规则（事先写定）**：一个会话算"已核对"，要求可用窗口 ≥ 10 且逐会话 |时间差| ≤ 100 ms。
     - **原属训练集的会话**：不论核对与否都保留在训练集。
     - **原属 val/test 的会话**：没通过核对的**整个排除**，不转入训练集——把靠近测试路线的会话挪进训练集会破坏 A 的地理隔离。
     - 按这条规则，A 的规模变成：

       | | 排除前 | 排除后 |
       |---|---|---|
       | test | 49 个会话，14.05 小时（占行驶数据 25.1%） | **21 个会话，11.01 小时（19.7%）** |
       | val | 15 个会话，8.31 小时（14.9%） | **9 个会话，5.98 小时（10.7%）** |
       | train | 64 个会话，33.57 小时 | 不变 |

     - 被排除的共 34 个会话、5.37 小时：test 里 26 个因窗口不足、2 个因时间差超 100 ms；val 里 3 个和 3 个。
     - **排除后的路线簇要重算，不能继续引用排除前的 26 和 9.5**：test 变成 **17 个簇、有效簇数 8.3**，最大簇占 28%；val 为 8 个簇、有效簇数 5.1。
       test 离 train/val 不到 50 m 的点也从 10.2% 降到 **6.3%**（200 m：17.3% → 13.1%）。
     - 固定划分时把这份排除清单写进划分文件。
   - 做法：`hdd/extract_features.py` 解码提取特征时顺带记下每帧的画面横向位移，再与 CAN 横摆角速度做互相关。全程只有数字，没有人看画面。
   - 注意：逐帧位移被量化到约 0.4°，单个窗口噪声大，要看逐会话中位数；对时的参照必须是转向角，不是横摆角速度。
2. ~~**D2**~~：**已通过（2026-09-22）**，见 [`results_2026-09-22_d2.md`](results_2026-09-22_d2.md)：三个 seed 的 skill +17.5%–21.9%、CCC 0.55–0.62，用满 6 次训练。ResNet-50 后备不需要了。原文：用已经提取好的 DINOv2 的 3 Hz 特征，确认"单张图像能预测转角"。ResNet-50 只在 DINOv2 未通过时才提取（顺序后备，不做双编码器比较）。
3. ~~**D3**~~：**已通过（2026-09-22）**，见 [`results_2026-09-22_d3.md`](results_2026-09-22_d3.md)：历史带来 32%–34% 的误差下降（门槛 5%），删 75% 历史回升 11%–14%（门槛 3%）。原文：用选定的编码器，只在一部分会话上提取 30 Hz 特征。确认历史信息确实有帮助，而且删掉历史帧会让时序基线变差：
   - 基线指 LSTM+Δt，不是单帧 MLP，因为单帧模型不读历史，删帧对它没有影响；
   - 通过条件：LSTM+Δt 在完整历史下明显好于单帧 MLP，而且随删帧率增大可测量地变差。数字待定。
   - **D3 的门槛（2026-09-22 拟定，待确认；确认后才跑）**：
     - **样本**：与 D2 相同的目标帧（车速 > 3 m/s、标签不跨 CAN 断档），训练集和验证集，测试集不动。
     - **历史窗口**：基础采样率 10 Hz，窗口 3 秒（30 步）。所有模型拿到相同的目标帧、相同的历史跨度和真实 Δt。
     - **条件**：完整历史（不删）、随机删 50%、随机删 75%。**三种条件用同一个 checkpoint 评测**：只删历史观测，当前帧始终保留，并按保留下来的帧重新计算真实 Δt。同一 batch 内只用同一种删点率（LTC 的 Δt = 0 会产生 NaN）。
     - **模型**：单帧 MLP（与 D2 同一结构）和 LSTM+Δt（隐层 64）。
     - **H1 历史有用**：完整历史下，LSTM+Δt 的验证 macro MAE 要比单帧 MLP 低 **≥ 5%（相对）**，且 3 个 seed 都满足。
     - **H2 删帧有影响**：LSTM+Δt 在删 75% 时的验证 macro MAE 要比完整历史差 **≥ 3%（相对）**，且 3 个 seed 都满足。
     - **预算**：每个模型族沿用 D2 的配方（3 次选学习率 + 3 个 seed），D3 合计最多 12 次训练。
     - **不通过怎么办**：H1 不过，允许一次预先声明的重试——把窗口延长到 6 秒，其余不变；仍不过就记为 D3 未通过，并如实报告"在这个设置下历史信息没有帮助"。
       H2 不过，说明删帧条件区分不了模型，D4 的主比较失去杠杆，要先重新设计条件再谈比较。
     - 两条门槛问的是不同的问题，不能互相推断：
       - 5% 不是"保证收益超过 seed 波动"。D2 里 seed 间的极差只是量级参考；D3 要看同一 seed 下 MLP 与 LSTM 的**配对差值**，它的波动另算。
       - 3% 也不等于更容易过。当前帧一直保留，历史冗余可能很高，删掉大量历史帧也许几乎不影响预测。
     - 以上数字在看到任何 D3 结果之前写定；跑完不得回头调整。
   - **必须在同一批目标帧上重新评测单帧 MLP 和 LSTM+Δt**：不能拿 3 Hz 的 D2 分数直接和 30 Hz 样本上的时序模型比。
4. **全量提取**：D3 通过后，再对全部会话提取 30 Hz 特征。
5. **D4**：预注册，然后正式比较。

**编码器：用 DINOv2 ViT-B/14 的冻结特征跑 D2；ResNet-50（ImageNet 预训练、同样冻结）只作顺序后备，DINOv2 未通过时才用。**

- **输入**：两种做法二选一。
  - 直接缩放到宽 392、高 224。原画面是 1280×720，宽高比 7:4，接近 16:9，横向压缩约 1.6%。
  - 等比缩放到宽 392，上下各补约 2 像素，没有变形。
  - 两边都是 14 的倍数（28 × 16 个 patch）。不再沿用 SullyChen 那种裁掉地平线的做法。
- **特征**：ViT 用 CLS token 与 patch token 均值**拼接**（768 + 768 = 1536 维，与 DINOv2 线性评估的做法一致）；ResNet 用全局池化（2048 维）。以 float16 存储。
- **读视频**：每次把一个视频从压缩包里临时写到磁盘，解码完就删掉。第一周检查已经用这种方式读过全部 137 个视频的元数据，不用额外占 112 GB 去解压。
- **成本**：
  - 压缩视频的帧与帧之间有依赖，只要 1/10 的帧也得把全部帧解码一遍，所以 D2 和全量提取各要完整解码一遍。
  - 吞吐量已实测（2026-09-22，5090，torch 2.14+cu130，392×224，fp16）：解码 4700 帧/秒，CPU 缩放 592 帧/秒，DINOv2 ViT-B/14 推理 1553 帧/秒，ResNet-50 推理 3356 帧/秒。单进程端到端 401 帧/秒；按会话切开跑 6 个进程约 4000 帧/秒。
  - 实际结果：3 Hz 子集（stride 10）137 个会话全部提取完用了 **50 分钟**，特征 3.6 GB。全量 30 Hz 估计约 2 小时（受 GPU 限制）。
  - 存储：ViT-B 全量约 35 GB，ResNet-50 全量约 46 GB；排除上面那 10 个会话后约少 10%。

**D2 的通过条件（2026-09-22 已定）：**

| 项目 | 规则 |
|---|---|
| 常数对手 | 用验证集最优常数作为**严格诊断**；同时报告在训练集上拟合、在验证集上评估的常数 |
| 数值门槛 | 3 个 seed 都满足 macro skill ≥ 5% 且 CCC > 0.3。**这是工程门槛，不是统计显著性标准**，报告里要这样写 |
| 曲线检查 | 弯道上的预测曲线作为**诊断**看，不作硬门槛。若要当门槛，必须事先量化（例如"峰值滞后不超过 X 帧"），不能"看起来滞后就判失败" |
| 编码器 | 先只跑 DINOv2（特征已提取）。通过就继续；不通过再按预定规则试 ResNet-50 |
| 调参 | 预测头只给有限且事先写明的搜索预算（学习率等），避免"默认配置没训好"就否定特征 |
| 额外报告 | 除总体分数外，给出逐会话结果，以及验证集中低重合片段（离训练轨迹 200 m 以外）的表现——验证集比测试集更靠近训练路线 |

**编码器是顺序后备，不是比较**：先跑 DINOv2；通过就用它，不再跑 ResNet-50，也就不需要平局阈值。
只有 DINOv2 未通过、且排查完数据与训练实现之后，才提取 ResNet-50 的同一批帧重跑同一套 D2 规则。
报告里据此写"按预设顺序选出可用特征"，不写"DINOv2 优于 ResNet"。

**预测头的调参预算（事先写定，照此执行）：**
- 结构固定：冻结特征 → LayerNorm → 一层隐藏层（512，GELU）→ 标量输出；
- 搜索只动学习率，取 {1e-3, 3e-4, 1e-4}，各跑 1 个 seed（共 3 次训练），按验证 macro MAE 选一个；
- 选定后用 3 个 seed 重跑（3 次训练），这 3 个 seed 的结果才用于 D2 判定；
- 上限：每次最多 30 个 epoch、早停 patience 5；**每个编码器最多 6 次训练**（3 次选学习率 + 3 个 seed 确认）。只有 DINOv2 未通过、触发 ResNet 后备时，总计最多 12 次。超了就记为未通过并说明原因。

### 4. 原始时间戳断档：怎么用

"断档"指 ROS 时间戳里帧间隔超过 1.5 倍中位数的地方（这里固定跳过每个会话开头 6 帧，所以个数与第一周检查的 92,440 略有不同）。它不一定都是相机丢帧，所以先用相机时钟核对了一遍：
- 92,432 处断档中，98% 在相机时钟上也是断档，说明确实少了帧；其余约 1,750 处只是接收晚了，没有缺帧。
- 按相机时钟估计共缺约 9.8 万帧，占 0.9%，大多只丢一帧；超过 1 秒的断档有 97 处，只出现在 7 个会话里（按 ROS 时间戳是 100 处、8 个会话）。
- 时间戳文件里的"计数器"除全部会话共 4 处外每行都加 1，约 9 万处相机时钟断档在它上面都看不出来。它是记录端的序号，不能用来判断丢帧。

**建议：**
- **Δt 用相机时钟算**。相机时钟的帧间隔非常规则；ROS 时间戳相对相机时钟的偏差，中位数 0.7 ms，99% 分位约 7 ms（各会话的中位数）。在那 15 个候选会话里，要等第 3 条的同步核对有了结论再定。
- 帧率混合的会话主实验不用（见第 1 条），也不放进补充条件。
- **主条件只用历史窗口里没有长断档的样本**，阈值事先写定，例如任何帧间隔都不超过 0.2 秒。
- **含长断档的样本单独组成补充条件**，名称叫"原始时间戳断档"，两者不重叠。用主实验训练好的同一批模型评估，只报告，不作为主检验：它样本少、集中在少数会话，而且断档可能和路况有关。
- **评测的任务**：断档结束、收到新画面之后那一刻的预测，与人工删帧条件一致。断档期间持续输出预测是另一个任务，本项目不做。
- 定阈值之前，先统计主条件因此丢掉多少样本，补充条件在测试集里有多少样本、来自几个会话。

### 5. 分工

决定记录里"负责人尚未分配"。**HDD 许可的状态（2026-09-22）：Alex 已获批，一位组员已获批，另两位尚未申请。**所以要接触数据的部分，现在可以由两个人分担；其余组员获批之前不能参与这部分。

| 部分 | 是否需要 HDD 许可 |
|---|---|
| 划分与数据流水线、同步核对 | 需要 |
| 视觉特征提取与 D2、D3 | 需要 |
| 时序模型：LSTM+Δt、CfC、LTC、Transformer | 写代码和单元测试不需要，可以用合成数据；在 HDD 特征上训练和评测需要，或者由持证人代跑 |
| 预注册与统计分析 | 写方法和代码不需要；在真实数据上跑需要许可。未获批的组员只能看公开仓库里那种总体汇总统计，不能接收逐会话或逐区块的结果 |
| README 与报告 | 文字部分不需要。README 和报告都在公开仓库里，按本项目规定不放 HDD 画面。如果课程提交的版本确实需要示例画面，只放在不进仓库的提交件里，由持证人制作，并模糊人脸和车牌（§4.c） |

LTC 的定位（2026-09-22 已定）：**LSTM+Δt、CfC、LTC 三者是核心比较，都属于最低交付**；Transformer 只保留课程高级概念所需的最小完整实验，
它的额外搜索和消融优先裁掉。决定记录里"LTC 排在 Transformer 之后、可因超时裁掉"的安排作废。

LTC 的注意事项：
- 仍然先做 D2、D3；LTC 的实现和核验可以并行进行；
- ncps 1.0.1 的 LTC 有四个已经核实的问题要先绕开，比 CfC 慢约 6.6 倍，见决定记录"顺序与关卡"一节的表格；
- **积分子步数要检查**：ncps 每个观测间隔固定积分 6 次，删帧后间隔变大，数值误差可能在稀疏条件下增大。同一组权重分别用 6、12、24 个子步评估，结果明显变化就改用更细的积分，否则"LTC 在稀疏时变差"可能只是积分太粗。细节见决定记录"顺序与关卡"一节的 LTC 表；表后另有几条比较规则（进入正式比较只看可行性、调参机会对等、计时方法、做不成不等于更差、差异不能只归因于积分方式）；
- 实现和测试可以现在就写；正式调参在 D3 通过之后开始，与 CfC、LSTM+Δt 同一预算。

## 二、只能由特定的人去办的事

| 事项 | 谁 | 说明 |
|---|---|---|
| 目视核对画面与转角是否同步 | Alex | D2 之前完成。在服务器上渲染几段叠加了转角曲线的短视频，包括 15 个候选会话中偏差最大的时段。本人下载观看，看完删除，文件不放进仓库目录 |
| 取得 HDD 使用资格 | 尚未申请的两位组员 | 见下方说明。已获批的组员要尽快决定怎么拿到数据：下载链接有有效期（Alex 的是 5 天） |
| 通知教授：更换数据集、新增 LTC 对照 | 待定 | 研究主题没有变，教授此前的两条裁定（CfC 可以作为 RNN 组件；固定窗口满足变长要求）继续适用。邮件是"告知"，不是"申请换题" |
| 问清 Advanced Concept | 待定 | 教授对原先的域适配增强是否算数回答"不确定"。现在的主要候选是带真实时间编码的 Transformer，这一点还没问过，建议在上面那封邮件里一并问 |

**关于 HDD 许可**（HRI Data Sharing Agreement，全文可向 Alex 索取）：
- §3.a 把使用限定在"数据接收方所在机构内部"的非商业研究。Alex 是以个人身份申请的，接收方就是 Alex 本人，所以同在多伦多大学也不算在内。
- 组员有两条路：
  1. 各自用大学邮箱申请：https://usa.honda-ri.com/dataset-request-form?dataset=hdd ；
  2. 由 Alex 向 HRI 书面申请，把组员加入本项目（§4.b 要求 HRI 书面同意）。
- **建议两条路同时走**：尚未申请的组员本周内各自提交申请；Alex 同时给 HRI 写信，问两件事：能否把组员加入本项目，获批后能否共用服务器上的副本。
- 获批之前，不要接触任何 HDD 数据，包括服务器上的副本、特征、逐帧转角和逐帧预测。
- **已获批的组员怎么拿到数据**，三种做法，各有代价：
  1. 用自己的链接下载一份到自己的存储（149 GB，链接有有效期）。最干净，但算力在服务器上，数据不在一起；
  2. 用自己的链接，直接下载到服务器上自己的账号目录里（需要服务器所有者另开账号，而且服务器是共享生产机）；
  3. 共用 Alex 那份副本。这在 Alex 这一侧仍然属于对外披露，按 §4.b 要先取得 HRI 书面同意。
  在没有 HRI 书面同意之前，默认按第 1 或第 2 种办。

## 三、不用等讨论、可以先做的事

需要 HDD 许可才能在真实数据上跑的：
- ~~**同步自动核对**~~：已完成，见第一节第 3 条。
- ~~**吞吐量实测**~~：已完成，数字见第一节第 3 条。
- **特征提取流水线**：已可用（`hdd/extract_features.py`），DINOv2 的 3 Hz 特征已经提取完（137 个会话，3.6 GB）；D3 要用的 30 Hz 子集等 D2 通过后再提。ResNet-50 只在 D2 未通过时才提。
- **划分脚本**：A、B 两种都写好，参数做成可配置，讨论定了再固定输出。

不需要许可的：
- **时序模型的实现和单元测试**。

## 四、已经确定的事实和约定

- **转角正负号**：沿用 HDD 原生约定，正值为右转。以后如果用 nuScenes 复现：
  - 用 100 Hz 的 `steeranglefeedback`，它的单位是弧度、正值为左转，读入时先换算成度，再取负号；
  - 两车转向比不同，5°/15° 的分档要另行核对；
  - `vehicle_monitor.steering` 的正负号文档没有写明，不要用。
- **车速**：CAN 车速是 km/h，读入时换算成 m/s。
- **GPS**：`rtk_pos.csv` 的表头写反了，实际是纬度在前、经度在后。`rtk_track.csv` 的航向列不可靠，不要用。
- **帧时间**：视频第 i 帧对应时间戳文件第 i 行。不用视频的标称 30 fps。每个会话开头 2–6 帧间隔异常，丢掉。相机时钟每 128 秒回绕一次，用之前要展开。
- **标签有效性**：
  - 100 Hz 转角插值到帧时间，但只在前后两个 CAN 样本相距不超过 50 ms 时才插值，否则这一帧没有标签，丢掉；
  - 按这条规则，落在 CAN 时间范围之外和 CAN 断档里的帧共约 7.4 万帧（0.7%）。有 4 个会话的 CAN 断档超过 1 秒，最长 32 分钟。
  - `hdd/week1_checks.py` 已经按这条规则改过。HDD 的 CAN、GPS、时间戳和划分统计都来自 2026-09-21 用当前三个 `hdd/` 脚本的同一次运行；视频容器的元数据（帧数、分辨率、帧率）由 `hdd/probe_videos.sh` 另外读取；SullyChen 和 ncps 的数字来自各自引用的文档。
- **服务器环境**：DL conda 环境，torch 2.14.0+cu130、torchvision 0.29、numpy 2.2.6、opencv 5.0，`ncps` 固定在 1.0.1。5090 上跑 DINOv2 必须设 `XFORMERS_DISABLED=1`，否则 flash-attention 报 CUDA 错误。升级前的环境规格备份在服务器的 `~/workspace/server-admin/DL_pip_freeze_20260922.txt`。
- **服务器只能用 `~/workspace`**：本项目在服务器上的所有东西（数据、仓库、缓存、临时文件）都放在 `~/workspace` 下。torch.hub、torch.compile、Triton、CUDA JIT 和 pip 默认会写到 `~/.cache`、`~/.triton`、`~/.nv` 和 `/tmp`，所以运行任何 `hdd/` 脚本之前先执行 `. hdd/env.sh`，把这些位置都改到 `~/workspace/.cache` 下；启动脚本已经自动做了这一步。
- **HDD 衍生文件放哪里**：
  - 特征、划分文件、GPS 轨迹、逐会话统计表、核对视频，一律写到服务器的 `~/workspace/hdd/` 下（仅本人可读），不要写进仓库工作目录；
  - `.gitignore` 已经补上了常见的特征、视频、划分和统计表格式，但它只是最后一道防线。
- **公开仓库**：
  - 只放代码和汇总统计；
  - 画面、特征、坐标、轨迹和由轨迹推出的划分文件一律不入库。原始数据及其衍生物属于 HRI 保密信息（§4.b）；不放画面是本项目比许可更严的规定，§4.c 只要求发表的画面模糊人脸和车牌；
  - 137 个会话 ID 中有 136 个已由 TRN 仓库公开，剩下 1 个在入库前单独决定。

## 五、以后再定

- nuScenes 复现做不做：D2 通过之后再定。
- 仿真闭环的 2–3 天可行性检查：决定记录已写明是加分项，时间和负责人待定。
- 旧驾驶流水线归档到 `driving/`。

## 六、风险与不同意见（待团队决定）

本节整理本轮提出的七条意见，并补充适用条件。**这些是会议讨论项，不自动替换第一节的方案或现有决定记录。** 特别是编码器选择顺序、特征提取时机和 LTC 优先级，讨论后需要同步修改正文，避免两套安排同时生效。

### 1. D2 不通过的预案需要提前写明

**风险。** A 主要留出低重合路线；更大的数据和冻结特征不保证单帧模型能在它上面稳定超过常数。SullyChen 已出现训练进步未迁移到验证路段的现象，但此前没有单独隔离道路变化的因果作用，因此应写成泛化风险，不能断言新道路是已证明的唯一病因。

**建议的处理顺序：**
- 保持 A 的划分不变，只在训练和验证数据上排查同步、标签、单位、预处理、特征读出及训练实现，预先约定诊断预算。
- 若采用第 4 条的顺序选择规则，DINOv2 不通过后再试 ResNet；不能无限更换编码器直到过线。
- 仍不过时，选择如实报告有效基线未建立，或明确修订研究问题为“熟悉道路上的新驾驶”。若选择后者，记录原 A 的失败、修订原因、日期和新划分，并在新的确认性评测前锁定方案。

在未看测试结果时，依据开发实验透明地修订设计是允许的；问题是隐藏修订或让测试集参与选方案。一旦某测试集用于决定换划分、条件或模型，它就不能继续被当作未参与开发的确认性证据。

### 2. 功效评估值得增加，但补救不能只靠 seed 或合并 A/B

**建议。** 先固定有实际意义的差异/等价界和允许调整的范围；D3 后，仅用开发数据的小规模结果估计方差，在最终预注册前评估可检测差异、等价检验功效及其敏感性。估计对象应是**模型间的配对误差差值**，并区分初始化波动和会话、路线间波动。验证会话也不多，不能把一次 pilot 方差当作精确已知值；应报告一组合理方差假设下的结果。

**需要修正的推论：**
- Kish 约 20 只反映数据量权重不均，不是已知的独立样本数，也不能单独推出本研究只能检出多大的差异。按路线簇算，排除后的测试集是 17 个簇、有效簇数 8.3，这才是功效估计该用的量级。
- 增加 seed 可以降低训练随机性对均值的影响，不能创造新的路线、日期或驾驶会话。如果主要波动来自路线，多跑 seed 的收益会很有限。
- A 与 B 使用同一批 HDD 数据，且回答的泛化问题不同。B 可以提供稳健性证据，但不能直接与 A 合并并宣称独立样本量增加。
- 功效不足时，可以在预先允许的范围内增加独立数据、调整资源分配或收窄结论；不能为得到“等价”而根据 pilot 结果放宽等价界，也不能用哪个模型领先来选择缺失率。

“未能区分”仍是一个带不确定性的结果，不必称为空结果。最终是否支持实际等价，应依据预先指定的检验/区间与等价界；规划阶段功效是否达到 80% 本身不是最终等价结论的判定规则。不同方差来源的区分可参考 [Accounting for Variance in Machine Learning Benchmarks](https://arxiv.org/abs/2103.03098)。

### 3. 研究问题与 LTC 优先级必须一致（2026-09-22 已定：三者同为核心比较，见第一节第 5 条）

**矛盾。** 研究问题明确包含 LTC，但当前决定记录允许 LTC 在 Transformer 之后、达到时间上限时被裁掉。

**两种可选定位：**
- 以 CfC 为主、LTC 为扩展：题目、摘要和主要假设都以 CfC 对 LSTM 为中心，LTC 未完成时不声称覆盖 LTC。
- 以 LTC 与 CfC 为核心：把两者都列为最低交付范围，先确保 LTC 正确实现并完成基本可行性检查。Transformer 保留一个能够解释、复现的最小完整版本，用于课程高级概念和参照，其额外搜索与消融可以缩减。

**本轮建议倾向第二种**，因为项目目标是评估 LNN 在驾驶中的应用，团队已明确关心 LTC。D1/D2 仍优先；如果 LTC 最终无法完成有效比较，就显式修订研究范围并披露原因。不能保留涵盖 LTC 的问题，却只提交 CfC 结果。Transformer 也不宜拖到最后一周才处理。

### 4. 明确最低交付与裁剪顺序

目前真正的约束包括人力和数据使用资格；工程工作可以用合成数据并行开发，真实数据运行仍按第二节已有安排执行。

**建议的精简版，待会议选择：**
- 编码器按固定顺序选择：先 DINOv2，通过 D2 即采用；只有不通过才尝试 ResNet。若这样做，报告称“按预设顺序选择可用特征”，不声称比较证明 DINOv2 优于 ResNet。
- 核心为 A 划分、一个冻结编码器、一套混合采样训练、单帧 MLP 与 LSTM/CfC/LTC，另保留最小 Transformer 实验。
- B 扩展、nuScenes 复现、仿真闭环默认不列入最低交付，核心完成后才另行安排。
- 自动同步检查复用特征提取的解码过程，减少重复读取；最终同步结论和有效样本掩码仍须在 D2 之前确定。
- 每项分配负责人、依赖和时间上限。不要用增加训练次数替代尚未完成的数据核验。

### 5. 提取顺序按全流水线实测决定

**对前一轮审查的修正。** “D3 通过之后才允许全量提取”不必作为硬规则。对于当前顺序解码方案，即使只保留约 1/10 的帧，解码工作也可能接近全量；先做 3 Hz 特征再补 30 Hz，可能重复支付解压和解码成本。

但 D3 不通过时，提前全量提取的代价也不只是约 35 GB 磁盘：还包括其余帧的推理、共享 GPU 占用、写盘，以及编码器或输入处理改变后的重做成本。

**建议先测两条实际流水线**：同一代表性片段上的“解码后只推理约 1/10 帧”和“解码并推理全部帧”。记录总耗时、解码/推理/写盘占用以及得到首批 D2 结果的时间；有流水线并行时以端到端结果为准。

- 若解码占主导，且缓存格式与预处理已确认，可以在第一轮就缓存完整 DINOv2 特征。
- 若推理占主导，或编码器/预处理尚不稳定，则沿用 3 Hz D2 → 局部 30 Hz D3 → 全量的顺序。
- 折中做法是先完成一小组代表性会话并尽早跑 D2，再决定是否扩大缓存。所有缓存保留会话、帧索引、时钟和模型/预处理版本的对应关系。

本规则只决定计算调度，不改变数据划分、测试使用纪律或 D2/D3 的判断标准。

### 6. D2 数字仍是待定的工程门槛（2026-09-22 已按下述规则定案，见第一节第 3 条）

CCC > 0.3、macro skill ≥ 0.05 目前是候选数值，没有被证明是适用于 HDD 的标准。需要在运行前讨论其实际含义、常数对手的拟合方式以及三个 seed 都通过的要求，并明确记录。

- D2 衡量“是否已经建立有效预测模型”；D4 的等价界衡量“两个模型之间多大差异不重要”。两者不能混用。
- 不按模型能否过线事后移动门槛。若开发后确需修订，保留旧结果并标明修订。
- “曲线明显滞后或幅度不足”也需要可复核的规则。可把曲线用于诊断，或事先确定量化判据；不能成为看到结果后随意放行或否决的隐性条件。

### 7. 对时钟异常设有限预算，但排除代价不能直接写成 2%

**已解决（2026-09-22）**：画面核对的结论是两个时钟之间有偏差，但画面与转角没有错位，所以不需要按阈值排除任何时段。下面关于排除代价的讨论保留，作为当初权衡的记录。

**同意避免为少量异常无限投入。** 可以复用解码过程计算低分辨率画面运动，配合抽查与 CAN 互相关做同步筛查，给自动核验设时间上限。互相关在低动态、低纹理或多峰情况下不一定能可靠估计时差，不能把相关峰当作同步正确的证明，或未经核验自动改标签时间。

按当前 [HDD 检查报告](hdd_week1_checks_2026-09-21.md)，最大残差超过 1 秒的有 13 个会话，超过 0.2 秒的涉及 15 个会话；约 2.2 小时是偏差超过 0.2 秒的时长，不能把候选异常范围只限定为前 13 个会话。

如果最终选择按预设阈值排除：
- 同时排除历史窗口经过异常时段的样本；逐帧评测需要在有效段起点重置状态并按既定规则处理历史不足。
- 因为窗口排除和边界处理，实际可用样本损失会高于原始时长的约 2%。
- 按 train/val/test、会话和转角分档统计保留量，检查损失是否集中在验证集、测试集或弯道。按 A 方案实测（`hdd/drift_exclusion.py`）：以 0.2 秒为阈值，加上 5 秒历史窗口，丢掉全部预测样本的 2.3%，但验证集丢 6.2%、训练集 2.1%、测试集 1.7%；以 0.05 秒为阈值，全部丢 4.7%，验证集丢 9.9%。被丢的样本中 ≥ 15° 的占比（0.2 秒时 17.6%）与全体（16.6%）相近，没有集中在弯道。15 个候选会话里，只有 6 个的异常落在预测样本上，其余 9 个只出现在停车或没有标签的帧上。
- 验证集本来就弱（见第一节第 1 条），再丢 6–10% 会削弱 D2。阈值定下来之前，要把这个代价和同步风险一起权衡。
- 相机时钟残差不是已确认的画面–CAN 错位。阈值排除是一项保守质量控制，并不能替代对保留数据的基本同步核验。

### 会议需要形成的结论

| 待定项 | 需要写入决定记录的内容 |
|---|---|
| D2 失败预案 | 排查预算、修订研究问题的条件（ResNet 作为顺序后备已定） |
| 核心模型 | LTC 是否属于最低交付；Transformer 的最低范围 |
| 门槛与统计 | D2/D3 数字、D4 实用差异/等价界、功效估计与预算调整规则 |
| 特征提取 | 根据实测选择分阶段或首轮全量提取 |
| 同步处理 | 检查预算、异常排除规则、最终窗口保留量 |
| 范围与人力 | 默认不做的扩展、各项负责人和截止时间 |
