# D4 预注册

日期：2026-09-22。状态：**已定稿**（试跑与方差估计完成后定下等价界和 seed 数）。**定稿之后不改；测试集在最终评测之前不碰。**

D1–D3 已通过：[第一周检查](hdd_week1_checks_2026-09-21.md)、[D2](results_2026-09-22_d2.md)、[D3](results_2026-09-22_d3.md)。

## 1. 研究问题与假设

**问题**：在驾驶视频的转向角预测中，LTC 与 CfC 相比于获得同样时间信息的 LSTM，在观测变稀疏时是否具有更好的精度与计算成本取舍？

两个假设分开，**不能互相替代**：

| | 假设 | 说明 |
|---|---|---|
| **主假设 H-A** | 在预先指定的稀疏条件下，CfC（以及 LTC）的 macro MAE 低于 LSTM+Δt | 这是主比较，检验"稀疏时更准" |
| **次假设 H-B** | 从完整历史到稀疏条件，CfC / LTC 的相对退化小于 LSTM+Δt | 鲁棒性补充。**本身预测就差的模型也可能退化很小**，所以 H-B 单独成立不算支持主问题 |

主比较的条件：**随机删除 75% 的历史观测**（当前帧始终保留）。删 50% 和完整历史作为次要条件一并报告。

## 2. 模型与参数配平

所有模型共享同一套冻结的 DINOv2 ViT-B/14 特征（3 s 历史、10 Hz 基础率、30 步），共享同一结构的投影层和读出头，只有时序模块不同。

| 模块 | 可训练参数（实测，输入 64、隐层 64） |
|---|---|
| 共享投影 Linear(1536 → 64) | 98,368 |
| LSTM+Δt：LSTM(65 → 64) | 33,536 |
| CfC（本仓库手写） | 33,024 |
| LTC（ncps 1.0.1，全连接接线） | 33,216（另有 8,192 个冻结掩码，单列不计入） |
| Transformer（最小实验，2026-09-25 补定，见第 9 节） | 33,472；一层、64 维、4 头、前馈宽度 128 |
| 共享读出头 Linear(64 → 1) | 65 |

- 三个时序模块相差在 1.5% 以内，是隐层 64 下的自然配平，不额外调整。
- **参数量按模块分列报告**（投影 / 时序 / 读出），不能只报总数，也不能只凭"隐层都是 64"就说配平。
- ncps 自带的 CfC 在同样配置下是 49,536，不用它；用仓库里手写的那个。

## 3. 训练与评测

- **训练**：混合采样增强——每个 batch 抽一种删点率（0 / 50% / 75%），同一 batch 内只用一种（LTC 的 Δt = 0 会产生 NaN）。所有模型用同一份数据、同一增强协议，各自独立训练。
- **评测**：同一 checkpoint 在三种条件下评测，只删历史观测，当前帧保留，Δt 按保留下来的帧重算。
- **窗口规则收紧**：窗口内最大步长 ≤ 0.2 秒（D3 用的是总跨度上限，训练集有 1.54% 的窗口因此混入断档）。
- **目标帧**：与 D2、D3 相同（车速 > 3 m/s、标签不跨 CAN 断档）。
- **补充条件**：原始时间戳断档（自然丢帧）的样本单独评测，只报告，不进主检验。
- **调参预算**：每个模型族 4 次选学习率（{1e-3, 3e-4, 1e-4, 3e-5}，各 1 个 seed）+ 8 个 seed 确认，最多 60 个 epoch、早停 patience 8。选学习率和早停都按三种条件下 macro MAE 的平均值。

## 4. 指标

- **主指标**：macro MAE（按 |转角| 分 < 5°、5–15°、≥ 15° 三档平均，`metrics.macro_mae`）。
- **次指标**：CCC、逐档 MAE、实测计算成本（同设备、同精度、同 batch、预热后同步计时；单窗口延迟与批量吞吐分开报，注明实现方式）。
- 主统计量：两个模型在**同一批目标帧**上的 macro MAE 之差（按帧加权），并按路线簇分解。

## 5. 统计方法与功效（定稿前必须先做试跑）

**不能直接用 D3 的方差估功效**：D3 只有 MLP 和 LSTM 的总体分数，没有配对差值，也没有区分 seed、路线簇、删帧掩码三种波动来源，而且没有经过混合采样训练。

**试跑已完成（2026-09-22，见 [`results_2026-09-22_d4_pilot.md`](results_2026-09-22_d4_pilot.md)）**：LSTM+Δt 与 CfC 各 3 个 seed，混合采样训练，保存了 19 万个目标帧的逐帧预测，每个删帧条件另用 5 组随机掩码。

配对差值（CfC − LSTM，macro MAE 度数）的方差分解：

| | 删 75%（主条件） | 完整历史 |
|---|---|---|
| 平均差值 | +0.210 | +2.034 |
| 跨 seed 标准差 | 0.178 | 0.175 |
| 同一 seed 内跨掩码标准差 | 0.035 | — |
| 跨路线簇的贡献标准差 | 0.069 | 0.219 |
| seed × 路线簇 bootstrap 95% 区间 | [−0.181, +0.620]，半宽 0.401 | [+1.078, +3.107]，半宽 1.015 |

**据此定下（不再更改）：**

- **等价界 δ = 0.5°**（约为主条件下 16° 的 3%）。理由：混合采样训练之后，删 75% 给 LSTM 带来的退化只有 0.38–0.65°；等价界不能大过我们要研究的效应本身，所以 1.0° 太宽，取 0.5°。
- **最终 seed 数 n = 8**（每个模型族）。外推半宽 0.245°，若真实差值在 +0.21 附近，区间上界约 0.45°，仍落在 ±0.5 之内；n = 3 的半宽 0.401 不够。测试集有 16 个路线簇（验证集只有 7 个），簇那一部分的波动还会更小，所以这个估计偏保守。
- **掩码不是瓶颈**：同一 seed 内跨掩码的标准差只有 0.035°，最终评测每个条件用 5 组掩码取平均即可。
- **训练预算随之放宽（对所有模型族对称）**：学习率网格加到 {1e-3, 3e-4, 1e-4, 3e-5}，每个 1 个 seed；最多 60 个 epoch、早停 patience 8。理由：试跑中 CfC 选中了网格最小的 1e-4，且有一个 seed 跑满 30 epoch 才停，说明原预算把它卡住了。选学习率和早停都用三种条件下 macro MAE 的平均值。
- **判定规则**：主统计量是删 75% 条件下 CfC 与 LSTM+Δt 的配对 macro MAE 差值，按路线簇分解，用 seed × 路线簇两层 bootstrap 给 95% 区间。
  - 区间完全落在 ±δ 之内 → 报告"实际等价"；
  - 区间不含 0 → 报告"有差异"，并给出方向和大小；
  - 其余 → 报告"未能区分"，不写"等价"。

**统计形式（拟定）**：
- 配对差值先在 seed 和掩码上平均，再按路线簇汇总；主检验用 seed × 路线簇的两层重采样。
- 敏感性分析：按会话聚类、按日期聚类。**若结论随聚类方式改变，如实报告敏感性，不取更显著的那个。**
- 等价界 δ 在看到任何测试集结果之前写定，且不得根据试跑结果放宽。功效不足时，报告"未能区分"，不写"实际等价"。
- 多重比较：主检验只有一个（H-A，在 75% 条件下 CfC vs LSTM+Δt）。LTC vs LSTM+Δt、LTC vs CfC、H-B 以及各条件曲线属于次要族，统一做 Holm 校正。

## 6. LTC 的额外要求

沿用[决定记录](decision_2026-09-21_keep_driving.md)的 LTC 表：batch 等于隐层数时的 Δt 错配、Δt = 0 的 NaN、`erev` 初始化固定种子、Δt 是否真的起作用，以及 2026-09-23 发现的第五条——每次优化步后必须调用 `apply_weight_constraints()`，否则参数变负、ODE 发散。另加：

- **积分子步数敏感性**：同一组权重，在最大间隔条件下分别用 6、12、24 个子步评测，结果明显变化就改用更细的积分重训。这是固定权重下的数值检查，不是三个独立模型的比较。
  - **判定标准（2026-09-24，检查运行之前写定）**：用 seed 0 的训练权重，在删 75% 条件下各取 5 组掩码的平均。若 6 与 24 个子步的验证 macro MAE 相差超过 **0.1°**（等价界 0.5° 的五分之一），判定积分未收敛，全部 LTC 改用 24 个子步重训；否则维持 6 个子步。只做这一轮检查，不在多个子步设置之间挑选。
- 若预算内得不到稳定结果，把 LTC 标为"未完成有效比较"，不能把粗积分的低分写成 LTC 能力不足。
- LTC 与 CfC 的差异不能只归因于"数值积分 vs 闭式近似"，两者不是同一网络的严格等价实现。

### 运行中的补充规则（2026-09-23，LTC 重跑之前写定，对所有模型族一样）

- 训练中出现非有限的损失或验证分数，立即停下，记为"发散"，保留发散之前最好的状态，不当作"没有改进"继续计数。
- 学习率网格里发散的格点**不参与**选择，无论它发散前的分数多好；四个格点都发散，就按上面的规则把该模型族标为"未完成有效比较"。
- 2026-09-22 至 23 的第一轮 LTC 结果（学习率网格 4 个格点、正式训练 3 个 seed）是在没有参数约束的情况下训练的，全部作废，
  文件保留在服务器的 `~/workspace/hdd/d4/invalid_ltc_no_weight_constraints/`，不进入任何分析。LSTM+Δt 与 CfC 不受影响。

### 积分子步数检查的结果（2026-09-24）：未通过，LTC 全部改用 24 个子步重训

按上面写定的规则，用 seed 0 的训练权重在验证集上评测（删 75% 条件各取 5 组掩码的平均）：

| 积分子步数 | 删 75% | 完整历史 |
|---|---|---|
| 6（原设置） | 17.563 | 17.141 |
| 12 | 17.725 | 17.133 |
| 24 | 17.961 | 17.147 |

- 6 与 24 在删 75% 条件下相差 **0.398°**，超过 0.1° 的门槛，判定积分未收敛。完整历史下三者几乎一样，说明问题出在观测间隔大的时候积分太粗——正是这项检查要防的情况。
- 按规则，**全部 LTC 改用 24 个子步重训**，学习率网格也重跑（在 6 个子步下选出的学习率不一定适用）。6 个子步的全部结果（网格 4 个格点、seed 0 完整、seed 1–7 各到第 8 个 epoch）留档在服务器的 `~/workspace/hdd/d4/ltc_unfolds6_superseded/`，**不进主比较**。
- 24 个子步时单个 LTC 进程要约 11.4 GB 显存（反向传播要保存每一步积分的中间结果），所以最多同时跑 2 个；批大小仍是 256，不因显存而改动，保持各模型族的训练协议一致。
- 预计耗时明显变长。由于每个 epoch 都有检查点，可以跨多个晚上续跑。

### LTC 改用编译后的积分单元（2026-09-24）

- 24 个子步时原生实现太慢（每个 epoch 约 18 分钟）、显存太大（每进程约 11 GB），所以用 `torch.compile` 编译 ncps 的 LTC 单元（输入映射、24 步积分、输出映射）。**数学完全不变**，用的仍是 ncps 自己的代码，差异只来自浮点运算的顺序。
- 验证（`hdd/ltc_compile_check.py`，在真实窗口上，6 种不同 batch 大小，训练与评测交替）：训练输出最大相对误差 1.1×10⁻⁶，所有参数梯度 1.3×10⁻⁵，评测输出 1.1×10⁻⁶；只编译了 3 张图，计时之后没有增加，说明没有悄悄退回原生模式。
- 效果：每个训练步从 114.9 ms 降到 18.9 ms（快 6.1 倍），峰值显存从 9.37 GiB 降到 0.53 GiB，所以 4 个网格点可以同时跑。
- 学习率 1e-3 和 3e-4 的第 1 个 epoch 是原生实现训练的，3e-5 的前 4 个 epoch 是在一次静态形状编译的尝试中训练的；之后都用动态形状编译版从检查点续跑。数值一致，所以不影响结果，这里如实记录。
- 编译只影响训练速度。第 4 节的计算成本比较另行测量，届时三个模型族要用同一种实现方式计时。

### 训练过程接入 SwanLab 记录（2026-09-25）

- 从这一天起，所有训练脚本（`hdd/d4_run.py`、`d2_train.py`、`d3_train.py`、`d4_pilot.py` 和根目录的 `train.py`）都通过 `tracking.py` 把每个 epoch 的数字发到 SwanLab。D4 的项目名是 `CSC413-D4`，每个检查点（模型族、学习率、seed、子步数）对应一个 SwanLab 运行；运行 ID 存在检查点里，所以停机后续跑接的是同一条曲线。
- 记录的内容：训练损失（标准化后的均方误差，按 epoch 平均）；验证集三种条件的平均宏平均 MAE（即选择准则）；每种条件各自的宏平均 MAE 和 CCC；当前最好成绩、已用的耐心值、每个 epoch 的耗时和显存峰值。final 阶段再加上 5 次掩码抽样后的最终评测。
- **只记录，不参与任何决定**：学习率选择、早停和第 5 节的分析仍然只读 `d4/dinov2/` 下的 JSON 和预测文件，和原来一样。
- 记录不影响训练结果：swanlab 从不碰 numpy 和 torch 的随机数；它给运行挑颜色和 ID 时会用 Python 自带的 `random`，`tracking.py` 在每次调用前后都会保存并恢复这个状态（`tests/test_tracking.py` 检查三种随机数在一次完整运行前后逐位不变）。训练损失在 GPU 上累加，每个 epoch 只同步一次。记录在检查点写盘之后才发出。
- 实测（2026-09-25，LSTM+Δt，学习率 3e-4，seed 0，真实数据，放在单独的测试项目里）：开启记录和关闭记录各训练 1 个 epoch，模型权重逐位相同，验证分数都是 20.291029517827507，三组随机数状态也相同；开启记录后停下、再从检查点续跑到第 2 个 epoch，接回的是同一个 SwanLab 运行，第 2 个 epoch 的验证分数 22.112，和之前不带记录时的续跑检查一致。
- 上传的只有汇总数字、运行配置、控制台输出和运行环境信息（主机名、软件包列表、GPU 用量等 swanlab 默认采集的内容）；控制台输出里只有汇总数字，不含会话 ID、画面、特征或轨迹。项目默认私有。本地副本写在服务器的 `~/workspace/hdd/swanlog/`，只有本人可读。
- 在接入之前已经开始的运行（LTC 的 seed 1 和 6），SwanLab 曲线从续跑的那个 epoch 开始；更早的 epoch 只在日志文件里。接入之前就已跑完的运行不在 SwanLab 上。
- 同时，final 阶段遇到已经写出结果 JSON 的 seed 会直接跳过。结果 JSON 只在训练结束后写出，而已完成的检查点不会再训练，所以重新评测只会得到完全相同的数，还会在 SwanLab 上多开一个运行。

## 7. 测试集纪律

- 定稿之前不提取测试集特征，也不做任何测试集评测。
- 最终评测只做一次：每个模型族用选定配置和最终 seed 数，在测试集上跑一遍，之后不再改动方案。
- 测试集一旦用于任何选择，就不再是确认性证据。

## 8. 原未定项的状态（2026-09-25 更新）

1. Transformer 最小实验已在第 9 节补定，按同一口径报告参数量。
2. LTC 已按第 6 节改为 24 个子步；新增的 24/48 步诊断按第 9 节执行。
3. 本文件的 commit hash 写进 README。

主检验（H-A：删 75% 条件下 CfC vs LSTM+Δt）的全部要素已经定完，可以开跑。

## 9. 测试前的补充决定（2026-09-25）

**记录时点**：LSTM+Δt、CfC、LTC 的 8 个最终 seed 已完成，团队已经看过三者的验证集比较；据团队报告，测试集尚未评测。本节补定原先留空的 Transformer 配置和成本测量细节，另增加一次 LTC 数值诊断。它不是在验证结果出现之前制定的规则。原主比较、等价界、学习率网格和已完成运行的结果保留。

### 9.1 Transformer 最小实验

- **定位**：课程补充实验，单列描述性结果，不加入第 5 节原有的主检验或次要检验族。课程是否认可它为 Advanced Concept，仍以教授确认的要求为准。
- **固定结构**：与其他模型相同的无仿射 LayerNorm、Linear(1536 → 64) 和 Linear(64 → 1)；中间一层 `TransformerEncoderLayer(d_model=64, nhead=4, dim_feedforward=128, dropout=0, activation="gelu", batch_first=True, norm_first=True)`，不另加末端 LayerNorm，不搜索架构。
- **时间信息**：用相对于当前目标帧的真实时间编码。令 `p_i = (t_i - t_target) / 0.1 s`，第 `2j` / `2j+1` 维分别为 `sin(p_i / 10000^(2j/64))` / `cos(p_i / 10000^(2j/64))`，`j=0..31`，加到投影特征上。删帧后仍用原时间戳，不能改成压紧后的序号。时间编码不增加可训练参数。
- **读出与掩码**：按时间排序，使用 causal attention mask 和 padding mask，只读当前帧对应的最后一个有效 token；禁用 nested-tensor 自动转换。用合成输入核对 padding 值改变不影响预测、当前帧不会被删掉。
- **预算与数据**：沿用第 3 节的混合采样、目标帧、窗口过滤、优化器及选择准则；4 个学习率各 seed 0 + 选定学习率的 seeds 0–7，共最多 12 次训练，60 epoch、patience 8。最终采用与其他模型相同的 5 组评测掩码。不追加架构搜索或时间编码消融。
- **参数量**：时序模块 33,472，投影 98,368，读出 65，总可训练参数 131,905；本地按上述 PyTorch 模块实例计数，服务器实现完成后再次核对。接口与掩码语义参照 [PyTorch TransformerEncoderLayer 文档](https://docs.pytorch.org/docs/2.14/generated/torch.nn.TransformerEncoderLayer.html)。

### 9.2 三个循环模型的计算成本

- **主表采用同一种实现方式**：LSTMCell、仓库 CfC 单元、ncps LTCCell 都用 eager 模式逐步运行，外层都是 Python 时间循环，不启用 `torch.compile`；LTC 使用 24 个积分子步。LSTMCell 复制现有 LSTM 的权重，不重新训练。
- **先核验再计时**：使用现有 seed 0 检查点，在相同验证窗口上核对转换后的输出与原推理路径一致（标准化输出 `rtol=1e-4, atol=1e-5`）；不通过就先修复转换，不比较错误实现的耗时。
- **测量范围**：主表只包含时序模块，使用已经放在 GPU 上的 64 维输入与 Δt；排除视觉编码、投影、读出、磁盘读取、掩码生成与 CPU/GPU 数据搬运。若另测投影到读出的整体耗时，单独列出范围。
- **固定条件**：同一 GPU、float32、同一 TF32 设置、`eval()` 和 `inference_mode()`；完整、保留 50%、保留 25% 三种条件均测。输入和掩码固定并共享，保留真实长度差异，注明 padding 后执行长度。模型每个窗口的状态都重新初始化。
- **报告**：batch=1 测单窗口延迟，batch=256 测吞吐量和峰值已分配显存；每种形状先预热 50 次，再测 200 次、重复 3 轮。同步后用墙钟记录一次完整调用耗时，报告中位数、p95 和窗口/秒。测量期间 GPU 无其他训练任务。
- cuDNN LSTM、编译版 CfC/LTC 的实际工程速度可以另表报告，标明各自优化路径，不与主表混合排名，也不把 eager 主表解释为硬件无关的架构速度。预热、同步及重复测量参照 [PyTorch benchmark 文档](https://docs.pytorch.org/docs/2.14/benchmark_utils.html)。

### 9.3 LTC 的 24 对 48 子步诊断：做一次，只评测

- **目的和边界**：第 6 节原定的一轮 6/12/24 检查已经完成。本项是看到验证结果后追加的数值诊断，用来检查最终 24 步训练权重的敏感性；不把它追溯写成原预注册检查，也不根据 48 步是否更准来选择设置。
- **固定输入**：采用最终 24 步训练的 seed 0 最佳检查点，权重不变；在同一验证目标帧上分别以 24、48 步推理。检查完整历史和删 75% 两种条件；删帧条件使用原来的 5 组共享掩码（种子 7、1007、2007、3007、4007）。不读取测试集。
- **判据**：两种条件各自的平均 macro MAE 差取绝对值，均 ≤ 0.1° 才记为“本次检查下稳定”；这不证明所有权重或所有时间间隔都已数值收敛。完整报告差值和逐掩码结果，不只写通过与否。
- **若通过**：继续用已训练的 24 步模型进行注册的性能和成本比较，不改成 48 步。
- **若不通过**：保留现有 24 步验证结果，标注“积分敏感性尚未解决”；按第 6 节的限制，不将其劣势解释为已完成有效比较后的 LTC 架构结论，也不启动 LTC 的确认性测试评测。不自动扩大到 96 步或重新训练 8 个 seed。若之后决定增加重训，须先另记方案修订及预算；CfC vs LSTM 的原主检验不因此改变。

### 9.4 执行顺序与分析检查

先做固定权重的 24/48 检查和 Transformer 实现核验，再按上述预算完成 Transformer；成本计时安排在 GPU 空闲时。最终测试只在对应配置与分析脚本锁定后执行，不在本次补充记录时启动。

测试前还须补齐第 5 节已经承诺的次要族 Holm 校正：当前 `hdd/d4_analyze.py` 分别输出每对模型的名义 95% 区间，尚未实现整个次要族的联合校正。现有验证表不能作为已做 Holm 校正的确认性结论。主检验仍只有 CfC vs LSTM（删 75%），不因 LTC 的验证结果改变比较族。

### 9.5 执行记录（2026-09-25）

**9.1 Transformer 的实现核验**（`hdd/d4_run.py` 的 `TransformerBlock`，`tests/test_d4_transformer.py`）

- 结构按 9.1 原样实现；直接用 `TransformerEncoderLayer`，不套 `TransformerEncoder`，所以不存在 nested-tensor 转换；另用 `torch.backends.mha.set_fastpath_enabled(False)` 关掉推理快速路径，训练和评测走同一套计算（测试核对两种模式输出一致）。
- 相对时间由 `forward_masked` 按删帧后保留下来的原始时间戳计算（`kept_t - t_target`）并传给模型；LSTM+Δt、CfC、LTC 不读这个输入（测试核对它们的输出与传不传、传什么都无关），也不改变任何随机数的消耗，已完成的检查点和预测不受影响。
- 服务器上（torch 2.14）实测参数量：时序模块 33,472，投影 98,368，读出 65，合计 131,905，与 9.1 一致。
- 合成输入的测试全部通过：padding 位置的特征和时间取任意值，预测不变；因果掩码下，改动后面的帧不影响前面位置的输出；删帧后的时间是原始时间戳而非压紧后的序号；目标帧总被保留且是最后一个有效位置，相对时间为 0。

**9.3 LTC 24 对 48 子步诊断**（`hdd/ltc_unfolds48_check.py`，结果文件在服务器 `~/workspace/hdd/d4/analysis/ltc_unfolds_24v48_seed0.json`）

| 条件 | 24 步 | 48 步 | 48 − 24 |
|---|---|---|---|
| 完整历史 | 17.7225 | 17.7005 | −0.0220 |
| 删 75%（5 组掩码平均） | 18.5476 | 18.6066 | +0.0590 |

- 逐掩码差值（种子 7、1007、2007、3007、4007）：+0.0577、+0.0622、+0.0555、+0.0637、+0.0560。
- 两种条件的差值绝对值都 ≤ 0.1°，按 9.3 记为**“本次检查下稳定”**，继续使用已训练的 24 步模型。这不证明所有权重和所有时间间隔下都已数值收敛；删 75% 条件下 48 步系统性地高约 0.06°，方向一致，如实记录。
- 核对：eager 实现的 24 步结果与存档的编译版最终评测逐掩码一致（完整历史 17.7225，各掩码到小数点后 4 位相同）。

**9.4 次要族 Holm 校正的实现**（`hdd/d4_analyze.py --family`，`tests/test_d4_analyze_family.py`）

- 主检验：CfC − LSTM+Δt，删 75%，仍按第 5 节的 95% 区间规则判定，不校正。
- 次要族（三个注册模型族都有预测时 m = 11）：CfC − LSTM+Δt 的完整历史和保留 50%；LTC − LSTM+Δt、LTC − CfC 的三种条件；三对的 H-B。Transformer 不进入任何一族。某一对没有评测时列为“未评测”，族的大小随之变小，不静默删掉。
- 每个次要比较由同一组 bootstrap 抽样给出两个 p 值：差值为 0 的双侧 p（`p_diff`），以及差值落在 ±δ 之外的 TOST p（`p_equiv`）；取 α = 0.05 时它们分别复现“95% 区间不含 0”和“95% 区间落在 ±δ 内”。p 值用 (计数 + 1)/(B + 1)，不会为 0。
- Holm 在整个次要族上做，“有差异”和“实际等价”两类结论各做一次；同一个比较的两个零假设（差值为 0、差值在 ±δ 之外）不可能同时成立。H-B 没有注册等价界，只做差异检验。校正后：`p_equiv` ≤ 0.05 → 实际等价；否则 `p_diff` ≤ 0.05 → 有差异；其余 → 未能区分。**这一口径由团队在 2026-09-25、测试评测开始之前确认。**
- bootstrap 次数不足以支撑校正时（最小可能的 p 值乘以 m 超过 α/5），脚本拒绝运行；注册的 10,000 次对 m = 11 足够。

**9.2 计算成本**（`hdd/d4_cost.py`，结果文件在服务器 `~/workspace/hdd/d4/analysis/cost_seed0.json`）

- 条件：RTX 5090，torch 2.14.0+cu130，cuDNN 9.24，float32，matmul 和 cuDNN 的 TF32 都关闭，`eval()` + `inference_mode()`；计时开始和结束时 GPU 上都没有其他进程。256 个验证窗口按固定种子从全部会话中抽取，掩码种子 7，三个模型族共用；batch=1 用其中第一个窗口。每种形状预热 50 次、200 次 × 3 轮，三轮中位数彼此相差不到 1%。
- 核验：三个模型族的 seed 0 检查点，逐步实现与原推理路径（LSTM 为 cuDNN `nn.LSTM`）在全部 6 种形状上都通过 `rtol=1e-4, atol=1e-5`；LSTMCell 的最大绝对误差 1.2×10⁻⁵，CfC 和 LTC 为 0。
- 主表（eager 逐步，只含时序模块；执行长度 = padding 后的步数，括号内为 batch 256 的平均保留步数）：

| 条件 | 执行长度 b1 / b256 | 延迟 b1：LSTMCell / CfC / LTC（中位数，ms） | 吞吐 b256：LSTMCell / CfC / LTC（窗口/秒） |
|---|---|---|---|
| 完整历史 | 30 / 30 | 0.608 / 1.682 / 39.37 | 417,340 / 134,403 / 6,353 |
| 保留 50% | 14 / 23（15.5） | 0.292 / 0.796 / 18.26 | 540,286 / 172,507 / 8,307 |
| 保留 25% | 6 / 15（8.3） | 0.133 / 0.353 / 7.90 | 810,083 / 265,065 / 12,634 |

- p95 与中位数相差不到 3%。batch 256 的峰值已分配显存在 104–157 MiB，其中超出输入和权重的部分最多 31 MiB（LTC）。
- batch=1 与 batch=256 的耗时几乎相同，说明这张表主要反映每一步发起的 kernel 数量（LTC 每步 24 个积分子步），而不是算力；按 9.2 的约定，不解释为与硬件无关的架构速度。
- 另表（各自训练时实际用的路径，不与主表排名）：cuDNN `nn.LSTM` 延迟 0.055 / 0.047 / 0.043 ms（完整 / 50% / 25%），吞吐 331 万 / 345 万 / 398 万窗口/秒；编译版 LTC 单元 3.08 / 1.46 / 0.65 ms，5.35 万 / 6.96 万 / 10.6 万窗口/秒；编译版 CfC 单元 1.78 / 0.85 / 0.37 ms，比 eager 略慢（逐步编译省下的 kernel 抵不过每次调用的检查开销）。三条路径都先通过了同样的输出核验。

**验证集上的试算**（2026-09-25，`d4_analyze.py --family --split val`，B = 10,000；验证集同时用于选学习率，只作预览，不是确认性结论）：主检验 CfC − LSTM+Δt（删 75%）+0.360 [−0.070, +0.822]，未能区分。次要族 m = 11，Holm 校正后全部为“有差异”：CfC − LSTM+Δt 完整历史 +1.98（Holm p 0.002）、保留 50% +0.87（0.004）、H-B −1.62（0.002）；LTC − LSTM+Δt 三种条件 +3.11 / +3.26 / +3.36（均 0.002）、H-B +0.25（0.011）；LTC − CfC 三种条件 +1.13 / +2.39 / +3.00（0.003 / 0.002 / 0.002）、H-B +1.87（0.002）。0.0002 是 10,000 次抽样能给出的最小 p 值，乘以 11 即 0.0022。

**测试评测的准备（2026-09-25，尚未执行）**

- `hdd/run_test_eval.sh test` 是唯一一次测试评测的完整流程：按训练集和验证集的原设置（DINOv2 ViT-B/14、stride 1、392×224）提取 21 个测试会话的特征；用 `make_cache.py --add` 只把测试会话追加进缓存，不改动已有会话的任何文件；按冻结的学习率给四个模型族共 32 个已完成检查点打分；然后跑注册分析（`--family`）和 Transformer 的描述性对比。它不做任何选择。
- 第一次运行时写下 `d4/TEST_EVALUATION_STARTED`，记录开始时间和各脚本、学习率选择文件的 SHA-256；之后再运行一律拒绝，只有 `--resume` 可以接着跑被打断的那一次。
- `d4_run.py` 用测试集时，只要有检查点没跑完就直接退出，绝不会把测试集当验证集去训练或早停。
- 验证集上的完整演练（`run_test_eval.sh rehearse`，用检查点副本，不记录到 SwanLab）逐位复现了已有结果：32 个模型的逐帧预测和逐掩码分数差值为 0，家族分析的全部估计、区间、Holm p 值和判定相同，Transformer 的描述性对比也相同。

**测试评测已执行**（2026-09-25 23:02–23:58，锁定版本 `dc4a36c`）：主检验为“实际等价”，结果见 [`results_2026-09-25_d4_test.md`](results_2026-09-25_d4_test.md)。此后本方案不再改动。
