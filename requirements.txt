# Pinned to the TRAINING machine, verified by scripts/smoke_test.py on 2026-09-19:
#   RTX 5090 (sm_120), driver 590.48.01, CUDA 13.1, Ubuntu 24.04
#   conda env `DL` at ~/miniconda/envs/DL, python 3.10.18
# The full test suite passes against these exact versions on that box.
#
# Blackwell note: read `nvidia-smi` before installing. With driver >= 580.65.06 the default
# PyPI wheel already ships sm_120 kernels and the widely-copied `--index-url .../cu128`
# advice is unnecessary. An older driver installs cleanly, reports the card correctly, and
# then fails every kernel -- scripts/smoke_test.py asserts a real on-device backward pass
# precisely to catch that, so run it before downloading 3.1 GB of data.
#
# Also verified on that box, and why smoke_test turns both off: PyTorch's Blackwell defaults
# are ASYMMETRIC -- cudnn.allow_tf32=True but matmul.allow_tf32=False -- so the convolution
# stack would run at a 10-bit mantissa while the CfC's linear layers ran true fp32, an
# asymmetric precision difference sitting inside the headline comparison.
torch==2.11.0
numpy==1.26.4
matplotlib==3.10.8
pillow==11.3.0
gdown==6.4.0

# The Mac development box runs torch 2.8.0 / numpy 2.0.2 on CPU and the suite passes there
# too; the pins above describe the environment that produced the reported results.
# Optional, not installed by this file: swanlab==0.7.15 for experiment tracking (tracking.py).
# Every training script runs untracked without it; --swanlab off skips it explicitly.
